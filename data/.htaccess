# Block all direct web access — this directory holds the enquiries database
# and a one-time admin credentials file, never meant to be served.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
