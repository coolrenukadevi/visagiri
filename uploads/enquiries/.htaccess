# Uploaded enquiry documents are only ever served through the authenticated
# admin/document.php script — never directly by the web server.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
