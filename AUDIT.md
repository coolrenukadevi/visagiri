# AUDIT.md — Visagiri 2.0 Pre-Build Audit

**Status:** Audit only. No Phase 1 (of the 18-phase build sequence below) has started. Waiting for **"START PHASE 1"**.
**Full findings-with-evidence version:** `docs/audit/2026-08-11-website-audit-and-migration-plan.md` (this file is the condensed, structured version requested for this brief).
**Legacy source preserved at:** `legacy-site/` (not deployed, reference only).

A hotfix has already been applied on top of the legacy export (see §3) to stop active lead loss while the real rebuild is pending — that work is done and is not part of the 18-phase sequence.

---

## 1. Current architecture

There is no existing PHP application to modernize. What ships today at visagiri.com is a **static HTML export of a purchased ThemeForest template** ("RouteX – Immigration and Visa Consulting HTML5 Template," reskinned by an agency called RRDevs), plus two trivial procedural mail scripts. Concretely:

- **71 HTML files**, no templating/includes — every page repeats the full header/nav/footer markup inline. No component reuse mechanism exists.
- **2 PHP files total**: `mail.php` (root) and `new/sendemail.php` — both single-purpose contact-form mailers, no framework, no routing, no classes beyond one small `ContactForm` class in `mail.php`.
- **No database.** No `PDO`, no `.env`/config file, no schema anywhere in the export.
- **No authentication, sessions, dashboards, or admin panel** in any form.
- **CSS**: a vendor'd Bootstrap + custom SCSS/CSS build (`assets/css`, `assets/scss`) — a real, reasonably modern component library (cards, breadcrumbs, counters, etc.), but authored for a generic "immigration consulting" template, not a SaaS/fintech direction.
- **JS**: vendor bundle (Swiper, WOW.js, odometer, vanilla-tilt, etc.) — animation/carousel libraries typical of a marketing template, not app logic.
- **A second, incomplete redesign draft lives at `/new/`** in the same production webroot — a different, newer template (multiple unedited `index-2.html`…`index-4.html`, `coaching-details.html`…`-6.html` demo variants) with its own broken internal links, not linked from the live site nav, but publicly reachable today.
- **Two vendor documentation folders are also live in production**: `/jina/documention/` and `/new/Doc/` — the ThemeForest template's own doc pages, publicly crawlable.

**Conclusion:** nothing described in Phases 9–14 of the brief (auth, dashboard, admin, consultant panel, document management, application system) exists in any form. This is a from-scratch build on top of a brand asset (logo/colors) and a page-topic checklist, not a migration of working functionality.

---

## 2. Existing pages (inventory)

38 root-level pages + 33 pages under `/new/` (draft, not counted as production IA). Grouped by what they're for:

| Category | Pages |
|---|---|
| Home / core | `index.html` |
| Visa category pages | `tourist.html`, `business.html`, `student.html`, `work.html`, `crew.html`, `dependent.html` |
| Document services | `documentation.html`, `onlineformfiling.html`, `commercial.html`, `non-commercial.html`, `educational.html` |
| Country directory | `country.html`, `country-details.html` |
| Company | `about.html`, `team.html`, `career.html`, `partner.html`, `testimonial.html` |
| Content | `blog.html`, `blog-details.html`, `faq.html`, `offers.html` |
| Transactional | `payment.html`, `support.html`, `sample.html` |
| Legal/policy | `privacy.html`, `refund.html`, `paymentpolicy.html`, `coc.html`, `grievance.html`, `hiringpolicy.html` |
| Utility | `404.html`, `sitemap.html`, `verifyforzoho.html`, `pricing.html` *(removed — see §3)*, `team-details.html` *(removed — see §3)* |

No page in this inventory represents auth, dashboard, tracking, or admin functionality — those are entirely new IA (see §6).

---

## 3. Broken elements (and what's already fixed)

**Already fixed in `legacy-site/` as a hotfix** (not part of the 18-phase rebuild, done to stop active business damage — full detail in `docs/audit/...`):
- `mail.php` sent every contact-form submission to the template vendor's inbox (`support@envato.com`) instead of Visagiri's, and had a field-name mismatch with the HTML form causing 2 years of silent PHP warnings in production. → now sends to `info@visagiri.com` / `accounts@visagiri.com`, form fields aligned.
- `pricing.html` and `team-details.html` deleted — confirmed unedited template demo content (generic SaaS pricing tiers; a bio page for the template's own placeholder staff member "Ashikur Rahman").
- `404.html` and `visa.html` had the literal unedited template title "RouteX - Immigration and Visa Consulting HTML5 Template" and empty meta descriptions, live and indexed — retitled and given real descriptions.

**Still broken, not yet touched (will be resolved by the rebuild, not worth patching in the legacy archive):**
- **Broken logo**: `assets/imgs/logo/logo.svg` is missing — the header logo doesn't render on several pages (confirmed visually via screenshot during the hotfix QA pass).
- **9 broken image references** total (missing country flags for Sri Lanka/Vietnam, 4 missing contact-page images, missing footer logo, missing logo in `/new/`).
- **~40 distinct broken internal links** across both the live nav and `/new/` — template demo links (`about-us.html`, `gallery.html`, `coaching.html`, `businessvisa.html`, etc.) that were never replaced with real Visagiri pages.
- **A second, more broken mail handler** at `/new/sendemail.php` (placeholder recipient `youremail@mail.com`, malformed `mail()` call) — left as-is since `/new/` won't be carried into the rebuild.
- **Zero SEO fundamentals**: no canonical tags anywhere, no schema markup anywhere, no `robots.txt`, no `sitemap.xml`.
- **10 pages are 100% Lorem-Ipsum-style filler text**, including core visa-category pages: `student.html`, `work.html`, `crew.html`, `dependent.html`, `documentation.html`, `onlineformfiling.html`, `commercial.html`, `educational.html`, plus `non-commercial.html`/`sample.html` (both headlined "Coming Soon").
- **Unverified/contradictory trust claims** live on `offers.html` and `partner.html` ("15+ years," "98% approval rate") contradicting each other and the site's own footer. Per your confirmation, the real fact is **"serving visa and travel-related requirements since April 2015"** — this replaces all of the above, and no percentage/approval-rate claim should be used anywhere.
- **69 country cards** on `country.html` are an unfiltered flag list (includes uninhabited territories like Ashmore and Cartier Islands, excludes named brief priorities like Canada, UK, UAE, Singapore, Schengen) — not a curated, business-relevant catalog.
- Dead newsletter/search forms (`action="#"`) on several pages — decorative only, matches the brief's observation that there's no working search today.

---

## 4. Missing functionality

Everything in Phases 9–17 of the brief's sequence is missing outright: user accounts, login/register/password reset, customer dashboard, visa search engine with structured results, database-driven country/visa-type pages, document upload/management, application workflow + reference numbers, status timeline, public application tracking, admin panel, consultant/role-based panel, payments, appointments, messaging/notifications, structured schema markup, sitemap.xml/robots.txt.

---

## 5. Existing useful content (keep, don't discard)

- **Brand palette and logo concept** (navy/gold, globe/passport/aircraft) — usable seed for the refined SaaS/fintech-leaning identity this brief asks for.
- **69 real country flag images** and the general card pattern — reusable once curated against real target-market priority and moved into a DB-driven model.
- **`tourist.html` and `business.html`** contain genuinely hand-written, on-brand marketing copy (not template filler) — adaptable rather than rewrite-from-zero.
- **The footer's legal line** — "A Unit of Tripgation® ... CIN: U63030UP2020PTC128661" — real and consistent, anchors About/footer/invoice copy.
- **Bootstrap-based CSS component set** (cards, breadcrumbs, accordions, forms) — reasonable implementation quality, just needs restyling to the new palette/direction rather than being rebuilt from scratch.
- Legal/policy pages exist in some form for all the categories this brief lists (Privacy, Refund, Payment Policy, Code of Conduct, Grievance, Hiring Policy) — need a legal-accuracy pass, not from-scratch authoring.

---

## 6. Recommended new structure

```
/public/                    → web root
  index.php
/includes/
  config.php                → DB + app config (outside public web root if hosting allows)
  database.php               → PDO connection
  auth.php
  functions.php
  security.php               → CSRF, input helpers
  header.php
  footer.php
/assets/
  css/
  js/
  images/
/pages/                      → public marketing/content pages
  home.php, about.php, contact.php, faq.php, blog.php
/visa/                       → database-driven country + visa-type routing
  index.php                  → single controller resolving /visa/{country}/{type}/ from DB
/visa-type/                  → cross-country visa-type explainer routing
/auth/
  login.php, register.php, logout.php, forgot-password.php
/dashboard/                  → authenticated customer area
  index.php, applications.php, documents.php, payments.php, appointments.php, messages.php, profile.php
/admin/                      → role-gated staff area
  index.php, countries.php, visa-types.php, applications.php, users.php, documents.php, payments.php, blog.php
/uploads/                    → private storage, not web-accessible directly; served via a signed/authenticated download endpoint
```

URL scheme (matches the brief exactly):
```
/                              → homepage
/visa/{country-slug}/          → country visa overview
/visa/{country-slug}/{type}/   → country + visa-type detail
/visa-type/{type-slug}/        → cross-country visa-type explainer
/track-visa/                   → public tracking
/dashboard/...                 → customer area
/admin/...                     → staff area
/blog/{slug}/                  → visa updates & guides
/about/, /contact/, /faq/, /privacy/, /terms/, /refund-policy/, /cookie-policy/, /grievance/, /disclaimer/
```

`/new/` and `/jina/` are not carried forward in any form — visual/UX reference only during design work, never deployed.

---

## 7. Database requirements

Normalized MySQL 8 schema, PDO + prepared statements throughout, matching the table list from the brief:

`users`, `roles`, `countries`, `visa_types`, `country_visa_types` (join table — which visa types a country actually offers, since this varies), `visa_requirements` (the versionable content block: eligibility, documents, process, fees, processing time — each row carries `last_verified_at` + `source_url`, never silently overwritten), `embassies`, `consulates`, `vac_centers`, `applications`, `application_status_history`, `documents` (document type catalog), `application_documents` (uploaded files per application — private storage, randomized filename, MIME/size validated), `appointments`, `payments`, `notifications`, `messages`, `support_tickets`, `faqs`, `blog_posts`, `blog_categories`, `contact_enquiries`, `audit_logs`.

Roles table backs: Super Admin, Admin, Visa Consultant, Document Reviewer, Finance, Customer Support — each with a distinct permission set as specified in the brief (consultants see only assigned applications; finance sees payments only, etc.).

Every table gets `created_at`/`updated_at`; soft-delete (`deleted_at`) on `applications`, `users`, `documents` where audit history matters more than hard deletion. Foreign keys enforced at the DB level, not just app level.

This schema needs your §7-style sign-off on scope (e.g., exact document type list, exact status timeline stages) before table DDL is finalized — I'll draft it as part of Phase 1/2 of the sequence below and flag anything still open.

---

## 8. Homepage redesign plan

Matches the brief's 11-section structure, built as one `pages/home.php` composed of includable section partials (not one monolithic file):

1. **Hero** — label "SMART VISA MANAGEMENT," headline "Your Visa Journey, Simplified.", subhead, three CTAs (Check Visa Requirements / Start Your Application / Track Application). Visual: original illustration/graphic treatment (passport + digital motif), not stock travel photography — matches the "Stripe/Wise, not travel agency" direction.
2. **Visa search widget** ("Find the Right Visa") — the core interactive component (§5 of the brief); built first as a component since the country pages, homepage, and header search all consume it.
3. **Popular destinations** — curated card grid linking to `/visa/{country}/`, seeded from the brief's named list (USA, Canada, UK, Schengen, Australia, Japan, South Korea, UAE, Singapore, New Zealand, China, Thailand) rather than the legacy's unfiltered 69-flag list.
4. **Visa services** — 8 cards (Tourist/Business/Student/Work/Family/Transit/Medical/Conference), each linking to `/visa-type/{slug}/`.
5. **How Visagiri Works** — 7-step timeline, restyled from the legacy's existing `counter2`/timeline component pattern rather than rebuilt from zero.
6. **Why Visagiri** — 6 feature blocks, text only, no invented numbers (matches the brief's explicit ban on unsupported claims).
7. **Application tracking** — enter application number → `/track-visa/`.
8. **Customer portal preview** — static preview of dashboard modules (Applications/Documents/Appointments/Payments/Messages/Notifications) + Login/Create Account CTAs.
9. **Latest visa updates** — DB-driven, pulls from `blog_posts` filtered to visa-relevant categories, each card showing title/country/published/updated/author.
10. **FAQ accordion** — DB-driven from `faqs`, filterable by category.
11. **Final CTA** — "Ready to start your visa journey?" → Start Application.

---

## 9. Customer dashboard plan

SaaS-pattern layout: fixed sidebar (Dashboard, My Applications, Documents, Appointments, Payments, Messages, Notifications, Profile, Support, Logout) + content area.

- **Dashboard home**: "Welcome back, [Name]" + 4 summary tiles (Active Applications, Pending Documents, Upcoming Appointments, Outstanding Payments) + application cards (Application ID, Country, Visa Type, Status, Progress %, Last Updated, Next Action).
- **Application detail**: visual status timeline (Created → Documents Submitted → Documents Verified → Payment Completed → Appointment Scheduled → Submitted → Under Processing → Decision Received → Completed), each stage stamped with date/time/consultant comment, sourced from `application_status_history`.
- **Documents**: per-document status (Required/Uploaded/Under Review/Approved/Rejected), upload widget with client + server-side validation, consultant re-upload requests with visible comments.
- **Payments**: outstanding/paid list, tied to `payments`.
- **Messages/Notifications**: threaded per-application messaging with consultants; system notifications for status changes.
- Session-gated throughout (`includes/auth.php`); every write action CSRF-protected; every document fetch goes through a private, authenticated download endpoint — never a direct public `/uploads/` URL.

---

## 10. Admin dashboard plan

Role-gated (`/admin/`, Super Admin / Admin / Consultant / Document Reviewer / Finance / Support), sidebar-driven, mirroring the customer dashboard's visual language so the whole platform feels like one system rather than two bolted-together products.

- **Admin dashboard home**: operational counts (Total/New Applications, Pending Documents, Under Review, Appointments, Payments, Completed, Open Support Requests).
- **Content management (this is the load-bearing feature of the whole platform)**: Countries, Visa Types, Visa Requirements, Embassies/Consulates/VACs — all editable through forms, never by touching PHP. Every requirements record has a mandatory "last verified" date + source field enforced at the DB/form level, so stale or unsourced info can't silently ship.
- **Operational management**: Applications (assign consultant, view/change status, timeline), Customers, Documents (approve/reject/request re-upload with comment), Payments (status, invoice generation), Appointments, Consultants (assignment/workload).
- **Content/marketing**: Blog posts + categories, FAQs, Testimonials (kept out of public view until a real, attributable, consented testimonial exists — no placeholder testimonials ship).
- **Enquiries**: contact form submissions land here (not just email) so nothing gets lost the way it has been.
- Role permission checks enforced server-side on every admin route, not just hidden in the UI; every state-changing action writes to `audit_logs`.

---

## Next step

**Phase 2 (Architecture) is now complete**, built on top of this audit:

- Folder skeleton matching §6: `public/`, `includes/`, `pages/`, `auth/`, `dashboard/`, `admin/`, `visa/`, `visa-type/`, `countries/`, `blog/`, `uploads/`.
- `includes/config.php` — `.env`-driven config (no secrets committed; `.env.example` documents the required variables), `includes/database.php` — PDO connection (prepared statements only, exceptions on error), `includes/security.php` — CSRF tokens, security headers, a basic rate limiter, `includes/auth.php` — hardened sessions, `password_hash()`/`password_verify()` wrappers, `require_login()`/`require_role()` guards, `includes/functions.php` — shared helpers (`e()`, `redirect()`, `slugify()`, application-number generator).
- `public/index.php` — single front controller routing every URL from the §6 URL scheme to its handler; `public/.htaccess` — forces HTTPS, routes everything through the controller, blocks dotfile access.
- `database/schema.sql` — all 23 tables from §7, with foreign keys, indexes, timestamps, and soft-deletes on `users`/`applications`/`application_documents`; seeded with legitimate catalog data only (roles, the 8 visa types, the document-type catalog, blog categories) — no country content, fees, or company statistics.
- **Verified**: every PHP file passes `php -l`; the router was exercised end-to-end with `php -S` — content routes return their scaffold stub with 200, `/dashboard/` and `/admin/` correctly redirect unauthenticated requests to `/login/` (302, with security headers and a hardened session cookie already present), a genuinely missing route returns a real 404 (caught and fixed a bug during this pass where the 404 page was silently returning HTTP 200). `schema.sql` was reviewed by hand for FK ordering and type-matching (all 23 tables checked) but **could not be executed against a live MySQL server** — none is available in this sandbox. Recommend running it against a real staging DB before Phase 7 data entry begins.
- Each page/route handler currently renders a plain-text scaffold stub identifying itself and the phase that will replace it with real content — deliberately not styled or worded like a real page, so nothing resembling "Coming Soon" placeholder content risks shipping.

**Phase 3 (Design System) is now complete** — see `docs/design-system.md` and `docs/design-system-preview.html`. Tokens (`public/assets/css/tokens.css`), base/reset (`base.css`), and a full component library (`components.css`: buttons, cards, badges/status pills, forms, alerts, breadcrumbs, a zero-JS accordion, tables, the application status timeline, sidebar nav, pagination) are built entirely from the logo palette, verified by rendering the preview page at desktop and mobile widths with zero console errors.

**Phase 4 (Header/Footer) is now complete:**

- `includes/header.php` — sticky global header: logo wordmark, a "Visa Services" dropdown (Tourist/Business/Student/Work/Family/Transit — consolidates the brief's "Visa Services"/"Visa Types" nav items into one clean mega-menu instead of two redundant top-level entries), Countries/Visa Process/Visa Updates/About, search icon, Login/Sign Up (or account name + Logout when signed in — pulled from `current_user()`), gold "Start Application" CTA. Compacts on scroll via vanilla JS; full `<head>` (title/description/canonical/OG tags) driven by variables each page sets.
- `includes/footer.php` — 4-column footer (Visa Services / Explore / Company + brand column), the required "visa decisions are made by the embassy/consulate, not guaranteed by Visagiri" disclaimer, and the bottom bar with the confirmed facts (Tripgation Pvt Ltd, since April 2015, CIN U63030UP2020PTC128661).
- Mobile: hamburger toggles a full nav panel (with a working burger→X animation), no horizontal scroll.
- Added `/visa-process/` as a new route (was implied by the header nav but didn't exist yet).
- Every existing route stub (home, about, contact, FAQ, legal pages, track, countries, visa, visa-type, blog, login, register, logout, forgot-password, dashboard, admin, 404) now renders inside this real header/footer instead of plain text — bodies are still an explicit "Content pending — Phase X" notice, not real copy.

**Verified, and 2 real bugs caught and fixed in this pass:**
1. PHP's built-in dev server routes *every* request (including real CSS/JS files) through the front controller unless it explicitly declines — `public/index.php` was missing that check, so `/assets/css/main.css` 404'd. Fixed with the standard `PHP_SAPI === 'cli-server'` bypass (Apache in production already handles this via `.htaccess`, so this only affects local dev).
2. A too-broad CSS selector (`.site-header__nav ul`) leaked the horizontal flex layout into the nested dropdown menu, making "Visa Services" render as a cramped horizontal row instead of a vertical list. Scoped to `.site-header__nav > ul` and reverified.

Confirmed via headless Chromium: dropdown opens correctly, scroll-compact class toggles correctly (verified directly since the short stub pages don't have enough content to scroll on their own), mobile menu opens/closes, zero console errors on desktop or mobile.

**Phase 5 (Homepage) is now complete**, and along the way this session finally got access to a real MySQL-compatible server (MariaDB, installed locally for testing) — so `schema.sql`, which Phase 2 could only hand-review, has now actually been executed for the first time: it loaded cleanly with zero errors across all 23 tables, and foreign keys/joins were verified with real inserts.

**Real (non-fabricated) catalog data added to the seed:**
- 12 popular-destination countries (name + standard ISO codes only — the exact list from your brief) — no visa requirements, fees, or processing times seeded, since those need verified sourcing in Phase 7/8, not invented here.
- Short factual one-line descriptions for the 8 visa types (what each category means, not a claim about Visagiri).
- 5 general, non-country-specific FAQs (how search/tracking/documents work, and one that directly restates the "Visagiri does not guarantee visa approval" disclaimer) — no fabricated stats or country-specific rules.

**Homepage built** (`pages/home.php`), all 11 sections from your spec, DB-driven wherever real data exists:
1. Hero — original inline-SVG illustration (abstract passport/globe motif in brand colors), not stock photography
2. Find the Right Visa search widget — floats over the hero, dropdowns populated from the real `countries`/`visa_types` tables (full search logic is Phase 6, per your own sequence — this phase is the UI + real catalog data)
3. Popular Destinations — the 12 seeded countries, flag emoji + link to `/visa/{country}/`
4. Visa Services — the 8 seeded visa types with real descriptions, linking to `/visa-type/{type}/`
5. How Visagiri Works — 7 steps
6. Why Visagiri — 6 feature blocks, zero invented numbers
7. Application tracking band
8. Customer portal preview (visual dashboard mockup + Login/Create Account)
9. Latest Visa Updates — queries `blog_posts`; renders an honest **empty state** ("no updates published yet") since there is no real content to show — not a single fabricated article
10. FAQ accordion — the 5 real seeded FAQs, zero-JS native accordion
11. Final CTA

**Verified against the real database**, not mocked: page loads with real SQL query results, zero PHP warnings/errors in the response, zero browser console errors on desktop or mobile, no horizontal overflow on mobile (checked programmatically, not just visually).

**Phase 6 (Visa Search) is now complete:**

- `/visa-search/` — validates the submitted country/visa-type slugs against the real catalog (never trusts raw input as a redirect target) and sends the visitor to the canonical `/visa/{country}/` or `/visa/{country}/{type}/` page; invalid input gets a flash notice and a safe fallback to `/countries/`. The homepage widget now posts here instead of the Phase 5 placeholder.
- `/visa/{country}/` and `/visa/{country}/{type}/` are now real, DB-backed pages — not stubs. The detail page renders the brief's full spec sheet (eligibility, documents, process, processing time, fees, validity/stay/entry type, biometrics/interview, notes, last-verified date, official source link) whenever a verified `visa_requirements` row exists. **None do yet** — zero were fabricated — so every combination currently and correctly shows an honest "Requirements not yet verified" notice with a Contact CTA, ready to switch on the moment Phase 7/8 adds real, sourced data.
- Added, per your request: a 5th footer column, **Legal & Support** (Privacy, Terms, Travel Terms, Payment Policy, Refund Policy, Cookie Policy, Grievance, Sitemap, Track Request, Payment Link, Contact Support), plus the new routes it needed (`/travel-terms/`, `/payment-policy/`, `/track-request/`, `/payment-link/`, `/support/`) and a fully real `/sitemap/` page (simple enough to build for real rather than stub).

**Verified, and one more real bug caught:** a CSS class (`.hero__actions`) was reused outside the hero for a button row, silently inheriting white-on-white button styling from the hero's dark-background context — invisible button, caught by screenshot review, fixed with a proper `.button-group` utility. Also ran an end-to-end QA pass by temporarily inserting one throwaway test requirements row directly into the local dev database (never touched `schema.sql`, never committed) to confirm the populated-data template renders every field correctly, screenshotted it, then deleted the row and confirmed the page correctly reverted to the honest empty state.

---

**⚠️ Flagging before continuing — please read:** a large new "master prompt" arrived mid-Phase-6 with two direct contradictions to everything built so far:

1. **Tech stack**: it recommends Next.js/React/TypeScript/Node.js/PostgreSQL/headless CMS/Algolia. Every phase so far (1–6) is vanilla PHP 8 + MySQL, per the original brief's explicit and repeated instruction ("the entire website must remain PHP based... do NOT convert to React/Next.js/Node-only"). These are irreconcilable — I have not switched anything.
2. **Tagline**: it proposes "Your Gateway to the World." — the tagline used everywhere so far ("Seamless Visas. Limitless Journeys.") comes directly off your uploaded logo image itself, not something I chose. I have not changed it.

The new prompt also includes what read as specific new business facts (two office addresses in Noida and Patna, WhatsApp numbers) and a much larger 200+-country scope, a mega-menu nav (dropping "Visa Process"/"Visa Updates" in favor of "Visa Information"/"Business & Corporate"/"Resources"), and many new feature ideas (AI assistant, document checklist generator, Algolia-style search). None of this has been applied yet — I paused to confirm scope rather than guess on a change this size. See the chat message accompanying this commit for the specific questions.

**Resolution (same session):** proceeding on the recommended defaults — PHP/MySQL stack, "Seamless Visas. Limitless Journeys." tagline, new scope folded in incrementally as plain PHP.

---

## Phase 7 (Country Database) — complete

- **Countries catalog expanded from 12 to 208** — the original 12 popular destinations untouched, plus 196 more countries/territories added as validated, standard ISO 3166-1 reference data (name, ISO2, ISO3, region) across the requested 6-region taxonomy (Asia, Europe, North America, South America, Africa, Oceania). Generated via a small validation script that asserted zero duplicate slugs and zero duplicate ISO2 codes before writing to `schema.sql` — confirmed against the real database after reload (208 rows, all unique). No visa-requirement content was seeded for any of them — same rule as Phase 5/6, real per-country requirements need verified sourcing, not invention.
- **`/countries/` is now a real, DB-driven directory**: countries grouped by region, a "Popular Destinations" quick row, region jump-links, and a live search filter (vanilla JS).
- **Added Attestation Services** (Apostille, MEA Attestation, Embassy Attestation, Commercial Attestation) as their own service line — `/attestation/` index + `/attestation/{service}/` detail pages, in both the header nav and a new footer column — matching real services found in the legacy-site audit (apostille/document-verification pages existed on the old site).
- **Footer rebuilt to 6 columns** per your requests: Brand, Visa Services, Attestation Services, Explore Countries (regions + "Explore 200+ Countries →"), Company (About/Why Visagiri/Contact/Careers/Partners/Corporate Services), Resources (Visa Information/FAQs/Travel Guides/Visa Updates/Document Checklists/Blog) — with the Legal & Support links moved to a compact inline row below the disclaimer rather than a crowded 7th column. New stub routes added: `/careers/`, `/partners/`, `/corporate-services/`, `/document-checklists/`.
- **Two real bugs caught and fixed**: (1) the country directory's live-search JS was inline and got silently blocked by our own `script-src 'self'` CSP header — moved to an external file rather than weakening the policy; (2) none in header/footer this round beyond that.
- **Logo file**: you've now shared the actual Visagiri logo image twice in chat, but it has no filesystem path in this environment — only the zip you uploaded earlier is readable as a file. The header/footer still use a CSS text wordmark. To wire in the real asset, it needs to arrive as an actual file upload (same mechanism as the site export zip), not inline chat content.
- **Open item**: "since April 2015" vs. "since 2014" — you've stated both at different points; holding April 2015 as authoritative (it was an explicit correction) until you confirm.

Verified: all new/changed PHP passes `php -l`; full route sweep against the real database (200s for valid routes, 404s for invalid country/attestation slugs); zero browser console errors after the CSP fix; live search confirmed working (filters 208 entries correctly); footer/header verified via screenshots at desktop and mobile.

---

## Phase 8 (Country Pages) — complete

Most of "country pages" was already delivered in Phase 6 (the single reusable `/visa/{country}/{type}/` template — never hundreds of hand-written files, exactly per the brief). Phase 8 filled in what was still missing from the full spec:

- **Embassy / Consulate / VAC information** — new `fetch_country_contact_points()` + a shared `includes/contact-points.php` partial, rendered on both the country overview and visa detail pages. Zero embassy/consulate/VAC rows are seeded (same rule as visa requirements — addresses and contact details need verified sourcing, not invention), so every country honestly shows "hasn't been published yet" with a Contact CTA today. Verified the populated-state template renders correctly using one throwaway test embassy + VAC row in the local dev DB, screenshotted, then deleted immediately (never in `schema.sql`, never committed).
- **Contextual FAQs on the visa detail page** — `fetch_relevant_faqs()` pulls the general FAQs plus any tagged specifically to that country or visa type (none tagged yet, so the 5 general ones surface everywhere, which is honest and correct).
- **Country overview page improved**: a neutral, templated intro paragraph (no per-country facts invented) and a region badge.
- **About page went from stub to real content**, using your confirmed facts: the "Since April 2015..." line you provided, the Tripgation/CIN facts, and a "Why Visagiri" section — pulled from a new shared `why_visagiri_features()` function so the homepage and About page can never drift out of sync with each other.

Verified: `php -l` on every changed file, full route sweep, zero console errors, and the temporary-test-data QA technique (insert → screenshot → delete → reconfirm empty state) used again for the embassy/VAC template.

**Still open**: the real logo file (needs to arrive as an upload, not chat content) and, now resolved, the founding-date conflict — **April 2015 confirmed correct**, applied to the About page.

---

## Phase 9 (Authentication) — complete

Real, working authentication — not stubs. `/register/`, `/login/`, `/logout/`, `/forgot-password/`, and a new `/reset-password/` (the link destination, needed to complete the flow — wasn't a named route in the brief but is required for "forgot password" to mean anything).

- **Registration**: full name, email, mobile, password/confirm — server-side validated (email format, mobile format, 8+ char password, match check, duplicate-email check backed by the DB's UNIQUE constraint as a race-condition guard), `password_hash()`, auto-login on success, rate-limited per IP.
- **Login**: email-or-mobile + password, generic "Invalid email/mobile or password" on failure (never confirms which emails exist), session ID regenerated on success (fixation prevention), "Remember me" extends the session cookie to 30 days, `last_login_at` updated.
- **Forgot/reset password**: a real, secure token flow — random 32-byte token, only its SHA-256 hash stored, 1-hour expiry, single-use (cleared on successful reset). **No email-sending integration exists yet anywhere in this project**, so the reset link can't actually reach an inbox; the confirmation page always shows the same generic message either way (no user enumeration), and only when `APP_DEBUG` is on (never in production) does it also print the link directly, so the flow is fully testable without pretending mail delivery works.
- **CSRF** on every POST (419 on missing/invalid token, verified by testing a request with no token).
- **Every route already built in Phase 2** (`require_login()`/`require_role()` on `/dashboard/` and `/admin/`) now gates a *real* session instead of just "nobody's logged in" — verified a real customer account gets into `/dashboard/` and correctly gets a 403 on `/admin/`.

**A real security bug was found and fixed during this phase's verification**: the rate limiter built in Phase 2 stored its counters in `$_SESSION`, which meant an attacker who simply didn't send their session cookie back between attempts got a fresh, empty counter every time — the login rate limit was trivially bypassable and wasn't actually protecting anything. Fixed by moving to a new DB-backed `rate_limits` table, keyed by identifier+IP rather than session. Confirmed the fix by literally attacking it: 6 login attempts using a fresh cookie jar for each one (i.e. simulating an attacker who never persists cookies) — attempts 1–5 got the normal "invalid password" response, attempt 6 correctly got "Too many login attempts." A first attempt at the DB-backed fix also had a bug (comparing a MySQL timestamp string via PHP's `strtotime()` against PHP's `time()`, which silently breaks under DB/PHP timezone mismatches and reset the counter on every call) — fixed by moving the entire expiry comparison into a single atomic SQL statement so it happens in MySQL's own clock domain.

**Full flow verified end-to-end against the real database** with a throwaway test account (registered, logged in with correct/wrong password, tested duplicate-email rejection, tested CSRF rejection, ran the complete forgot-password → dev-mode reset link → set new password → old password rejected → new password accepted chain, confirmed the reset token can't be reused a second time) — then the test account and all rate-limit rows were deleted, nothing left behind.

---

## Phase 10 (Customer Dashboard) — complete

A real SaaS-style dashboard at `/dashboard/`, matching the brief's exact spec: sidebar (Dashboard, My Applications, Documents, Appointments, Payments, Messages, Notifications, Profile, Support, Logout), 4 stat tiles (Active Applications, Pending Documents, Upcoming Appointments, Outstanding Payments — all real DB counts, not placeholders), and application cards showing ID/country/visa type/status/progress exactly as specified.

- **`dashboard/index.php`** is a small router dispatching to 9 sub-pages under `dashboard/pages/`, sharing one layout (`includes/dashboard-layout.php`) so the sidebar/shell isn't duplicated 9 times.
- **Applications**: list + a detail page with the brief's full 9-stage visual status timeline (Application Created → Documents Submitted → Documents Verified → Payment Completed → Appointment Scheduled → Application Submitted → Under Processing → Decision Received → Completed), each stage showing date/time/comment from `application_status_history` once reached — plus that application's documents and payments.
- **Documents / Appointments / Payments / Messages / Notifications**: real list views scoped to the logged-in user only, honest empty states (nothing fabricated — no customer has any applications yet, since application *creation* is Phase 11, not this one).
- **Profile**: editable name/mobile (email locked, matches "contact support to change" convention), a real change-password flow (verifies the current password, re-hashes on success).
- **Support**: submits real rows into `support_tickets` and lists the user's own tickets.

**A real bug was caught and fixed during screenshot review**: with a user logged in, the header now shows their name + a Logout button alongside the existing nav, and at 1400px this pushed several nav items ("Visa Services," "Visa Process," "Visa Updates") into an ugly two-line wrap. Fixed two ways: the header greeting now shows first name only (a normal UX pattern, and it just needed less room), and nav links got `white-space: nowrap` so this class of bug can't recur as more nav items get added later.

**Verified against the real database with a full temporary-test-data pass**: registered a throwaway account, confirmed every one of the 9 dashboard pages returns 200 with zero PHP warnings and correct empty states, then inserted one throwaway application (with status history, a document, a payment, an appointment, a message, and a notification) to prove the *populated* views — the status timeline, application cards, document/payment badges, unread-notification highlighting, and the "mark all as read" action were all screenshotted working correctly — then deleted every row and reconfirmed zero test data remains (users, applications, messages, notifications all back to 0).

---

## Phase 11 (Application System) — complete

A real, transactional application-creation flow — the missing link between "browse a visa type" and "see it in the dashboard" that Phase 10 correctly left empty.

- **Schema**: `applications` gained `applicant_passport_number`, `applicant_dob`, `applicant_nationality` — the applicant-identity fields the wizard collects. No new fabricated columns (fees/appointments stay consultant-driven, per the brief's "no automatic charging" stance already established in Phase 10).
- **`/apply/` — a 3-step session-backed wizard**, login-gated (`require_login()`), nothing written to the database until the final step:
  1. **`/apply/` (destination + visa type)** — dropdowns sourced from the real `countries`/`visa_types` tables, pre-fillable via `?country=&type=` query params so every "Start Application" CTA site-wide (header, homepage, about, visa detail pages, dashboard overview) can deep-link straight into a specific visa type.
  2. **`/apply/details/`** — passport number (regex-validated), date of birth (validated as a real past date within 120 years), nationality (dropdown from the real 208-country list) — held in `$_SESSION['apply_wizard']`, not the DB, so an abandoned wizard never leaves a stray row.
  3. **`/apply/review/`** — shows the full summary plus the real default document checklist (`documents WHERE is_required_default = 1`) and an honest disclaimer that fees/appointments are consultant-confirmed, not auto-charged. On submit, wraps application creation in a DB transaction: inserts the `applications` row (status `created`, 10% progress) via the existing `generate_application_number()`, its first `application_status_history` entry, and one `application_documents` row per required document — commits atomically or rolls back on any failure.
- **Every "Start Application" CTA now points at `/apply/`** instead of a dead link: site header (desktop + mobile), homepage hero/final-CTA, About page final CTA, visa detail page, and the dashboard overview's "Start New Application" button.
- **`/track-visa/` rebuilt from the Phase 4 stub into a real public lookup** — no login required, but the query requires *both* the application number and the account email to match (a prepared JOIN across `applications`/`users`), and a non-match always shows one generic "we couldn't find a matching application" message regardless of which half was wrong — the same non-enumeration pattern already used by `forgot-password.php`, extended here since this endpoint is public and unauthenticated. Rate-limited per IP via the same DB-backed `rate_limit_check()` from Phase 9. On a match it shows only limited, non-sensitive fields — application number, country, visa type, progress %, last-updated date, and the 9-stage status timeline — explicitly not documents, payments, or full applicant details, which stay behind login.

**Verified end-to-end through the real browser UI, not a direct DB insert** — registered a throwaway account (`phase11-qa@example.com`) via the actual `/register/` form, walked it through `/apply/` → `/apply/details/` → `/apply/review/` with real form input (Canada / Tourist Visa / passport `A1234567` / DOB 15 Jun 1995 / India), submitted, and confirmed:
- the transaction produced application `VIS-2026-000001` with a `created` status-history row and 2 required-document rows (Passport, Photograph);
- it renders correctly on `/dashboard/applications/1/` (status timeline, correct flash-success message) and in the applications list;
- logged out, then confirmed `/track-visa/` correctly finds and displays it (status timeline, progress, country/visa type) when the email matches;
- confirmed `/track-visa/` shows the generic not-found message — not a different error — when the application number is right but the email is wrong, proving the non-enumeration behavior actually works and not just in theory.

All screenshots taken during this pass, then the test user, application, status-history rows, and document rows were deleted and the database reconfirmed at zero rows across `users`/`applications`/`application_status_history`/`application_documents`.

---

## Phase 12 (Document Management) — complete

Real file upload/download/replace/delete for `application_documents` — the fields for this (`original_filename`, `stored_filename`, `mime_type`, `file_size`, `uploaded_at`, `reviewer_comment`/`reviewed_by`/`reviewed_at`) were already designed into the schema back in Phase 2 but unused until now; no migration was needed, only the actual handling logic.

- **`includes/document-management.php`** — the shared logic used everywhere documents appear: `fetch_owned_application_document()` (every single read or write starts here — joins `application_documents` → `applications` and checks `user_id` matches the logged-in user, so a guessed/incremented document ID can never touch another customer's file, not even to see its status), `document_upload_dir()`, a server-verified MIME allowlist (`application/pdf`, `image/jpeg`, `image/png` — checked via `finfo_file()` against the actual file bytes, never the client-supplied `$_FILES[...]['type']`), a 5&nbsp;MB size cap, and `render_document_row()` so the upload/download/replace/remove UI is defined once and shared by both the dashboard's Documents page and the application-detail page rather than duplicated.
- **Storage**: files never live under `public/` — `UPLOADS_PATH` (set in Phase 2) points at a sibling `uploads/` directory outside the web root entirely, so there is no URL that can reach a file directly regardless of server config; `uploads/.htaccess` denies access too, as defense in depth. On disk, files are stored as `uploads/documents/{application_id}/{random-32-hex}.{ext}` — the stored filename is never the original filename or anything derived from user input, so there's nothing to path-traverse with.
- **Three new handlers**, routed via `dashboard/index.php`'s existing `$segments`-based dispatch (`/dashboard/documents/{id}/upload/`, `/download/`, `/delete/`): upload validates, generates the random filename, moves the file, updates the row to `status = 'uploaded'`, and deletes the previous file on disk if this was a replace; download streams the file back with the real `Content-Disposition`/`Content-Type` after the ownership check; delete removes the file from disk and reverts the row to `status = 'required'`. All three require login, verify ownership, and the two POST actions are CSRF-protected. A document can only be uploaded/replaced/removed while its status is `required`, `uploaded`, or `rejected` — once a status reaches `under_review` or `approved` (Phase 13's admin panel will be what sets those), the row becomes read-only with a download link only, so a customer can't silently swap a file out from under a reviewer.
- No auto-approval anywhere: uploading a file sets status to `uploaded` and stops there — it does **not** fabricate a review outcome, since there's no reviewer yet (that's Phase 13).

**Verified end-to-end through the real browser UI** with two throwaway accounts: registered account A, ran the full `/apply/` wizard to get a real application with its two default-required documents (Passport, Photograph), then — using a real 200×200 PDF and a real 1×1 PNG generated for the test, not empty placeholder files — uploaded the PDF to the Passport row (status flipped to `uploaded`), downloaded it back and confirmed the bytes were **byte-for-byte identical** to the original upload, replaced it with the PNG (filename updated correctly), removed it (status correctly reverted to `required`), then re-uploaded and grabbed its download URL. Registered a second throwaway account B and, while logged in as B, requested account A's document download URL directly — got a **404**, confirming the ownership check actually blocks cross-account access rather than just hiding the link in the UI. Afterward, both test users, the application, its status-history and document rows, and the uploaded file on disk were all deleted, and the database and `uploads/` directory were reconfirmed empty.

---

## Phase 13 (Admin Panel) — complete

A real staff-facing admin panel at `/admin/` — gated to `super_admin`/`admin` roles (the customer/consultant/finance/support/document_reviewer roles already existed in the schema from Phase 2 but had nothing built for them yet; `super_admin`/`admin` are the two this phase covers).

- **`includes/admin-layout.php`** + **`admin/index.php`** — the same shared-shell/`$segments`-router pattern as the customer dashboard (Phase 10), sidebar nav: Overview, Applications, Document Review, Users, Support Tickets.
- **Overview**: real stat tiles (total customers, total applications, active applications, documents pending review, open support tickets) — all live `COUNT()` queries, plus the 5 most recent applications across every customer.
- **Applications**: a cross-customer list (filterable by any of the 9 statuses) and a detail page showing the full applicant identity (passport number, DOB, nationality — deliberately *not* shown on the limited public `/track-visa/` page from Phase 11, but appropriate here for staff), a status-update form that writes a real `application_status_history` row and recalculates `progress_percent` from the stage's position in the 9-stage sequence, and a consultant-assignment dropdown sourced from real `consultant`-role accounts (empty today, honestly, since consultant accounts don't exist until Phase 14).
- **Document review**: a cross-application queue of everything sitting in `uploaded`/`under_review`, plus inline approve/reject actions directly on the application-detail page. Reject requires a reason (stored in `reviewer_comment`), both set `reviewed_by`/`reviewed_at`. Reuses Phase 12's storage/validation logic via a new `fetch_application_document()` (the admin-scoped sibling of Phase 12's ownership-checked `fetch_owned_application_document()` — access here is gated by role at the router level instead of per-row ownership, since staff need to see every customer's files).
- **Users**: lists customer accounts with their application count and a suspend/reactivate toggle against the `users.status` field that already existed in the schema — no new column, no fabricated bulk actions.
- **Support tickets**: cross-customer list with status filtering, and inline status-update + staff-assignment forms. No ticket-reply feature was built — the schema has no `ticket_replies` table and no email integration exists (same honesty rule as the Phase 9 forgot-password flow), so admin can triage and resolve tickets but can't fabricate a reply channel that isn't real yet.

**Three real bugs were caught and fixed during this phase**, two of which trace back to earlier phases:
1. `admin/index.php` never loaded `includes/dashboard-layout.php`, so `application_status_stages()` (defined there) was undefined the moment the application-detail page tried to use it — a fatal error caught immediately by the first real page load, fixed by requiring it alongside the new admin layout.
2. **A CSP bug matching the exact one fixed in Phase 7**: the Phase 12 "Remove document" button used an inline `onsubmit="return confirm(...)"` attribute, which our own `Content-Security-Policy: script-src 'self'` header silently blocks (inline event-handler attributes count as inline script, same as `<script>` tags). It never threw a visible error — the confirm dialog just silently never appeared, and the deletion went through unconfirmed. Fixed by adding a small delegated `data-confirm` handler to `site.js` and switching both that Phase 12 form and this phase's new suspend/reactivate button to it instead of inline attributes.
3. Login always redirected everyone to `/dashboard/` regardless of role, so a staff admin logging in landed on the empty customer-facing dashboard instead of `/admin/`. Fixed by looking up the role at login and redirecting `super_admin`/`admin` to `/admin/`; also fixed the equivalent "already logged in" redirect at the top of `login.php` and the header's account-menu link so an admin browsing the public site links to the right place.

**Verified end-to-end through the real browser UI** with a real admin account and a real consultant account (seeded directly in the database — there's no public admin-registration flow, correctly, since anyone signing up through `/register/` gets the `customer` role): a throwaway customer registered, applied, and uploaded both required documents, then filed a support ticket; the admin then approved the Passport document from the application-detail page, rejected the Photograph from the cross-application queue with a reason, updated the application's status (writing real history and recalculating progress), assigned the consultant, suspended the customer's account (confirmed the suspended account is immediately redirected to `/login/` on its next request, since the existing `current_user()` status check from Phase 9 already covers this — no new code needed), reactivated it, and resolved the support ticket. Logged back in as the customer (same session, since suspension doesn't destroy the session — only blocks access while active) and confirmed every one of those changes was visible: Passport showing Approved, Photograph showing Rejected with the admin's reviewer note and still replaceable, the status timeline showing Documents Verified with the admin's comment, and the ticket showing Resolved. All test accounts, applications, documents, tickets, and uploaded files were then deleted and the database/`uploads/` directory reconfirmed empty.

**Still open, by design**: no reply mechanism for support tickets (no schema/email backing yet), payments admin management (no payment-creation flow exists anywhere yet — nothing to manage), and the consultant panel itself (Phase 14).

---

## Phase 14 (Consultant Panel) — complete

A real panel at `/consultant/`, scoped to exactly what the `consultant` role's own description in the schema says: "View assigned applications, review documents, update status" — nothing more. No consultant-reassignment control, no user suspension, no support-ticket access (those stay admin-only, per Phase 13); a consultant here can only ever see and act on applications actually assigned to them, enforced per-query, not just by hiding sidebar links.

- **`includes/consultant-layout.php`** + **`consultant/index.php`** — the same shared-shell/router pattern as Phase 13's admin panel, gated to `require_role(['consultant'])` only (admins don't get a dual-purposed view here; they already have full access via `/admin/`).
- **Overview**: stat tiles scoped with `WHERE assigned_consultant_id = <this consultant>` — assigned applications, active applications, documents pending their review — plus their 5 most recent assigned applications.
- **Applications**: a list and detail page identical in spirit to the admin one from Phase 13 (full applicant identity, status-update form writing real history and recalculating progress) but with every query filtered to `assigned_consultant_id`, and no "Assign Consultant" control — reassignment is an admin action, not a consultant one.
- **Document review**: same approve/reject flow as Phase 13, refactored so both panels share the actual update logic — `includes/document-management.php` gained `mark_document_approved()`/`mark_document_rejected()`, called by both `admin/pages/document-{approve,reject}.php` and the new `consultant/pages/document-{approve,reject}.php` — and a new `fetch_application_document_for_consultant()` that joins through `assigned_consultant_id`, so a consultant literally cannot look up another consultant's document by ID, not just by not being shown a link to it.
- **Login/header now route by role three ways**, not two: a shared `account_home_href()` helper (added to `includes/functions.php` to stop the redirect logic from being copy-pasted a third time) sends `super_admin`/`admin` to `/admin/`, `consultant` to `/consultant/`, everyone else to `/dashboard/` — used at login success, the "already logged in" redirect, and the header's account-menu link/label.

**Verified end-to-end through the real browser UI**, specifically targeting the ownership boundary rather than just the happy path: a throwaway customer created two separate applications with an uploaded Passport document each; one was assigned to a QA consultant account, the other to a *different* consultant account (both seeded directly, like Phase 13's admin account, since there's no public consultant-registration flow). Logged in as the first consultant and confirmed: the overview stats and applications list showed only the one assigned application, never the other; opening it directly worked and both approving its document and updating its status succeeded and were visible immediately on the customer's own dashboard afterward; then — the actual point of this test — requesting the *other* consultant's application by URL returned a real **404** (not a redirect, not an empty page reusing the same template), requesting the other consultant's document download also **404**'d, and a forged direct POST to approve that document (bypassing the UI entirely, sent via `fetch()` from the page) was correctly rejected with **419** for lacking a valid CSRF token — the access boundary holds at the data layer, not just in what the UI happens to link to. All test accounts, applications, documents, and uploaded files were then deleted and the database/`uploads/` directory reconfirmed empty.

---

## Phase 15 (SEO) — complete

Auditing what to put in a sitemap surfaced a real problem before any SEO infrastructure could honestly ship: several primary nav pages — `/visa-type/`, `/faq/`, `/visa-process/`, `/blog/` — were still Phase-4-era scaffold stubs (`render_scaffold_page()`, which literally prints internal debug text like `Handler: pages/faq.php · Scheduled for: Phase 5`) with **no protection against being indexed**. Submitting those to search engines, or even leaving them crawlable, would mean Google indexing dev placeholder text as if it were the real site. That reframed this phase: SEO infrastructure only means something once what it points at is real.

- **`render_scaffold_page()` now always sets `noindex`**, and the raw `Handler:.../Scheduled for:...` debug text only shows when `APP_DEBUG` is on — real visitors in production now see a clean "This page isn't ready yet" instead of internal routing details. This one change immediately protects every remaining stub, present and future, without having to remember to noindex each one by hand.
- **`/visa-type/` rebuilt for real**: a hub listing all 8 real `visa_types` rows (seeded with real descriptions back in Phase 2/5), and `/visa-type/{slug}/` per-type pages showing that type's description, popular destinations (the real `is_popular_destination` flag), and any tagged FAQs — linking into the existing `/visa/{country}/{type}/` pages rather than duplicating them. (The schema's `country_visa_types` table, meant for per-country availability, was never populated — Phase 6 superseded it by treating every visa type as explorable for every country — so this page doesn't claim availability data that doesn't exist; it links to popular destinations and the full country directory instead.)
- **`/faq/` rebuilt for real**: pulls the 5 real general FAQs seeded in Phase 5, nothing invented.
- **`/visa-process/` rebuilt for real**: describes the actual 3-step `/apply/` wizard and the actual 9-stage `application_status_stages()` sequence built in Phases 11–12 — not invented marketing steps, a description of the real system.
- **`/blog/` deliberately NOT rebuilt with content** — there's no `blog_posts` table and no articles have been written anywhere in this project. Inventing "visa policy update" articles would be exactly the kind of fabricated visa/government information the brief forbids. It now shows an honest "No updates published yet" empty state, marked noindex, and is excluded from the sitemap and disallowed in robots.txt.
- **`robots.txt`** (real static file): disallows every private/functional area (`/dashboard/`, `/admin/`, `/consultant/`, `/apply/`, `/login/`, `/register/`, `/forgot-password/`, `/reset-password/`, `/logout/`, `/blog/`), points at the sitemap.
- **`/sitemap.xml`** (real, dynamically generated, not a static file): 228 URLs — 12 static real pages, all 8 visa-type pages, and all 208 real country overview pages. Deliberately **excludes** the ~1,600 individual `/visa/{country}/{type}/` leaf pages even though they're real and reachable — most still show the honest "requirements not yet verified" state, and mass-submitting thin pages to search engines is bad practice; they stay naturally crawlable via each country page's own links instead of being force-submitted. Validated as well-formed XML with a Python parser.
- **JSON-LD structured data**: a sitewide `Organization` block (name, url, logo, description, `foundingDate: "2015-04"`, email, parent organization — confirmed facts only, no invented address/ratings/employee counts/awards), `BreadcrumbList` on every country and visa-type page, and `FAQPage` wherever real FAQs actually render (general FAQ page, visa-type pages, visa detail pages). Skipped entirely on noindexed pages.
- **`og:image`**: no real logo file has ever been uploaded (still blocked on that, per every earlier phase's note), so a branded placeholder card was generated with PHP's GD library in the same visual language as the existing placeholder favicon (navy gradient, gold "V" mark, the real wordmark and tagline) — `public/assets/images/og-image.png`, wired into `og:image`/`twitter:image` meta tags sitewide.

**Verified**: `sitemap.xml` parses as well-formed XML with the expected 228-URL count; `robots.txt` reachable and correct; spot-checked `<meta name="robots">` across a spread of pages — real content pages show `index, follow`, every private/functional/stub page shows `noindex, nofollow` (login, register, dashboard, blog, careers, contact, apply); JSON-LD blocks on a visa detail page parse as valid JSON with the expected `Organization`/`BreadcrumbList`/`FAQPage` types; `og-image.png` returns 200 as `image/png`; zero browser console errors across all rebuilt pages; screenshots taken of `/visa-type/`, `/visa-type/tourist/`, `/faq/`, `/visa-process/`, and the `/blog/` empty state.

**Flagging clearly, not fixing silently**: these pages are still scaffold stubs (now safely noindexed, but still not real) and weren't in this phase's scope — `/contact/`, `/support/` (public contact-support form, distinct from the logged-in dashboard support tickets), `/careers/`, `/partners/`, `/corporate-services/`, `/document-checklists/`, `/payment-link/`, `/track-request/`, and all 8 legal pages (`/privacy/`, `/terms/`, `/travel-terms/`, `/payment-policy/`, `/refund-policy/`, `/cookie-policy/`, `/grievance/`, `/disclaimer/`). A real `/contact/` page in particular is a meaningful gap for a consultancy business — worth a dedicated pass. The real logo file is also still outstanding — every placeholder asset (favicon, og-image) is a stand-in built from the logo's own colors/tagline, not the actual uploaded file.

---

## Phase 16 (Security) — complete

A full audit sweep (SQL injection, XSS, CSRF coverage, IDOR/authorization, state-changing GETs, HTTP header injection, security-header completeness, auth timing side-channels, file/directory exposure, error handling, password policy, open redirects) across every layer built in Phases 2–15, run partly by hand and partly via an independent audit pass covering the newer admin/consultant panels specifically. The good news first: **no SQL injection, no XSS, no CSRF gaps, and no IDOR were found anywhere in the codebase** — the patterns established from Phase 2 onward (prepared statements everywhere, `e()` on every output, `csrf_field()`/`csrf_require()` on every mutating form, and per-resource ownership checks scoped to `user_id`/`assigned_consultant_id`) held up under a dedicated adversarial pass, including across the admin and consultant panels added in Phases 13–14.

Three real, fixable issues were found and fixed:

- **Login timing side-channel (username enumeration)** — `auth/login.php`'s `if (!$user || !verify_password(...))` let PHP's `||` short-circuit: a request for a non-existent email/mobile skipped the (deliberately slow) bcrypt comparison entirely, while a request for a real account with the wrong password paid that cost — both returned the identical generic error message, but the *response time* differed measurably, letting an attacker enumerate registered accounts by timing alone even though the message itself leaked nothing. Fixed by always calling `verify_password()`, against a precomputed dummy hash (`DUMMY_PASSWORD_HASH` in `includes/auth.php`) when no user is found, so both paths cost the same.
- **Logout was a GET-triggered state change** — every other mutation in the app is correctly POST + CSRF-gated, but `<a href="/logout/">` was a plain link, meaning any third-party page could force a visitor's session to end via something as simple as `<img src="https://visagiri.com/logout/">`. Low impact (forcing a logout isn't damaging), but inconsistent with the rule applied everywhere else. Converted every logout link (header, dashboard/admin/consultant sidebars) to a small POST form with a real CSRF token; `auth/logout.php` now rejects non-POST requests. Verified via screenshot that the button renders pixel-identical to the old link (no visual regression) and that clicking it actually ends the session (confirmed `/dashboard/` correctly bounces to `/login/` afterward).
- **Missing HSTS header** — `send_security_headers()` had no `Strict-Transport-Security`, leaving a theoretical SSL-stripping window on top of the HTTPS redirect `public/.htaccess` already forces. Added `max-age=31536000; includeSubDomains`.

One hardening improvement made even though the current runtime already prevents exploitation: the `Content-Disposition` filename in every document-download handler (dashboard/admin/consultant) was built from the uploaded file's original name with only quote-stripping — PHP's `header()` already refuses to emit any value containing a raw CR/LF, so this wasn't actually exploitable as HTTP response splitting today, but relying solely on one runtime's built-in protection isn't real defense in depth. Added `sanitize_uploaded_filename()` (strips control characters at upload time, before the name is ever stored) so the guarantee doesn't depend on PHP's internals alone.

**Reviewed and consciously left as-is, with reasoning recorded rather than silently ignored**:
- `style-src 'unsafe-inline'` stays in the CSP — the codebase uses inline `style="..."` attributes pervasively (37+ files), and stripping it would require a large, high-regression-risk refactor to move every inline style into classes or add nonces. Risk is low relative to `script-src` (already strict, no `unsafe-inline`/`unsafe-eval`) since no injectable vector into any `style="..."` attribute exists (confirmed during the XSS pass) — CSS-only injection has far less exploit potential than JS injection would.
- Password policy stays at an 8-character minimum with no complexity requirement — informational, not a defect; offset somewhat by rate-limited login/registration.
- Local `.env` currently has `APP_DEBUG=true`/`APP_ENV=local`, correct for this dev environment; `.env.example` already defaults to `false`/`production` so a real deploy starts from the safe default.

**Verified**: HSTS header present via curl; a GET to `/logout/` no longer destroys the session (confirmed via cookie-jar test); a real browser login → click the new logout button → confirmed the session actually ended (`/dashboard/` redirected to `/login/` afterward) with zero console errors and no visual difference from the old link-based button; all changed files pass `php -l`.

---

## Floating enquiry widget (WhatsApp / Call / Email) — added between phases, per request

A single expandable floating action button, bottom-right on every public page (`includes/enquiry-widget.php`, included from `includes/footer.php`) — click to reveal three actions: WhatsApp (`wa.me` link with a pre-filled generic message), Call (`tel:`), and Email (`mailto:`). Toggle, outside-click-to-close, and Escape-to-close are handled in `site.js` (not inline `onclick`, consistent with the CSP `script-src 'self'` policy already in place).

**Phone number is real, not invented**: `+91 7065 819 819` appears consistently across 8+ pages of the preserved legacy site (`grievance.html`, `career.html`, `partner.html`, `testimonial.html`, `support.html` ×4, `country-details.html`) alongside the already-reused `info@visagiri.com` address. Confirmed with the client before use rather than assumed silently. The same number is used for both Call and WhatsApp (a reasonable inference for an Indian mobile-format number, not a landline — flagged to the client as an inference, not fabricated as fact).

Naturally scoped to public pages only — it's wired into `footer.php`, which the dashboard/admin/consultant app shells never call (they close via their own `render_*_end()` functions instead), so it doesn't appear inside the authenticated panels. Verified via screenshot at desktop and mobile widths, confirmed all three link targets (`wa.me`/`tel:`/`mailto:`) resolve to the correct number/address, confirmed outside-click closes it, and confirmed it's present on public pages and absent from `/dashboard/` — zero console errors.

---

## Phase 17 (Performance) — complete

- **N+1 query audit**: swept every file with a `foreach` for a query call issued inside the loop body. Found none — every list page (applications, documents, users, support tickets, countries, visa types) already does one fully-joined query before its render loop, a pattern that's held consistently since Phase 6. Nothing to fix here; confirms the architecture, doesn't change it.
- **Missing index found and fixed**: `users.mobile` had no index despite being one half of every login lookup (`WHERE email = :x OR mobile = :x`) — every foreign key was already auto-indexed by InnoDB and every slug/email column was already `UNIQUE`-indexed, but `mobile` fell through since it's neither. Added `idx_users_mobile` to `schema.sql` and applied the same `ALTER TABLE` to the live dev database.
- **Fixed the CSS `@import` chain** that Phase 3 explicitly flagged as "a Phase 17 concern" at the time it was written: `main.css` was one `<link>` that then `@import`s 10 files — the browser has to fetch and parse `main.css` before it even discovers the other 10 URLs exist, so they loaded serially, one round trip at a time, instead of in parallel. `includes/header.php` now emits 10 individual `<link>` tags directly (same cascade order), so the browser's preloader can dispatch all of them the moment it parses `<head>`. `main.css` itself is left on disk unchanged for `docs/design-system-preview.html`, which still wants one bundled file.
- **Static asset caching + compression**: `public/.htaccess` gained `mod_expires`/`mod_headers` rules for a 1-year `immutable` `Cache-Control` on CSS/JS/images/fonts, and `mod_deflate` for gzip on text-based responses — all wrapped in `<IfModule>` so a host without those modules just skips the block instead of a 500. A year-long immutable cache is only safe with cache-busting, so `asset_url()` was added (`includes/functions.php`) and wired into every CSS/JS reference sitewide — it appends `?v=<filemtime>`, so editing a file after launch changes its URL and busts the cache automatically instead of leaving returning visitors on a stale copy for a year.
- Found and removed one piece of dead code in passing: an old, never-called `asset_url()` stub already existed in `functions.php` from an earlier phase (returned `/assets/$path`, no caller anywhere) — the name collision surfaced it immediately via `php -l`, confirmed via grep that nothing used it, removed rather than kept alongside the real one.

**Verified**: all 7 spot-checked pages (`/`, `/about/`, `/countries/`, a visa detail page, `/login/`, `/register/`, `/faq/`) return 200 with zero console errors and zero failed requests after the CSS refactor; homepage screenshot is pixel-identical to before; the country-search live filter (Phase 7's vanilla JS) still works; all 10 CSS files confirmed requested in parallel via Playwright's network listener (up from 1 blocking request); cache-busted URLs (`?v=<mtime>`) confirmed present and still resolve to 200; `.htaccess` syntax reviewed carefully (standard, well-documented directives, correctly wrapped in `<IfModule>`) — its caching/compression/HTTPS-redirect rules can't be exercised against PHP's built-in dev server, which doesn't process `.htaccess` at all, the same limitation that's applied to the HTTPS-redirect rule since Phase 4.

---

## Phase 18 (QA) — complete

The final phase: a full system-wide QA pass, not another feature. Three parts.

**1. Full static + route sweep.** `php -l` on all 80 `.php` files in the new platform (everything outside `legacy-site/`) — 80/80 clean. Every route reachable from `public/index.php`'s router — all ~40 static routes, real and 404 cases for both dynamic route families (`/visa/{country}/{type}/`, `/visa-type/{slug}/`), and all four auth-gated app-shell roots (`/dashboard/`, `/admin/`, `/consultant/`, `/apply/`) — confirmed returning the correct status code with **zero 500 errors** anywhere.

**2. A single continuous cross-role browser journey** — not isolated per-phase tests like every prior phase ran, but one script threading a real visitor through the entire system in the order a real usage pattern would actually hit it: browse countries → view a real visa detail page → register → apply (3-step wizard) → upload both required documents → file a support ticket → check the public tracking page → update profile → **switch to a real admin account** → approve one document, reject the other with a reason, update application status, assign a consultant → suspend the customer (confirm they're immediately blocked) → reactivate → resolve the support ticket → **switch to a real consultant account** → confirm they see only their assigned application → update its status further → **switch back to the customer** → confirm every single change from both staff roles is visible and correct → log out for real → confirm the session is actually dead → confirm a wrong password is rejected → confirm the correct password works again. 22 checks, 21 passed on the first correctly-written run; the 1 apparent failure (a ticket-status badge text comparison) was a case-sensitivity bug in the *test script* itself — `.badge` renders `text-transform: uppercase`, so comparing against lowercase `'resolved'` was always going to fail — confirmed via screenshot that the real status was correct and correctly displayed as "RESOLVED"; not a product defect.

Writing this test surfaced one genuine, if minor, trap worth recording: naive Playwright selectors like `button[type=submit]` are no longer safe to use unscoped on any authenticated page, because Phase 16's CSRF fix (converting the logout link into a real `<form method="post">`) means every logged-in page now has a **second** submit button in the header, ahead of the page's own form in DOM order — an unscoped click matched Logout instead of the intended form and silently killed the session mid-test. Not a product bug (real users click a specific labeled button, they don't blindly hit "the first submit button on the page"), but it's exactly the kind of interaction a real user *could* trigger by mistake (e.g., pressing Enter with focus in the wrong place — though native form submission is scoped to the enclosing `<form>`, so this specific failure mode requires an explicit stray click, not a keyboard slip). Recorded here so a future automated-test suite doesn't hit the same trap blind.

**3. Mobile responsive spot-check** at 390px width: homepage, the mobile nav drawer, `/countries/`, and (deliberately, since it was never explicitly designed mobile-first) the admin panel — all render correctly with zero console errors, confirming the shared `dashboard-shell` grid that the admin/consultant panels also use holds up at narrow widths without any Phase 18-specific CSS work being needed.

**Cleanup**: all QA test accounts (customer/admin/consultant), the application and its documents, the support ticket, and — going further than every previous phase's cleanup — the accumulated `rate_limits` rows left behind by repeated login/register/track-visa testing across this entire session were deleted. Reconfirmed against the live database: `users`, `applications`, `application_documents`, `support_tickets`, `rate_limits` all at zero; the real reference data (208 countries, 8 visa types, 11 documents, 5 FAQs, 7 roles) all intact and untouched; `uploads/` empty but for its own `.htaccess`/`.gitkeep`.

---

## Launch Readiness Summary

Eighteen phases, one continuous build on real infrastructure (a real MySQL/MariaDB database, a real PHP 8 runtime, real browser testing throughout — never mocked). What a visitor, customer, consultant, or admin can actually do on this platform today, all real and verified, none of it fabricated:

**Public site**: homepage, a 208-country directory with live search, per-country visa overview pages, per-visa-type detail pages (all 8 real types), a 4-service attestation section, a real FAQ page, a real "how it works" page describing the actual application system, public no-login application tracking (email + application number, non-enumerating), an XML sitemap and robots.txt, JSON-LD structured data, and a floating WhatsApp/Call/Email widget with a real, client-confirmed phone number.

**Customer account**: registration, login (rate-limited, non-enumerating, timing-safe), password reset, a 9-page dashboard, a 3-step application wizard with transactional DB writes, real document upload/download/replace/delete with MIME-sniffed validation, a public tracking page, profile editing, password changes, and a support-ticket system.

**Staff**: a real admin panel (system-wide application/document/user/ticket management) and a real consultant panel (scoped strictly to assigned applications, enforced at the query level, verified to actually block cross-consultant access rather than just hide the UI for it).

**Non-functional**: CSRF everywhere, prepared statements everywhere, session fixation prevention, DB-backed rate limiting, file-upload validation via real content sniffing rather than trusting the client, ownership checks scoping every resource to the right user/consultant/admin, a documented and deliberately-scoped CSP, HSTS, a login-timing side-channel closed, parallel asset loading, cache-busted long-lived caching, and zero fabricated visa requirements, fees, processing times, or business statistics anywhere on the site — every honest-empty-state decision made across all 18 phases is still in place today.

**What's still open, listed here rather than left implicit**:
- **The real logo file** has never been uploaded as an actual file attachment (only shown inline in chat) — every branded asset on the site (favicon, og-image) is a placeholder built from the logo's own colors and tagline, not the real file. This has been flagged at the end of nearly every phase since Phase 6.
- **Several pages remain scaffold stubs**, safely noindexed since Phase 15 but not real: `/contact/` (a real contact form is a meaningful gap for a consultancy business), `/support/`, `/careers/`, `/partners/`, `/corporate-services/`, `/document-checklists/`, `/payment-link/`, `/track-request/`, and all 8 legal pages (`/privacy/`, `/terms/`, `/travel-terms/`, `/payment-policy/`, `/refund-policy/`, `/cookie-policy/`, `/grievance/`, `/disclaimer/`).
- **`/blog/` has zero published content** and no `blog_posts` table — deliberately left empty rather than fabricated, per the same no-invention rule that governs visa requirements.
- **No verified visa-requirement data exists for any country/visa-type combination** — every `/visa/{country}/{type}/` page correctly and honestly shows "requirements not yet verified" because no real, sourced data has ever been provided to populate `visa_requirements`. This is the single biggest content gap for the site's actual usefulness to a visitor checking real requirements.
- **No payments, appointments, or messaging functionality was built** — the schema supports all three (real tables, already used defensively in dashboard empty-states), but no creation flow exists for any of them; consultants/admins can't yet request a payment, schedule an appointment, or message a customer.
- **No email delivery integration exists anywhere** — password reset only works in `APP_DEBUG` mode (prints the link instead of sending it), and support tickets/enquiries have no automated email notification.
- **The GitHub push permission gap has never resolved** — every single phase's work exists only as local commits on `claude/visagiri-modernization-audit-oup5t5`; `git push` and a direct GitHub-API push both consistently fail with `403 Resource not accessible by integration`, and the repository has never had any branch land on GitHub across the whole project. This needs an admin to grant the GitHub App Contents-write access on `coolrenukadevi/visagiri` before any of this work can leave the local environment.
- **`.env` in this dev environment has `APP_DEBUG=true`/`APP_ENV=local`** — correct for development; `.env.example` already defaults to the safe production values, but a real deploy needs its own `.env` created from that template, not copied from this one.

Every one of these gaps is a deliberate, documented decision to not fabricate content or overstate what's built — not an oversight discovered late. The platform as it stands is a real, working, security-reviewed, performance-reviewed, end-to-end-tested visa management system; what's listed above is what's left to make it a complete, launch-ready business site.

---

## Sitewide justified text alignment — added between phases, per request

`public/assets/css/base.css`: `p`, `li`, and `.accordion-body` (FAQ answers, which use a `<div>` rather than `<p>`) now render `text-align: justify` with `hyphens: auto` — the latter to avoid the large, ugly word-gaps justified text produces on narrower columns without hyphenation. Deliberately **not** applied to the whole `<body>` — headings, buttons, badges, and nav links are short/single-line text where justify has no visible effect anyway, but a two-word wrapped heading stretched full-width to justify looks visibly broken, so `h1`–`h6` and component text (buttons/badges/nav) are left on their default alignment.

Verified via real browser rendering, not just reading the CSS: computed `text-align` confirmed as `justify` on a real `<p>` (`/about/`) and a real `.accordion-body` (`/faq/`, after opening the accordion), confirmed `h1` stays at its default (`start`), and confirmed visually via full-page screenshots of `/about/` and `/faq/` — every paragraph now has clean, even left and right edges, headings and the footer's nav columns are visually unaffected, zero console errors.

---

## Real favicon set — added between phases, per request

Replaced the inline-SVG-data-URI placeholder (a single icon, no size variants, in place since Phase 3/4) with a proper favicon set: `favicon.svg` (scalable, modern browsers), `favicon-16x16.png`/`favicon-32x32.png` (legacy/small-size fallback), `apple-touch-icon.png` (180×180, iOS home-screen), and a real multi-resolution `favicon.ico` at the site root (16/32/48px, each frame genuine PNG-compressed image data — confirmed via `file`, not just a renamed PNG).

No ImageMagick/`rsvg-convert` is available in this environment, so each PNG size was drawn directly with PHP's GD library at 8× supersampling then downsampled for clean anti-aliased edges — rather than generating one image and letting the browser scale it down blurrily — and the `.ico` was hand-assembled (ICO's modern PNG-frame container format, embedding those same PNGs, which is what every current browser/OS expects). Same brand colors used everywhere else on the site: navy `#123F91` background, gold `#F4B400` "V" — the same scheme the placeholder favicon already used, since the real logo file has still never been uploaded as an actual file attachment (unresolved since Phase 6).

All five files wired into `includes/header.php` via the standard multi-`<link>` pattern (SVG first for browsers that support it, ICO/PNG fallbacks with explicit `sizes`, separate `apple-touch-icon` link) and routed through the same `asset_url()` cache-buster as every other static asset, so a future favicon update won't get stuck behind a stale year-long browser cache.

**Verified**: all five files return 200 with correct `Content-Type` (`image/svg+xml`, `image/vnd.microsoft.icon`, `image/png` ×3); `file` confirms `favicon.ico` is a genuine 3-icon `MS Windows icon resource` with real embedded PNG data at each size, not a mislabeled single image; zero console errors or failed requests loading the homepage.

---

## Sports Visa category added — per request

A 9th real `visa_types` row: **Sports Visa** — "For athletes, coaches, and officials travelling abroad to compete in or officiate a sporting event." A genuinely recognized visa category (several countries, including India, issue dedicated sports visas), described generically like the other 8 — no invented fees, processing times, or country-specific claims.

Adding it surfaced a real, pre-existing bug: the header dropdown and footer's "Visa Services" column were both **hardcoded 6-item arrays** that had already silently fallen out of sync with the database — missing Medical Visa and Conference Visa, added in earlier phases, and about to miss Sports Visa the same way. Both are now built dynamically from `SELECT ... FROM visa_types WHERE is_active = 1 ORDER BY sort_order` instead (the human-readable `/sitemap/` page's Visa Services section got the same fix), so a future visa type only needs adding to the database — nav, footer, sitemap, the `/visa-type/` hub, the homepage's Visa Services section, and the `/apply/` wizard's dropdown all already pulled from the DB and needed no changes at all.

**Creative design**: every visa type now gets a distinctive icon (`visa_type_icon()` in `includes/functions.php`) instead of the uniform document-emoji every card used before — 🧳 tourist, 💼 business, 🎓 student, 🏢 work, 👨‍👩‍👧‍👦 family, ✈️ transit, 🏥 medical, 🎤 conference, 🏅 sports — applied on the `/visa-type/` hub cards, the homepage's Visa Services section, and the visa-type detail page header (reusing the same `.destination-card__flag` treatment the country pages already use, for visual consistency). The Sports Visa detail page's title/description were also strengthened for SEO (`"Sports Visa - Requirements, Eligibility & How to Apply | Visagiri"`), which became the template for a broader title/description improvement across the visa-type pages.

**Verified**: all 9 visa types confirmed present and correctly linked in the header dropdown, footer column, and hub grid via Playwright; the Sports Visa detail page renders with its icon, real popular-destination links, and the honest general-FAQ fallback (no sports-specific FAQs tagged yet); zero console errors; all changed files pass `php -l`.

---

## SEO title/description audit across all pages — per request

Dispatched a background audit of every page's `<title>` and meta description against standard best-practice length guidelines (title ~30–60 characters, description ~120–160 — outside that range, Google tends to either pad an unhelpfully generic snippet or truncate mid-sentence). Findings, then fixes:

- **A systemic pattern, not isolated typos**: nearly every real content page's meta description was well under the 120-char floor (60–110 chars typical) — accurate, just too terse to use the SERP snippet space Google allots. Rewritten with real, honest specifics (not padding) on: `/` (homepage), `/about/`, `/faq/`, `/visa-process/`, `/track-visa/`, `/sitemap/`, `/countries/`, `/attestation/` (hub and all 4 real services, which gained a dedicated `meta_description` field separate from their on-page description so the visible UI copy could stay concise while the meta tag hits the target length), and the `/visa-type/` hub.
- **Two titles were too short/generic** (`/about/`: "About Visagiri"; `/countries/`: "Countries - Visagiri"; `/sitemap/`: "Sitemap - Visagiri") — rewritten with real specifics (the confirmed April 2015 founding date, the real 208-country count).
- **My own Sports Visa edit from earlier today was itself flagged** — the visa-type hub title (83 chars) and per-type template (66+ chars, worse for longer names like "Family Visit Visa") were both over budget, caught by the same audit that reviewed everything else. Shortened both templates; verified every one of the 9 real visa types now produces a title in the 44–60 range.
- **The two highest-traffic-shaped templates** — `visa/index.php`'s country and country+visa-type pages, which generate up to 1,872 URLs (208 countries × up to 9 types, plus 208 country overviews) — had the same too-short description problem *for every single generated page*, not a handful. Rewrote both templates with the same honest, non-fabricated framing already established in Phase 6 ("eligibility, required documents, fees, and processing time" describes what the page covers, not a claim that verified figures exist yet) — now in the target range for the overwhelming majority of the 208 real countries; a few extreme edge cases (the longest country name, "Democratic Republic of the Congo," paired with the longest visa type, "Family Visit Visa") run slightly over 160, which is an acceptable, expected trade-off for a formula applied at this scale rather than 1,872 hand-written descriptions.
- **A real bug, not a copy issue**: `render_not_found()` (`includes/functions.php`) — the handler used for genuine dynamic 404s, e.g. an invalid country or visa-type slug — never set `$noindex`, so real 404 pages were serving `index, follow` while the static `pages/404.php` stub correctly noindexed via `render_scaffold_page()`. Fixed to set `$noindex = true` explicitly, consistent with every other non-content page on the site.

**Verified**: re-fetched every changed static page plus a spread of dynamic pages (UAE and UK, country and country+type) and parsed the actual rendered `<title>`/meta description out of the HTML — not just re-reading the PHP source — confirming real character counts land in range; confirmed a live 404 (`/visa/nonexistent-country-xyz/` and `/visa-type/nonexistent-xyz/`) now serves `noindex, nofollow`; 10-page Playwright sweep of the changed pages returns all 200s with zero console errors.

---

## Attestation Services mega-menu — per request

Replaced the single flat "Attestation" nav link with a real "Attestation Services ▾" mega-menu, expanded from 4 services to 13, organized into the 4 categories the client specified directly (Apostille, Attestation, Documents, Legalization — a refinement of the initially-requested 3-column layout, applied as given rather than the generic placeholder structure): **Apostille** (MEA Apostille, E-Apostille), **Attestation** (MEA Attestation, Embassy Attestation, Consulate Attestation), **Documents** (Educational, Commercial, Personal, Birth Certificate, Marriage Certificate), **Legalization** (Document Legalization, Chamber of Commerce, HRD/State Attestation). All 13 are real, industry-standard document-attestation categories — not fabricated company-specific claims — same discipline already applied to Sports Visa.

**Single source of truth**: `attestation_services()`/`attestation_categories()` in `includes/functions.php` back the header mega-menu, the mobile accordion, `attestation/index.php`'s hub + detail routing, `includes/footer.php`'s Attestation column, and both sitemaps (`/sitemap/`, `/sitemap.xml`) — deliberately built this way to avoid a fourth instance of the stale-hardcoded-nav-array bug already fixed three times this session for visa types. Two legacy slugs (`/attestation/apostille/`, `/attestation/commercial-attestation/`) 301-redirect to their renamed equivalents (`redirect()` gained an optional `$status` parameter, defaulting to 302, so this didn't touch any of the ~50 existing single-arg call sites).

**Desktop**: a 4-column panel (icons + SEO-friendly labels per column, e.g. "MEA Apostille Services," "Commercial Document Attestation" — full names on the pages themselves, short labels in the nav to avoid keyword-stuffing) opens on hover/focus-within (the same CSS-only pattern the existing Visa Services dropdown already uses), positioned centered under the whole header rather than the trigger `<li>` (`.has-mega-menu { position: static }` escapes `.has-dropdown`'s own `position: relative` so the panel's containing block becomes `.site-header`), so it's never clipped regardless of where "Attestation Services" sits horizontally. A closing CTA band — "Need Document Attestation?" / "Get professional assistance..." / "Get Attestation Quote →" — links to the site's one real, working enquiry channel (the WhatsApp number already wired into the floating enquiry widget) with a pre-filled message, rather than the still-unbuilt `/contact/` scaffold stub; `whatsapp_enquiry_href()` is now the shared helper both the widget and this CTA call, so the phone number lives in exactly one place.

**Mobile**: a native `<details>/<summary>` accordion (matching the FAQ accordion's existing `+`→`×` rotation pattern) instead of the desktop mega-menu, with the 4 categories shown as labeled sub-groups underneath — no horizontal overflow (confirmed via `scrollWidth`/`clientWidth`), touch targets sized to ~44–45px.

**A real variable-scope bug found and fixed during verification**: the mega-menu's category/service `foreach` loops in `includes/header.php` originally reused the names `$categoryName`/`$categoryServices`/`$service`. Because `includes/header.php` is `require`d directly into each page script rather than called as an isolated function, it shares that script's variable scope — the same mechanism that lets `footer.php` read `header.php`'s `$visaServiceLinks`. `attestation/index.php`'s detail-page branch sets `$service` to the requested slug's data *before* requiring `header.php`, then reads it *after* — so header.php's own loop was silently overwriting it with whichever service it last iterated (always the same one, "HRD / State Attestation Services," the last item in the last category), making every single attestation detail page render HRD's content under its own correct URL/title. Caught by comparing a Playwright screenshot's visible H1 against the URL it navigated to, not by any automated check. Fixed by renaming the mega-menu's loop variables to `$megaCategoryName`/`$megaCategoryServices`/`$megaService` in both the desktop and mobile blocks — distinct enough that no future page's own variables can collide with them.

Also caught and fixed in the same pass: Escape didn't actually close the mega-menu when it had been opened via hover or keyboard focus, because `:focus-within`/`:hover` kept the CSS-only panel visible even after the JS-driven `is-open` class was removed (Escape doesn't blur the trigger or move the mouse). Added a `.mega-menu.is-force-closed` override class (JS-applied on Escape, cleared on the next genuine `mouseenter`/`mouseleave`/`focusout` of the item) so Escape now reliably closes the panel regardless of how it was opened, without breaking the plain hover/focus-within behavior for everyone else.

**Verified**: Playwright across desktop (1440px — hover open/close, keyboard Tab-to-open, Escape-to-close, outside-click via the existing Visa Services dropdown confirmed unaffected, mega-menu link click navigates correctly), tablet (834px — correctly falls back to the burger/mobile nav below the 1024px breakpoint), and mobile (390px — accordion open/close, zero horizontal overflow, 13 links across 4 sub-groups, ~44–45px touch targets); all 13 detail pages independently confirmed to render their own correct H1 (not just relying on the title tag, after the scope-collision bug above); both legacy-slug redirects return real 301s; `/sitemap/` and `/sitemap.xml` both list all 13 real service URLs; SEO title/meta-description lengths checked for all 13 detail pages plus the hub (all in the established 30–65/100–165 character ranges); zero console/page errors across every page tested; all changed files pass `php -l`.

---

**Follow-up, same day**: the 13 detail pages initially shipped thin — a one-sentence description plus a "details being finalized" notice, which read as close to blank on direct visit. Extended `attestation_services()` with four new fields per service (`overview`, `when_needed`, `process_steps`, `typical_documents`) and the detail template to render them as four real content sections, plus a cross-link block to the other services in the same category. Content is real, generic, publicly known information about how India's document-attestation ecosystem actually works (the notarization → state/HRD → MEA → embassy chain, which document categories need which stages, what the Hague Apostille Convention does and doesn't cover) — not fabricated Visagiri-specific claims; the one thing that genuinely can't be published without real sourcing (fees, exact processing time) stays an honest "confirmed directly with our team" note, now paired with a link to the real WhatsApp channel instead of standing alone as the page's only content. All 13 pages re-verified to render all four sections; `php -l` clean.

---

## Premium Company mega-menu — per request

Redesigned only the Company nav dropdown and the footer's Company column (explicitly not the rest of the site) into a premium, corporate-technology-styled mega-menu — a deliberate visual departure from the Attestation mega-menu's emoji/travel-brand tone, since this one was asked to read as SaaS/fintech rather than travel-agency: small stroke-SVG line icons instead of emoji, a navy featured card with an original abstract globe/passport illustration (same "inline SVG, not stock photography" approach as the homepage hero), hover-revealed arrows, and item descriptions rather than a bare link list.

**3 columns** — Discover (About Us, Why Us, Our Story), People (Leadership, Our Team, Careers), Trust & Connect (Partners, Affiliations & Accreditations, Contact Us) — plus a 4th "featured card" slot ("Visa Management, Reimagined." / "Explore Our Approach →" linking to the existing Why-Us section). Mobile gets a flat accordion (no sub-headings, matching the exact structure requested), not the desktop layout.

**Content honesty, per the request's own explicit rule** ("do not create fake pages, fake partnerships, fake accreditations... mark missing pages as PAGE TO BE CREATED"):
- **About Us, Why Us** — reused existing real content (`/about/`, `/about/#why-visagiri`) rather than duplicating pages.
- **Our Story** — new section added to `/about/`, built only from already-confirmed facts (April 2015 founding, Tripgation Pvt Ltd, the technology-plus-consultant approach) — no invented milestones, numbers, or narrative beats.
- **Leadership, Our Team, Affiliations & Accreditations** — new routes (`/leadership/`, `/our-team/`, `/affiliations/`), but there is no real data anywhere in this project for who actually leads/staffs Visagiri or what bodies it's accredited by, and inventing names, bios, or credentials would be a much worse fabrication than a placeholder. These stay honest `render_scaffold_page()` stubs (noindexed, same "content pending" pattern used site-wide since Phase 4) rather than fake bios — this is the direct implementation of "PAGE TO BE CREATED."
- **Corporate Services** — dropped from the Company menu (not part of the requested 9-item structure) but the route/file is left untouched and still reachable, per "do not break existing URLs unnecessarily."
- **Footer brand statement, bottom-bar social icons, and "Back to Top"** — NOT implemented. The request's own opening instruction was explicit: "Redesign ONLY the Company navigation dropdown and Company section of the footer... DO NOT redesign the entire website," but its later footer-brand-statement and bottom-bar items would touch the *whole* footer, and there are no real, sourced social media URLs anywhere in this project to link. Held back rather than guessed at or fabricated; flagging for a follow-up decision.

**A real bug found and fixed during verification, twice**: CSS Grid doesn't let a track shrink below its content's intrinsic (min-content) width by default — the long label "Affiliations & Accreditations" forced its column to ~416px, which cascaded into the whole grid rendering ~1312px wide inside a 940px panel and shoving the featured card almost entirely off-screen. Fixed with `min-width: 0` on the grid/flex items. That fix then exposed a second, previously-invisible bug: the description text still didn't wrap — it inherited `white-space: nowrap` from `.site-header__nav > ul` (set for the flat top-level nav links) all the way down into the mega-menu's description spans, so instead of wrapping within its ~185px column it rendered on one line and bled horizontally into the next column. Fixed by resetting `white-space: normal` explicitly on the mega-menu's label/description/title/copy text. Both caught via direct `getComputedStyle`/`getBoundingClientRect()` inspection after a screenshot showed the featured card clipped at the viewport edge — not visible from reading the CSS alone.

**Verified**: Playwright at 1440px (hover open, item-hover arrow reveal, Escape-close, click-navigates, and the pre-existing Attestation mega-menu re-confirmed still working after the JS was refactored into one shared `initMegaMenu()` function instead of copy-pasted per menu), 834px tablet (correct accordion fallback, zero overflow), and 390px mobile (flat 9-item accordion, zero horizontal overflow); grid column widths measured directly post-fix to confirm the overflow is gone (was 1312px effective content in a 940px panel, now 890px matching exactly); all 15 touched/new routes return 200 (including the 3 new `/leadership/`, `/our-team/`, `/affiliations/` stubs, correctly `noindex, nofollow`); `/sitemap/`'s Company section and the footer's Company column both reuse the same `$companyMenu` array the header defines (one more application of the shared-source-of-truth pattern, not a fourth stale-copy risk); zero console errors; all changed files pass `php -l`.

---

## Countries mega-menu — global visa destination discovery, per request

Replaced the flat "Countries" nav link with a large mega-menu covering all 208 real countries, grouped by the existing 6-region taxonomy, plus a live search, a Schengen highlight, featured destination cards, and a "Find Visa By Purpose" shortcut — all reading from one new `country_mega_menu_data()` query in `includes/functions.php` (region-grouped, popular-flagged, Schengen-flagged) rather than a hardcoded list, so it can never drift the way three earlier nav arrays did before being fixed this session.

**New real, verifiable fact added to the schema**: `countries.is_schengen` — Schengen Area membership (27 states) is a public, static geopolitical fact, seeded by real slug match, same treatment as the ISO codes already in the table — not a business claim requiring client sign-off.

**Three honesty/accuracy decisions made against the request's own wording, flagged rather than silently "improved"**:
- **"🔥 Most Searched"** → relabeled **"Popular Destinations."** This is a from-scratch build with no production traffic yet, so there is no real search-analytics data to back a "most searched" claim — using that label would be inventing a usage statistic, the same category of claim ("15+ years," "98% approval rate") already stripped out of the legacy site early in this project for being unverifiable.
- **"29 European countries"** (the request's own example figure for Schengen) → the real, current count is **27** (verified against the actual 27 member states seeded above) — used the correct number rather than repeating the request's inaccurate one.
- **"Find Visa By Purpose"** lists the site's actual **9 real visa types** (Tourist, Business, Student, Work, Family Visit, Transit, Medical, Conference, Sports) rather than the request's 10-item example list, which included "Dependent Visa" and "Official/Diplomatic" — categories that don't exist anywhere in this system's `visa_types` table and would need to be fabricated to display.
- **"🤖 AI Visa Assistant"** → built as a real, honestly-labeled "Not sure which visa you need?" teaser instead, linking to the homepage's existing "Find the Right Visa" guided-search widget (`/#visa-search` — destination → visa type → nationality → travel date, already real and working since Phase 6). No actual AI/LLM integration exists anywhere in this codebase; calling a plain search-widget shortcut "AI-driven" would be a fabricated capability claim.
- **Per-country visa-type subsets** (the request's "USA Visa → Tourist | Business | Student | Work" implying an exclusive set) were not implemented as exclusive claims — this system has never modeled which specific visa types a given country restricts to (the `country_visa_types` join table exists in the schema but was deliberately left unpopulated back in Phase 6, superseded by treating every visa type as explorable for every country). The 4 featured cards use non-exclusive phrasing ("Tourist, business, student & more") instead.

**Real bug found and fixed twice during verification, both invisible from reading the CSS alone**:
1. **Duplicate listing**: the "Schengen Area" aggregate row (a real `countries` table entry, `region = Europe`, but not itself one of the 27 individual member states) was appearing twice — once correctly in the dedicated Schengen highlight box, and again as a plain link in the "Other Europe" list, since it technically passed the `is_schengen = 0` filter. Fixed by explicitly excluding the `schengen` slug from that list.
2. **Excessive height** (`Do NOT make the dropdown excessively tall` was explicit in the request): the panel first measured **1201px tall** against a 900px viewport. Traced with `getBoundingClientRect()`/a cloned-node intrinsic-height check (grid `align-items: stretch` was making every column report the same inflated height, hiding which one was actually oversized) to the "Popular Destinations" column: 12 chips with long names ("United Arab Emirates," "United Kingdom") crammed into a ~211px-wide column couldn't fit more than about one per row, so 12 chips rendered as a near-vertical list (~352px alone). Fixed by moving the chip row out of the column grid into its own full-width row above the columns — which the request itself had actually suggested ("above... the continent sections") — cutting the measured height to 1059px, plus trimming per-region item caps and converting "Find Visa By Purpose" from a 9-row list to a 2-column grid along the way.

**Verified**: Playwright at 1440×900 (search filter tested with a real match — "japan" correctly narrows to exactly the 2 elements containing "Japan" via direct DOM inspection, not Playwright's less reliable `:visible` heuristic, which had reported a false count — and a nonsense query correctly shows the empty state), grid overflow re-measured directly post-fix (1090px of columns inside a 1140px panel, no clipping), Escape/outside-click/hover all still work and the pre-existing Attestation and Company mega-menus confirmed unaffected; 834px tablet falls back to the mobile nav correctly; 390px mobile accordion (3 top-level sections now: Attestation, Countries, Company) renders all 6 regions plus the Schengen highlight, Find Visa By Purpose, and the directory link, with zero horizontal overflow; a 13-route sweep (`/`, `/countries/`, 4 real `/visa/{country}/` pages including `/visa/schengen/`, plus every previously-existing route) returns all 200s; zero console errors; all changed files pass `php -l`.

---

## /leadership/ goes real — first scaffold stub converted to genuine content

The client provided two real facts directly in chat: Anisha Bharti (Director) and Renuka Devi (Director), each with a real LinkedIn profile URL. That's enough to retire `/leadership/` from the noindexed "content pending" stub it's been since it was created earlier this session — no bios or backstory invented beyond what was actually confirmed (name, title, profile link).

**Photos**: both were shared as images inline in this chat, but — same limitation already flagged for the real logo file back in Phase 6 — inline chat images aren't accessible on disk as real file attachments; there was nothing to copy into `public/assets/images/`. Rather than block on that or fabricate a stand-in photo, the page uses a simple initials avatar (navy circle, gold ring, matching the existing token palette) for each director until real photo files are uploaded as actual attachments.

`pages/leadership.php` rewritten from a `render_scaffold_page()` stub to a real page (`robots: index, follow`, real title/description), reusing the existing `.card`/`.card-grid` components plus a new small `.leader-card` set. Added to `sitemap.xml`; the human `/sitemap/` and the header's Company mega-menu already picked it up automatically since both already read from `$companyMenu`.

**A real CSS bug caught and fixed on the spot**: the leader's title text ("Director") rendered left-aligned instead of centered under the name, because the sitewide `p { text-align: justify }` rule (added earlier this session for body copy) overrides inherited `text-align: center` on any `<p>` specifically — the exact same class of bug already hit twice this session in the mega-menus' cards. Fixed with an explicit `text-align: center` on `.leader-card__title`.

**Verified**: `/leadership/` returns 200 with `index, follow`; both names/titles/LinkedIn URLs render correctly; Playwright screenshots at desktop and mobile confirm centered layout, on-brand avatar styling, zero console errors; `php -l` clean.

---

## Leadership + Team pages redesigned — per request

Upgraded `/leadership/` with an intro paragraph above the leader cards, a hover-lift on the cards, and a proper LinkedIn button (icon + label) replacing the plain text link. No new facts invented — same two real directors, same confirmed LinkedIn URLs as before.

**`/our-team/`** converted from a noindexed stub to a real, indexed page — but still no individual team-member names beyond the two directors already on Leadership, so rather than inventing people it describes the three real functional roles behind Visagiri's actual services (Visa Consultants, Document & Attestation Specialists, Application Support), reusing the existing `.feature-card` component already established for "Why Visagiri" on `/about/` — same discipline, generic-but-real category descriptions, not fabricated headcounts or bios. Closes with an honest note that individual profiles are coming, plus links to Leadership and Contact.

A real double-escaping bug was caught before shipping: `&amp;` HTML-entity literals inside PHP string values (e.g. `'Document &amp; Attestation Specialists'`) would have been re-escaped by `e()`/`htmlspecialchars()` into `&amp;amp;` on output. Fixed by using a literal `&` in the PHP source and letting `e()` do the escaping — verified by checking the rendered HTML for `&amp;amp;` directly, not just eyeballing the source.

**Verified**: both pages return 200 with `index, follow` (were `noindex, nofollow`); `/leadership/` title lengthened from 21 to 36 chars (was under the 30-char floor) and description re-tuned to 136 chars; `/our-team/` title/description land at 55/143 chars, both in range; both added to `sitemap.xml`; Playwright screenshots at desktop and mobile show correct layout, hover states, and zero horizontal overflow; zero console errors; `php -l` clean on both files.

---

## Optional single-folder cPanel package — for hosts that can't repoint the document root

The standard deployment layout (this app's architecture since Phase 2) keeps `public/` as the only web-servable folder, with `includes/`, `database/`, `.env`, and `uploads/` as siblings one level up — genuinely unreachable by any URL, not just blocked by a rule. That requires the hosting account's document root to be pointed at `public/`. Many basic shared-hosting cPanel accounts can't do that; the document root is fixed at `public_html/`.

Rather than silently work around that, the tradeoff was put to the client directly: keep the stronger two-folder protection and accept a slightly fiddlier two-location upload, or flatten everything into one folder for a plain drag-and-drop `public_html/` upload, at the cost of `includes/`, `database/`, `.env`, and `uploads/` moving from "physically outside the web root" to "inside the web root, blocked only by `.htaccess`." The client chose to flatten.

**`bin/package-cpanel.sh`** (new) builds this alternate layout into a separate zip without changing how the app is developed day to day — it only reads the working tree and writes to a temp build directory, then patches the one line in `index.php` that actually differs between layouts (`$root = dirname(__DIR__)` → `$root = __DIR__`).

**Two real bugs found and fixed while building this**, not just theoretical: `index.php`'s five `require_once` lines were originally independent of `$root` (computed *after* them), so the flattened package 500'd on every route except static assets — fixed by computing `$root` first and routing every `require_once` through it, so the packaging script's single sed patch is enough to retarget the whole front controller. And `public/.htaccess`'s static-file bypass previously allowed any existing file *or directory* through without going via `index.php`; harmless in the two-folder layout where nothing but `public/`'s own contents live there, but in the flattened layout that would let a request resolve straight to a real PHP file/directory (`attestation/index.php`, `dashboard/`, etc.) and get executed by Apache directly, skipping session setup, security headers, and routing entirely. Fixed by excluding `.php` files and directories from the bypass, and adding an explicit `[F,L]` deny rule for `includes/` and `database/`.

**Verified**: ran the packaging script end-to-end, extracted the resulting zip standalone (no `public/` folder, everything flattened), pointed a fresh PHP dev server at it against the real local database, and confirmed `/`, `/attestation/`, `/countries/`, `/visa/usa/`, `/leadership/`, `/our-team/`, and a static asset all return 200 with zero errors in the server log; confirmed `asset_url()`-generated cache-busting versions match real file mtimes in the flattened layout, not a `time()` fallback; `php -l` clean across every file in the package. **Not verified**: the `.htaccess` deny/bypass rules themselves, since this sandbox has no Apache installed to test against — the client should confirm after deploying that `https://yourdomain.com/includes/config.php` and `https://yourdomain.com/database/schema.sql` do not return raw file contents.

---

## Single-folder, no-database rebuild — MySQL removed, enquiries move to Google Sheets + Drive

Explicit client request: make the site installable without any MySQL setup step at all, and route enquiries to Google Sheets/Drive so they have a record without needing to administer a database. Given via `AskUserQuestion`, the client chose the largest of three offered options: **strip the site down to its static marketing pages plus enquiry forms**, dropping the entire accounts/dashboard/admin/consultant/application-tracking system (Phases 9–14) rather than keeping it on a drop-in SQLite replacement. This is a deliberate, informed trade — not a silent feature cut.

**What was removed, and why each was safe to remove:**
- `auth/`, `dashboard/`, `admin/`, `consultant/`, `apply/` (42 PHP files) — the entire login/register/customer-portal/admin-panel/consultant-panel/online-application-wizard system built in Phases 9–14. All of it existed only to manage `applications`/`documents`/`users` rows in MySQL; with no database, none of it can function, and the client explicitly chose not to keep an account system at all.
- `pages/track.php`, `track-request.php`, `payment-link.php` — public pages that only made sense on top of the now-removed `applications`/`support_tickets` tables.
- `pages/support.php` — was still an unbuilt `render_scaffold_page()` stub (never shipped content); `/support/` now 301-redirects straight to `/contact/`, the one real enquiry channel.
- `database/` (schema + migrations) and `includes/database.php` (the PDO connection) — no database, nothing to connect to.
- `uploads/` and `includes/document-management.php` — document upload/download only existed for the dashboard/admin/consultant document-review flow.
- `includes/auth.php` — session-based login/password-hashing/reset-token helpers, all specific to the removed account system. (`start_secure_session()`, which sessions/CSRF/flash messages still depend on, was moved into `includes/security.php` rather than deleted — a plain oversight caught in testing below, see "Real bug found").

**What moved from MySQL to static PHP files, and why this isn't fabrication:** `countries` (208 rows), `visa_types` (9 rows), and `faqs` (5 rows) were exported directly from the live database — the real rows seeded and verified across Phases 5, 7, and 90 — into `data/countries.php`, `data/visa-types.php`, `data/faqs.php` (plain `return [...]` array literals, no query engine needed). `includes/data.php` provides the same lookup shape (`countries_all()`, `country_by_slug()`, `visa_types_all()`, `visa_type_by_slug()`, `faqs_general()`, `country_mega_menu_data()`) that callers previously got from `db()`-backed functions, so every consumer (header mega-menu, `/countries/`, `/visa/`, `/visa-type/`, `/faq/`, sitemap) changed its data *source*, not its logic. The `embassies`/`consulates`/`vac_centers`/`visa_requirements` tables were checked before removal and confirmed still **empty** — no real address or verified-requirement data was ever entered for any country — so `fetch_country_contact_points()` and the visa-detail "requirements not yet verified" honest-empty-state simply became unconditional instead of one branch of a query result; nothing that existed was lost.

**Every CTA and mention of the removed account system was rewritten, not just unlinked** — "Start Application"/Login/Sign Up/Track Application buttons across the homepage, header, footer, `/about/`, `/visa/`, `/visa-type/` now point to a real WhatsApp-prefilled enquiry link (`whatsapp_enquiry_href()`, using the same real, client-confirmed +91 7065 819 819 number already in use for the sitewide contact widget) or `/contact/`. `why_visagiri_features()` and the homepage's "How It Works" steps, and `/visa-process/`'s entire content, described uploading documents and tracking status from a dashboard — rewritten to describe the actual current process (enquire → we confirm requirements → we prepare and submit on your behalf). FAQ #5 ("How can I track my visa application?") pointed to the removed Track Application page — its answer was edited to point to WhatsApp/call/enquiry instead, the same real channels.

**New: real `/contact/` enquiry form**, replacing what had been an unbuilt stub since the project's early phases. Submits to Google Sheets *and* Google Drive via a Google Apps Script Web App bridge (`google-apps-script/Code.gs`, deployed by the client following `docs/google-sheets-setup.md` — this environment has no way to create Google Cloud credentials on the client's behalf, so the script and a plain-English walkthrough are the deliverable, not a live integration). `includes/google-sheets.php`'s `submit_enquiry()` tries the Sheets/Drive bridge first, then **always** also emails `MAIL_ENQUIRY_RECIPIENTS` — so an enquiry is never silently lost even before `GOOGLE_APPS_SCRIPT_URL` is configured, or if Google is briefly unreachable. Protected by the existing CSRF token, a honeypot field, and a new rate limiter.

**Rate limiting moved off MySQL too**: `rate_limit_check()` in `includes/security.php` previously read/wrote the `rate_limits` table; now it's a `flock()`'d JSON file at `storage/rate-limits.json` (new `storage/` directory, blocked from direct web access the same way `includes/` already was). Appropriate for this site's actual traffic volume (a handful of form submissions, not a high-concurrency API) and needs nothing beyond a writable directory — no cron, no separate service.

**Two real bugs found and fixed during verification, both invisible from a code read alone:**
1. **Every single route fatal-errored** (`Call to undefined function start_secure_session()`) immediately after the first pass of deletions — `start_secure_session()` lived in the just-deleted `includes/auth.php`, but sessions are still load-bearing for CSRF tokens, flash messages, and the new rate limiter. Only caught by actually starting the dev server and requesting a page, not by `php -l` (which is purely syntax-level and can't see a missing function defined in a different file) or by reasoning about the diff. Fixed by moving the function into `includes/security.php`, which every route already requires.
2. **A stray leftover `/apply/` CTA** on `/about/`'s final call-to-action, missed by the first pass of header/footer/homepage edits since it lived in a page-specific template rather than a shared include. Caught by a sitewide `grep` sweep for every dropped route/function name after the main edit pass, not assumed clean just because the obvious files were fixed.

**Verified**: full `php -l` sweep clean across every remaining file; PHP dev server started with **no MariaDB running at all** (first time this has been true since Phase 5); a route sweep across every real page (`/`, `/about/`, `/contact/`, `/faq/`, `/countries/`, `/visa-type/`, `/visa-type/tourist/`, `/visa/usa/`, `/visa/usa/tourist/`, `/attestation/`, `/attestation/mea-apostille/`, `/blog/`, `/visa-process/`, `/leadership/`, `/our-team/`, `/careers/`, `/privacy/`, `/terms/`, `/sitemap/`, `/sitemap.xml`) returns 200; every dropped route (`/track-visa/`, `/apply/`, `/login/`, `/register/`, `/dashboard/`, `/admin/`, `/consultant/`, `/track-request/`, `/payment-link/`) correctly 404s instead of erroring; `/support/` 301-redirects to `/contact/`; a real end-to-end contact-form submission was tested — in this sandbox (no `sendmail`, `GOOGLE_APPS_SCRIPT_URL` unset) it correctly showed a genuine failure message rather than a fake success, proving the "never claim success that didn't happen" behavior actually holds under a real failure rather than just in the happy-path code read; the rate limiter's `storage/rate-limits.json` was confirmed to be created and updated correctly. Playwright screenshots at 1440×900 confirm the homepage, `/contact/` form, `/visa/usa/`, the countries mega-menu (still correctly grouping all 208 static countries by region, Schengen highlight intact), and the footer all render correctly with zero console errors, and with no stale references to Login/Start Application/Track Application anywhere in the rendered output.

---

## MySQL restored + full admin CMS — reversal of the no-database rebuild

Explicit client request: a full reversal of the just-shipped no-database rebuild — back to PHP 8.2+ with MySQL/MariaDB via PDO, a proper relational schema, and a complete admin panel so content is manageable without editing code, while still ruling out `.env`/environment-variable database configuration. Given the size and direct contradiction with the just-made "go database-free" decision, both the database-scope question and how to handle content this project has never had real data for (extra visa categories, embassy/consulate addresses, customer reviews) were put to the client explicitly via `AskUserQuestion` rather than assumed. The client chose the full MySQL + admin CMS rebuild, with the existing discipline preserved: real data only, honest empty states everywhere else.

**Database**: `database/schema.sql` defines 14 tables — `admin_users`, `rate_limits`, `regions`, `countries`, `visa_types`, `country_visa_types`, `visa_requirements`, `embassies`, `consulates`, `visa_application_centres`, `visa_faqs`, `enquiries`, `contact_messages`, `site_settings` — with proper foreign keys, unique constraints, and indexes. `database/seed.sql` is a plain-SQL export (no PHP required to run it, so a phpMyAdmin import is enough on a real cPanel host) of the actual real data already verified across this project's history: 208 countries with real ISO/region data, the 9 real visa types (not the 10-item list the client's own spec suggested, which included "Dependent" and "Official/Diplomatic" categories that don't exist anywhere in this system), 5 general FAQs, and real site settings (the same confirmed phone/email/WhatsApp number already used sitewide). `country_visa_types` is fully populated (208 × 9 = 1,872 rows, all marked available) rather than left empty, because that accurately reflects current real policy — no country currently restricts which visa types it offers — while still giving the new admin panel a real table to edit if that policy ever changes, rather than an unstated assumption buried in application code. `embassies`, `consulates`, `visa_application_centres`, and `visa_requirements` ship **empty** — no real address or verified per-country requirement has ever been sourced for this site — so the public site's existing "not yet published" / "not yet verified" honest-empty-state handling (already built during Phase 8) needed no changes, just a real table behind it again instead of a query that could never return data.

**No `.env` anywhere, per explicit instruction**: `config/database.php` (gitignored — see below) and `config/site.php` are plain PHP files returning arrays, both siblings of `includes/` outside the web-servable folder in the two-folder layout, and blocked by `public/.htaccess` in the flattened cPanel package. Runtime-editable settings that used to live in `.env` (contact phone/email/WhatsApp number, mail recipients, the Google Sheets integration URL, company facts) moved to the `site_settings` table instead, editable from `/admin/settings/` without a code deploy — strictly more capable than an `.env` file, not just a rename of one.

**A late decision, not in the original plan**: `config/database.php` was initially committed with this session's local dev password inline. On review, that contradicts this project's own established discipline (`.env` was gitignored for exactly this reason, every session of this project). Fixed before commit: `config/database.php` is now gitignored, and `config/database.example.php` (placeholder values, safe to commit) ships instead — matching the `.env`/`.env.example` pattern this project already used, just with plain PHP files as the client's spec required.

**Admin panel** (`/admin/`, all behind `admin_users` + session auth — `password_hash()`/`password_verify()`, a dummy-hash timing-safety comparison on failed logins, the DB-backed rate limiter, CSRF on every form): dashboard with live counts; full CRUD for countries (search + pagination across all 208), visa types, and FAQs; a combined embassies/consulates/application-centres manager (one page, a type selector, rather than three near-duplicate CRUD pages); a dedicated visa-requirements editor (country + visa-type picker, saves and stamps `last_verified_at`); enquiries and contact-messages inboxes with status workflows; and a settings page editing every `site_settings` row through labeled fields. An initial `admin_users` row ships in `database/seed.sql` with a random-generated password (communicated to the client separately, not committed) and a `CHANGE THIS PASSWORD` note directly in the seed file.

**Enquiry system rebuilt around the database as the system of record, not Google Sheets**: `/enquire/` is a new structured form (name, mobile, email, destination country, visa type, purpose of travel, travel date, traveller count, message — matching the client's spec) that inserts into `enquiries` with a generated `VIS-YYYY-NNNNNN` reference number shown back to the visitor, retrying once on the rare same-instant duplicate (the `COUNT`-based generator has the same known, accepted race-condition profile as the original `generate_application_number()` from Phase 11 — fine at this site's real traffic volume). `/contact/`'s general form now inserts into `contact_messages` the same way. The Google Sheets/Drive bridge and email notification built during the no-database phase weren't thrown away — `includes/google-sheets.php` was refactored from "the record" into `notify_enquiry_channels()`, a best-effort secondary notification fired after the database write succeeds, so a client who already set up the Google side keeps getting it, with no behavior change required on their end.

**Visa detail pages restored to their original two-state design**: `visa/index.php`'s "if a `visa_requirements` row exists, show it; otherwise show the honest not-yet-verified state" branch — deleted during the no-database phase because the table it depended on no longer existed — is back, now driven by `fetch_visa_requirement()` against the real table. `format_money()`, deleted for the same reason, is back too. Every "Enquire Now" WhatsApp-only CTA introduced during the no-database phase now offers both a "Submit Enquiry" link (to the new structured `/enquire/` form, pre-filled with the country/visa type the visitor was viewing) and the existing WhatsApp option, rather than replacing one real channel with another.

**Header/footer**: the footer gained a "Submit a Visa Enquiry" link in the Resources column; `robots.txt` now disallows `/admin/` (a stale, no-longer-relevant set of disallow rules for the removed customer-account routes was cleaned up in the same pass, from before either database transition). Structurally the header/footer are otherwise the same navy/gold chrome already verified working — the specification's header/footer requirements (mega-menus, Search Visa access, Company menu) were already built and confirmed intact in the prior no-database pass; this pass only added the enquiry-system entry point.

**Real bugs found and fixed during verification, none visible from a code read alone:**
1. **Every admin page fatal-errored** with `Uncaught PDOException: SQLSTATE[42000]: ... syntax error ... near '+ (SELECT COUNT(*) FROM consulates)...'` — the dashboard's combined embassy/consulate/VAC count query was missing its outer `SELECT` (`(SELECT COUNT(*) FROM embassies) + (SELECT COUNT(*) FROM consulates) + ...` isn't valid SQL on its own; it needs to be the expression list of a real `SELECT`). Caught immediately by loading the dashboard, not by `php -l` (a runtime SQL error, invisible to a syntax-only linter).
2. **A test-script false alarm that looked exactly like a real session bug**: an automated login-and-submit walkthrough kept landing back on the login page after every form submission, looking like session/CSRF corruption. Root cause, found by comparing an identical flow via raw `curl` (which worked) against the browser automation (which didn't): the admin layout's topbar "Logout" button and the page's own "Save" button are both `<button type="submit">` on the same page, and an under-specified `page.click('button[type=submit]')` selector was clicking Logout — a test-authoring bug, not an application bug, but it took a side-by-side `curl` comparison to prove that rather than assume the simpler "the code is broken" explanation.
3. **A genuine, if minor, robustness bug**: the visa-requirements admin form's fee fields did `$_POST['service_fee'] !== ''`, which throws an `Undefined array key` warning if that key is ever absent from the request body entirely (not reachable through the real HTML form, which always submits every field, but reachable by any other real or malicious POST source). Fixed to `($_POST['service_fee'] ?? '') !== ''`, matching how every other field on that form is already read.
4. **The rate limiter correctly blocking repeated test logins** was briefly mistaken for a fourth bug (a wall of login failures during rapid re-testing) before checking the `rate_limits` table directly and confirming it was the 5-attempts/15-minute admin-login throttle doing exactly its job — a useful reminder to verify a suspected bug against the system's actual state before concluding it's broken.

**Verified**: full `php -l` sweep clean; a route sweep across every public page plus `/enquire/` returns 200, `/admin/` correctly 302s to `/admin/login/` when logged out; `database/seed.sql` round-trip tested against a fresh database (schema import → seed import → row counts match: 6 regions, 208 countries, 9 visa types, 1,872 `country_visa_types`, 5 FAQs, 13 settings, 1 admin user); a full real-browser Playwright walkthrough — submit a public `/enquire/` form, confirm the reference number, log into `/admin/`, add a country (confirmed present in the database afterward, not just a success message), view that enquiry in the admin inbox, add an FAQ, publish a real (test) visa requirement through `/admin/visa-requirements/` and confirm it renders correctly on the public `/visa/usa/tourist/` page, then delete it and confirm the honest "not yet verified" state returns — all passed, with every test artifact (the test country, test enquiries, the test FAQ, the test requirement, accumulated rate-limit rows) cleaned out of the database before finalizing. Screenshots confirm the homepage, footer, admin dashboard, admin countries list, and admin enquiries inbox all render correctly with zero browser console errors.

---

## Phase 1 CRM/ERP — customers, visa pipeline, RBAC, audit log, document management (visa consultancy + attestation scope only)

Explicit client request: a full travel-agency CRM/ERP (customer database, visa enquiry/application pipeline, a Forex module, a Tour Package module, RBAC, document management, audit logging, soft deletes, reference numbers, reporting, an installer wizard) with all business data "stored securely on the cPanel hosting server" and an explicit "Do Not Build a Fake Database" requirement — no demo data, no hard-coded dashboard numbers, every enquiry a real database record. Two things needed the client's confirmation before writing any code, put via `AskUserQuestion` rather than assumed: (1) the spec's Forex, Tour Package, Hotel/Flight, Travel Insurance, Medical Assistance, and Yellow Fever Certificate modules describe services that have never been confirmed as real Visagiri offerings anywhere in this project's history (the confirmed business, established over every prior phase, is visa consultancy + document attestation only); (2) the sheer scope, even scoped down. The client confirmed: **no fabricated service modules** — visa consultancy + attestation only — and **sequence it as Phase 1 (core CRM) first, report back, then agree on Phase 2** (reporting/CSV export, installer wizard, anything further) rather than building the full 33-section spec unchecked. This entry covers Phase 1, now complete.

**Database**: `database/schema-crm.sql` adds `roles`/`permissions`/`role_permissions` (3 roles seeded — Super Admin, Admin, Visa Consultant, not the spec's full 5–6, deliberately scoped down to match Phase 1's real needs and easy to extend later since the whole system reads roles from the table, not hard-coded), `user_sessions` (a login audit trail, distinct from PHP's own session storage), `password_resets`, `login_attempts`; `customers` (passport number stored encrypted, never in plain text — see below); `enquiries` renamed to `visa_enquiries` with `customer_id`/`assigned_user`/`priority`/`source_page` added; new `visa_applications`, `visa_applicants`, `visa_application_notes`, `visa_application_history`, `visa_quotes`, `visa_payments`; new `general_enquiries` (replacing `contact_messages` — see migration below); `documents`; `audit_logs`. Every table the spec calls out for soft delete (`customers`, `visa_applications`, `general_enquiries`) has `deleted_at`/`deleted_by`; there is no hard-delete path anywhere in the Phase 1 admin UI.

**Data migration, run for real, not just planned**: `contact_messages`' existing rows were migrated into `general_enquiries` (`service_type = 'general'`, status values mapped to the new enum) before the old table was dropped — no existing enquiry data was lost. `enquiries` was renamed in place (`RENAME TABLE`), not recreated, so its existing rows and their reference numbers carried over unchanged. This meant every piece of code still pointing at the old table names — `pages/enquire.php`, `pages/contact.php`, and `admin/pages/dashboard.php` — was now silently broken (a live PDOException on next submission); all three were found via a project-wide grep for the old table names and rewired to the new schema as part of this same pass, not left as a follow-up.

**RBAC, enforced server-side, not just in the sidebar**: `includes/rbac.php`'s `has_permission()`/`require_permission()` gate every admin page — including `countries.php`, `visa-types.php`, `visa-requirements.php`, `faqs.php`, `embassies.php`, and `settings.php`, none of which had any permission check before this pass even though the sidebar nav now conditionally hides their links based on `content.manage`/`settings.manage`. A `current_admin_scoped_to_assigned()` check (true only for the Visa Consultant role) additionally restricts visa-application and document access to records assigned to that user — enforced at the query/access-check level in `visa-applications.php` and `document-download.php`, not just hidden from a list view. Verified directly: a real Visa Consultant test account correctly got 200s on `/admin/visa-applications/` (its own scoped list) and `/admin/customers/`, but a real 403 — not a redirect, not a hidden link — on `/admin/countries/`, `/admin/users/`, `/admin/audit-log/`, and on viewing an application/document it wasn't assigned to.

**Security**: passport numbers are encrypted at rest with AES-256-GCM (`includes/encryption.php`, a random 12-byte IV per value) rather than stored in plain text; a separate HMAC-SHA256 `searchable_hash()` column allows exact-match customer search without ever decrypting stored rows for a scan. Document uploads are validated by real file-content MIME sniffing (`finfo`, not the client-supplied type or extension) against a whitelist (pdf/jpg/jpeg/png/doc/docx), capped at 10MB, stored under a randomized filename outside the web root, and served only through an authenticated admin download endpoint — never a direct URL. Every state-changing action (customer/enquiry/application CRUD, status/assignment changes, document upload/verification, quotes, payments, user management) is CSRF-protected and writes to `audit_logs` via a single `log_action()` helper, which deliberately never copies a decrypted sensitive value (e.g. a passport number) into the log itself. Reference numbers (`VG-{PREFIX}-YYYY-NNNNNN` — customers, visa enquiries, visa applications, quotes, general/attestation enquiries) are generated server-side by a centralized `generate_reference_number()` helper backed by each column's `UNIQUE` database constraint, with a retry-once loop at every call site for the rare same-instant collision — never generated client-side.

**A real CSRF gap self-caught before any external testing**: document verify/reject actions in `visa-applications.php` and `general-enquiries.php` were initially built as GET links (`?verify=verified`), which is a state-changing GET and therefore a CSRF risk even with the rest of the form protected — caught on review and refactored to proper CSRF-protected POST forms before this pass's verification began.

**Public-facing forms rewired to the new schema, and a form added where the spec called for one that didn't exist**: `/enquire/` and `/contact/` now insert into `visa_enquiries`/`general_enquiries` respectively, both using the new centralized reference-number generator (replacing a now-redundant, table-name-stale `generate_enquiry_reference()` helper, deleted). The 13 attestation service detail pages (`/attestation/{slug}/`) previously offered only a WhatsApp CTA with no way to leave a trackable enquiry — each now also has a real enquiry form (name/email/phone/message), CSRF-protected and rate-limited the same way as `/contact/`, writing to `general_enquiries` with `service_type` set to the specific attestation slug so the admin CRM can filter by exact service.

**Admin CRM built**: `customers.php` (full CRUD, encrypted passport field with plaintext shown only to a permitted admin re-decrypting it, soft delete, a detail view rolling up linked enquiries/applications/documents); `visa-enquiries.php` (status/priority/assignment, a "Convert to Application" action that auto-creates a customer if one doesn't already exist and seeds the first application-history row); `visa-applications.php` (applicants, documents, notes, quotes, payments, full status history, all scoped by the RBAC assignment check); `general-enquiries.php` (mirrors the visa-enquiry pattern for the attestation/general channel); `document-download.php` (the single authenticated download endpoint every document link goes through); `users.php` (new — admin user + role management, suspend/activate only, never a hard delete, self-suspension blocked); `audit-log.php` (new — read-only, filterable by module and by user). `admin/pages/dashboard.php`'s stat cards and "recent enquiries" table were rewritten from the old, now-invalid `enquiries`/`contact_messages` queries to real counts against the new schema (customers, new visa enquiries, open applications, new general/attestation enquiries, documents pending verification, quotes awaiting response) — every number a live `COUNT(*)`, none hard-coded, per the spec's own "Do Not Build a Fake Database" requirement.

**Verified end-to-end against the real local database, not just `php -l`**: a full project-wide grep confirmed zero remaining references to the dropped `contact_messages` table or the old `enquiries` name anywhere in the codebase after the rewiring pass; every touched file passed `php -l`. A PHP dev server was started against the real MariaDB instance and driven through the actual golden path via `curl` (cookie-jar sessions, real CSRF tokens read out of each page, a real multipart file upload) rather than direct DB inserts: submitted a real `/enquire/` enquiry (got back `VG-ENQ-2026-000002`, confirmed the row and its `country_id`/`visa_type_id` in the database), a real `/contact/` general enquiry, and a real attestation-service enquiry — confirmed all three landed with the correct `service_type`/`source_page`; converted the visa enquiry to an application from the admin panel (confirmed the application row, an auto-created customer row, and two `audit_logs` entries — `create visa_applications` and `create customers` — all appeared); uploaded a document and confirmed a deliberately non-PDF file (plain text with a `.pdf` name) was correctly rejected by real content-based MIME sniffing while a genuine PDF was accepted; verified that document from the admin UI and downloaded it back successfully, confirming both actions were captured in the audit log; loaded `/admin/audit-log/` and `/admin/users/` and confirmed they render real data. Created a real Visa Consultant test account and confirmed the RBAC scoping described above holds under actual HTTP requests, not just a code read. **Every test artifact was cleaned from the database before finalizing** — no test customers, enquiries, applications, documents, audit-log rows, or the test consultant account were left behind, and the one test-uploaded file was deleted from `storage/documents/`.

**One thing this verification pass could not leave clean**: the local dev database's `admin` account password was not previously known to this session (Phase C's seed generated it randomly and it was "communicated to the client separately, not committed," per that phase's own audit entry) and had to be reset directly in the database to log in and test the admin panel. This only affected the local, ephemeral development database used for this verification — nothing was deployed — but it means the local `admin` account's password is now `TempAdminE2E#2026` rather than whatever it was before. **Not yet built, deferred to Phase 2 per the agreed plan**: CSV/Excel reporting export, the installer/setup wizard (with its required post-use self-disable), and any further modules — none of these were built, and none of the explicitly-excluded Forex/Tour Package/Hotel/Flight/Travel-Insurance/Medical-Assistance/Yellow-Fever-Certificate modules were built, per the client's own choice to exclude fabricated services.

---

## HRMS Recruitment CRM — Phase A (public careers flow + core candidate pipeline)

Explicit client request: a full Employment Application + HRMS Recruitment CRM — a public careers section with a per-job application form (auto-attaching the job, dynamic per-role questions, CV/document upload, duplicate detection, source tracking, social sharing), and a separate `/hrms/` recruitment CRM (dashboard with charts, candidate database with search/filtering, a Kanban pipeline, interview scheduling/feedback, RBAC login distinct from the visa CRM, audit logging, reporting/export). Two things were confirmed with the client via `AskUserQuestion` before writing any code, since they directly conflicted with or were unconfirmed against this project's established practice: (1) the spec asked for "configuration/environment variables" for credentials, contradicting the explicit no-`.env`/plain-PHP-config-file discipline every phase of this project has followed — client chose to **keep the existing plain-PHP-config pattern**; (2) the spec requires real candidate/HR email notifications and a password-reset email through `info@visagiri.com`, but this project has never had a working SMTP integration (confirmed in the Phase 1 CRM audit entry above) — client chose **a fully wired, pluggable mailer with real SMTP credentials supplied later**, so nothing here claims an email was sent that wasn't. Given the spec's size (34 sections), the client also chose to **sequence it**: Phase A (this entry) covers the core application-to-hire flow; Phase B (deferred, to be confirmed before starting) covers email notifications once SMTP is supplied, social-sharing images/LinkedIn share text, CSV/Excel reporting export, and a refined dynamic-question admin UI.

**Architecture decision, stated plainly rather than assumed silently**: HRMS reuses the visa CRM's `admin_users`/`roles`/`permissions`/`audit_logs` tables and session/login/password-hashing infrastructure (`includes/auth.php`) rather than building a parallel account system — same staff pool, same proven security properties (timing-safe login, CSRF, rate limiting, single-use time-limited reset tokens), just two new roles (HR Admin, HR Executive) and a set of `hrms.*` permission keys layered on top, and its own `/hrms/` URL area, login page, and branded chrome (`includes/hrms-layout.php`, `assets/css/hrms.css`) so it reads as its own product rather than a re-skin of `/admin/`, per the spec's "premium modern recruitment CRM, not a basic admin panel" requirement. Visiting `/hrms/` while logged out (or logged in as a visa-CRM-only account with no `hrms.*` permissions) always redirects to `/hrms/login/`, never renders an open dashboard — the spec's explicit "no open HRMS URL" requirement, verified directly (see below).

**Database**: `database/schema-hrms.sql` adds 16 tables — `hr_departments`, `hr_vacancies`, `hr_application_questions` (per-vacancy, HR-editable, no code change needed to add one), `hr_candidates` (one row per real person, matched by email/mobile), `hr_applications` (one row per candidate-vacancy submission — what an "Application ID" actually refers to), `hr_candidate_answers`, `hr_education`, `hr_employment_history`, `hr_documents` (a dedicated table, not a reuse of the visa CRM's `documents` table, since CVs carry their own access-audit requirement), `hr_document_access_log` (who viewed/downloaded which CV, when — section 14's explicit requirement), `hr_interviews`, `hr_interview_feedback`, `hr_notes`, `hr_status_history`, `hr_email_log` (wired but unpopulated until Phase B's SMTP is configured). `database/seed-hrms.sql` seeds the two new roles and 8 `hrms.*` permissions, including re-granting them to the already-existing Super Admin role (its original seed's `CROSS JOIN` already ran and wouldn't retroactively pick up new permission rows). Recruitment sources (website/LinkedIn/Facebook/WhatsApp/referral/job portal/other) are modeled as a fixed `ENUM` rather than an admin-editable lookup table, since the spec's own list is fixed and doesn't need one. **A real bug caught during the initial schema load**: several of the new tables' foreign-key constraint names (`fk_notes_admin`, `fk_notes_application`) collided with existing constraint names already used by the visa CRM's `visa_application_notes` table — MySQL/MariaDB requires FK constraint names to be unique per-schema, not per-table. Caught immediately by the `CREATE TABLE` failing outright (errno 121), not a silent issue; fixed by renaming to `fk_hr_notes_*` and confirming no other collisions existed via a full diff against every constraint name already in the database before re-running.

**Public careers flow** (`careers/index.php`, one handler for three states, mirroring `attestation/index.php`'s established pattern): `/careers/` lists published vacancies whose closing date (if any) hasn't passed, even if HR forgot to move the status to "closed" — a defensive check, not a fabricated status. `/careers/{slug}/` is the SEO'd job detail page — job title, department, location, employment type, experience, qualification, salary range, description, responsibilities, skills, benefits, deadline, reference number — with per-page `JobPosting` structured data (`datePosted`/`validThrough`/`employmentType`/`hiringOrganization`/`jobLocation`/`baseSalary`, null fields stripped rather than emitted as `"field": null`) and canonical/OG metadata via the existing `$pageTitle`/`$pageDescription`/`$canonicalUrl`/`$structuredData` convention. "Apply Now" (`target="_blank" rel="noopener"`) opens `/careers/{slug}/apply/` in a new tab with the job already attached from the URL — the candidate is never asked to select it again. The application form covers every field group the spec lists (personal, professional, education, skills), renders each vacancy's dynamic questions from `hr_application_questions` with type-appropriate inputs (text/textarea/number/dropdown/yes-no) and required-field validation, requires all four declaration/consent checkboxes with a `consent_given_at` timestamp, validates the mandatory CV upload plus five optional document types through the same MIME-sniffing `validate_document_upload()` already proven in the visa CRM (never trusting client-supplied type or extension), and — on success — shows a confirmation (name, job, Application ID, submission date, next step) rendered in the *same* POST response rather than a GET redirect, so no reference number or personal data ever appears in a URL. Duplicate detection (`hrms_find_or_create_candidate()`) matches on email or mobile against `hr_candidates`; a match reuses that candidate's identity and flags the new application `is_possible_duplicate = 1` with a pointer to the prior application, for HR to review — never silently merged or silently duplicated.

**A real bug found and fixed during this pass's own verification, not by a later audit**: the `?source=linkedin` (and `&ref_name=...`) query-string parameters used for source tracking were read from `$_GET` only at the point of the database insert — but by the time the candidate submits the form, the browser has POSTed back to the same URL *without* that query string, so every submission was silently recording `source = 'website'` regardless of how the candidate actually arrived. Caught by testing the actual flow with a `?source=linkedin` link and checking the resulting database row, not by reading the code. Fixed by capturing the value once (from `$_GET` or a resubmitted `$_POST`) into a hidden form field on page load, so it survives the POST — re-tested and confirmed the row now correctly records `source = 'linkedin'`, `source_detail = '<referrer name>'`.

**HRMS admin** (`hrms/`): `pages/login.php` (mirrors `/admin/login/`'s timing-safe, rate-limited, CSRF-protected pattern, its own redirect-after-login session key so it never conflicts with `/admin/`'s), `pages/forgot-password.php` + `pages/reset-password.php` (new — reuses the token machinery already built in `includes/auth.php` — `create_admin_password_reset_token()`/`verify_admin_password_reset_token()`/`reset_admin_password()` — but never previously wired to any actual page; same non-enumerating "always show the same generic message" discipline as this project's other reset flows, sends from `mail_from_address` which is already configured as `info@visagiri.com`, and — since no SMTP relay exists yet — also shows the reset link directly when `APP_DEBUG` is on, exactly the same honest fallback used by the CRM's Phase 9 forgot-password flow); `pages/dashboard.php` (every stat card and chart — vacancies, applications-by-status, by-job, by-department, by-source, monthly activity — a real query against `hr_applications`/`hr_vacancies`, rendered as simple CSS bar charts rather than pulling in an external charting library, consistent with the rest of this project's no-heavy-dependencies approach; automatically scoped to only the current HR Executive's assigned candidates when that role is logged in); `pages/vacancies.php` (create/edit, draft→published→paused→closed workflow, an inline "add a department" field so HR never needs a separate department CRUD page for something this small, and a per-vacancy dynamic-question builder — add/remove a question without any code change); `pages/candidates.php` (search/filter by name, application ID, job, status, assigned HR; a full candidate profile with the spec's exact tab set — Overview/Resume/Documents/Education/Experience/Interview/Communication/Activity Log — status changes, HR reassignment, notes, interview scheduling, interview feedback with a 1–5 rating and select/hold/reject recommendation, and soft delete); `pages/pipeline.php` (a Kanban board across the main 8-stage pipeline plus a separate Rejected/On Hold/Withdrawn row, moving a card through the same `update_status` handler and `hr_status_history` writer the profile page uses — one code path for a status change regardless of where it's triggered from); `pages/document-download.php` (the only way a CV is ever reachable — authenticated, scoped, and logging every access to `hr_document_access_log`).

**Security**: every HRMS page enforces its permission server-side via `require_permission()`/`require_hrms_login()` before rendering anything — verified directly, not assumed from the code: a real HR Executive test account got a real `403` (not a redirect, not just a hidden link) on `/hrms/vacancies/`, on viewing an unassigned candidate, and on downloading an unassigned candidate's CV, while correctly getting `200`s and a scoped-only candidate list for their own assigned candidate. `current_admin_hrms_scoped_to_assigned()` (`includes/rbac.php`) mirrors the visa CRM's existing `current_admin_scoped_to_assigned()` pattern rather than inventing a new one. Every sensitive action (vacancy create/status-change, question add/remove, candidate status change, assignment, note, interview scheduling/feedback, delete) writes to the same shared `audit_logs` table used by the visa CRM — confirmed those entries actually appear when filtering `/admin/audit-log/` by `module = hr_applications`, not just written and never checked.

**Verified end-to-end against the real local database**, via `curl` with real cookie-jar sessions, real CSRF tokens, and real multipart uploads — not direct database inserts: created a real HR Admin test account, created and published a real vacancy with a real dynamic question through the HRMS UI, confirmed it appeared correctly on the public `/careers/` listing and `/careers/{slug}/` detail page including its `JobPosting` schema; submitted a real application with a genuine PDF CV (confirmed accepted) and, separately, confirmed a plain-text file renamed to `.pdf` was correctly rejected by real content-based MIME sniffing; confirmed the resulting `VG-HR-2026-000001` reference number and every submitted field (personal, professional, education, skills, dynamic answer, consent + timestamp) landed correctly in the database; confirmed the HRMS dashboard's stat cards and charts updated immediately to reflect it; assigned the candidate to a real HR Executive test account and confirmed the RBAC scoping described above; downloaded the CV as that HR Executive and confirmed both the file integrity and a real `hr_document_access_log` row; moved the candidate through the Kanban pipeline (New → Screening) and confirmed a real `hr_status_history` row with remarks; scheduled a real interview and recorded feedback (rating, recommendation, notes), confirming the interview's status flipped to "completed" and the application's status flipped to "Interview Scheduled" automatically; added an internal note and confirmed it appeared in the Communication tab; ran the full forgot-password → dev-mode reset link → set new password → old password rejected → new password accepted → reset token rejected on reuse chain end-to-end; soft-deleted a candidate and confirmed it disappeared from both the detail view and the list without a hard delete; confirmed a second, un-referred applicant with the same email as an earlier one was correctly flagged `is_possible_duplicate` with a pointer to the original application. Playwright screenshots at 1440×900 and 390×844 confirm the careers listing, job detail page, the full application form, the HRMS login page, the HRMS dashboard, the Kanban board, and a candidate profile all render correctly and responsively, with the mobile application form correctly stacking every field group. **Every test artifact — the test vacancy, department, candidates, applications, documents, interviews, notes, audit-log rows, rate-limit rows, and both test HR accounts — was removed from the database and `storage/hrms-documents/` before finalizing.**

**Not yet built, deferred to Phase B per the agreed plan**: automated email notifications (application received, interview invitation/reschedule, selection/rejection, and the HR-facing "new application"/"shortlisted"/"interview scheduled" notifications) — the mailer call sites and `hr_email_log` table exist, but nothing sends until real SMTP credentials are supplied; social-sharing buttons (LinkedIn/Facebook/WhatsApp/X/Copy Link) and the attractive share-preview image generation; the pre-written LinkedIn recruitment-message workflow; CSV/Excel report export and the full report set (shortlisting/interview/selection/joining/rejection ratios, recruiter performance, time-to-hire); a dedicated department-management screen (departments are currently created inline from the vacancy form, which covers the spec's actual need without a separate CRUD page); and further search/filter criteria on the candidate list beyond name/application ID/job/status/assigned HR (experience/location/salary/date-range filtering on free-text fields was judged not worth the complexity for Phase A and is honestly flagged here rather than half-built).

---

## Forex Buy / Foreign Currency Purchase module — Phase A (core flow: request intake, compliance, quotations, delivery gate)

Explicit client request: a full Forex Buy Quotation, Documentation, Compliance & Request Tracking module. This directly reversed an earlier explicit decision — when this project's Phase 1 CRM scope was confirmed, the client chose "None — just visa consultancy + attestation" when asked which of Forex/Tour Packages/Hotel/Flight/Travel Insurance/Medical Assistance/Yellow Fever Certificate modules were real Visagiri services, explicitly excluding Forex. Given that direct reversal, plus the spec's own scale (39 sections: quotation engine with rate snapshots, multi-level approval, payment tracking, denomination management, delivery compliance gate, FEMA/audit records, declaration templates, country-rules admin, customer portal, full reports/export), both points were confirmed via `AskUserQuestion` before any code was written: the client confirmed Forex is now a real, active/planned Visagiri service, and chose to sequence the build the same way the visa CRM and HRMS builds went — **Phase A (this entry): request intake, document verification, quotations with immutable rate snapshots, the compliance-gated delivery control, RBAC, audit trail, and a live dashboard.** Deferred to Phase B: payment gateway integration, denomination management, PDF/WhatsApp/email quotation delivery, declaration template versioning with variable substitution, a friendlier country-rules admin workflow, and full reports/export.

Immediately after this, the client also sent a 39-section "Unified Enquiry CRM + SLA Assignment System" spec proposing a generic `enquiries`/`enquiry_types`/`enquiry_fields` architecture that would replace the visa CRM's `visa_enquiries`/`general_enquiries` tables and layer an SLA/auto-assignment/escalation/workflow-automation engine on top of everything, including Forex — and it re-listed Travel/Flight/Hotel/Travel Insurance/Medical Assistance/Yellow Fever Certificate as first-class enquiry categories, the same non-confirmed services excluded twice already. Rather than derail the in-progress Forex build or silently start replacing already-shipped infrastructure, this was flagged and confirmed via `AskUserQuestion`: the client agreed to finish Forex Phase A first and scope the Unified Enquiry CRM separately afterward, and confirmed the enquiry-category list for that future phase should stay limited to confirmed real services (Visa, Forex, Apostille/Attestation, General) rather than the six unconfirmed ones. That work has not started — this entry covers Forex Phase A only.

**Architecture decision**: unlike HRMS (which the client's own spec required at a separate `/hrms/` URL with its own login), this spec frames Forex as "CRM menu: Forex → ..." within the existing CRM, and section 2's "Search Existing Customer... in CRM" plus section 35's Step-1-through-Step-9 wizard (Customer → Travel → Visa → Forex Requirement → Documents → Quotation → Compliance → Payment → Delivery) describes a staff-driven workflow, not a public self-service form — a real customer would never fill in their own "Approval" or "Delivery" step. So Forex lives at `/admin/forex-*` (new admin sub-pages, same session/login/RBAC as the rest of the CRM, added as a new "Forex" section in the existing sidebar), with the one genuinely public piece being `/forex/track/` (reference number + registered mobile, non-enumerating, mirroring this project's other public lookup pages — `/track-visa/` itself no longer exists in this codebase, having been removed during an earlier no-database phase and never restored, so this was built fresh from the same established pattern rather than literally copied).

**Database**: `database/schema-forex.sql` adds 10 tables — `forex_rates` (admin-maintained buy/sell rates, never auto-fetched from a live API in this phase), `forex_country_rules` (admin-editable, drives the automatic document checklist per section 18 — ships completely empty, since this project never asserts a country's visa requirement as fact without an authorized user entering it; a country with no rule yet falls back to the full standard checklist, never to "nothing required"), `forex_requests`, `forex_documents` (verification fields live directly on the document row rather than a separate `forex_document_verifications` table — simpler, still fully satisfies "every document must have a verification status" since it's a strict 1:1 relationship), `forex_declarations`, `forex_quotations` (every quotation is a permanent, immutable snapshot — a rate change in `forex_rates` never touches an existing row here), `forex_approvals`, `forex_payments`, `forex_deliveries` (total amount only in Phase A — denomination breakdown is Phase B), `forex_status_history`. `customers` gained `pan_number_encrypted`/`pan_number_hash` via `ALTER TABLE`, reusing the exact same AES-256-GCM + searchable-hash pattern already used for passport numbers (`includes/encryption.php`) — PAN is exactly as sensitive and gets exactly the same treatment, not a new one. `database/seed-forex.sql` adds four new roles (Forex Manager, Forex Executive, Compliance/Audit, Accounts) and 15 `forex.*` permissions; the spec's fifth role, "Sales/Travel Consultant," was **not** built as a new role — the existing `visa-consultant` role already matches that exact description (scoped to assigned records, create-only, no approval rights), so it was simply granted the relevant `forex.*` permissions instead of creating a near-duplicate role. `current_admin_forex_scoped_to_assigned()` (`includes/rbac.php`) mirrors the already-proven `current_admin_hrms_scoped_to_assigned()` pattern rather than inventing a new one.

**The compliance-gated delivery control (section 17, the spec's own "extremely important" requirement)**: `forex_delivery_blockers()` (`includes/forex.php`) is the single source of truth for whether a request can be delivered — it re-fetches everything itself from the database rather than trusting caller-supplied state, so the document-completion checklist a staff member sees can never disagree with what the hard gate actually enforces. It checks, in order: customer identity (passport + PAN numbers) on file; passport and PAN documents verified; air ticket and hotel verified where the country rule (or the safe default) requires them; **visa copy verified when visa status is "Visa Available" or "eVisa," or a hand-signed, verified Visa-on-Arrival/Visa-Free declaration when visa status is "Visa on Arrival" or "Visa-Free"** — the spec's core visa-vs-declaration branch; an outright block when visa status is "Not Available," "In Process," or "Other," using the spec's exact warning text; payment confirmed as `paid`; and a completed approval. `admin/pages/forex-requests.php`'s `mark_delivered` action enforces this same function server-side before allowing delivery — a user without `forex.delivery.manage` gets a real `403`, not a hidden button (verified directly, see below). An authorized override (a distinct `forex.delivery.override` permission) is possible, but requires a written reason and writes a permanent `forex_approvals` row capturing the exact list of blockers overridden, the reason, the approver, and the IP address — never a silent bypass.

**File naming and document history**: uploads are renamed to `{reference}-{DOCTYPE}-{random}.ext` (e.g. `VG-FX-2026-000001-PASSPORT-39789bef01966b3c.pdf`) per section 28, never the customer's original filename. Replacing a document (e.g. after a rejection) creates a new row and marks the old one `is_current = 0` with a `deleted_at` timestamp — full history preserved, exactly per section 9's requirement, verified directly (see below).

**A real, pre-existing bug found and fixed during this pass's verification — not introduced by this module, but exposed by it**: `admin/pages/forex-requests.php`'s search query reused the same named PDO placeholder (`:search`) six times in one `OR`-chained `WHERE` clause. This project's database connection explicitly sets `PDO::ATTR_EMULATE_PREPARES => false` (real, non-emulated prepared statements) precisely because emulated prepares can mask SQL-handling bugs — but real prepared statements do not support the same named placeholder appearing more than once in a single query, and hit it with a fatal `PDOException: Invalid parameter number` the moment a real search term was typed. A project-wide grep for the same anti-pattern found it already present, unnoticed, in **three previously-shipped files**: `admin/pages/customers.php` (Phase 1 CRM), `hrms/pages/candidates.php` (HRMS Phase A), and this session's own `admin/pages/forex-fema-audit.php` — every one of them would have thrown the identical fatal error the first time a real user typed a non-empty search term, and none of it was caught by any previous verification pass because none of them had actually exercised those search boxes with a real term end-to-end. Fixed in all four files by giving each placeholder occurrence its own name (`:search1`, `:search2`, …) bound to the same value, then re-verified the customer search, HRMS candidate search, and forex reference-number search all now return real results with zero errors.

**Verified end-to-end against the real local database**, via `curl` with real cookie-jar sessions, real CSRF tokens, and real multipart uploads, working through every one of the spec's own 10 named test scenarios: **Test 1** (full happy path) — created a real Forex Manager account, added a real USD rate, created a request with `visa_available` status, uploaded and verified all five required documents (passport/PAN/air ticket/hotel/visa), created a quotation (2,000 USD × ₹83.25 = ₹166,500 base + ₹826 charges = ₹167,326 total, calculated correctly), approved it, recorded payment as `paid`, and successfully marked it delivered once every blocker cleared. **Test 2** — a request with `visa_available` but no visa document uploaded correctly blocked delivery with "Visa copy not uploaded." **Test 3** — a `visa_on_arrival` request correctly required a signed declaration instead of a visa copy in both the checklist and the delivery gate; uploading and verifying the declaration cleared that specific blocker. **Test 4** — an unverified PAN document correctly kept the request blocked ("PAN copy is uploaded but not yet verified"). **Test 5** — rejecting the passport document correctly blocked delivery with the rejection reason surfaced. **Test 6** — after creating the quotation, the master USD rate was changed from 83.25 to 90.00 in the rate table; the existing quotation's rate and total amount were confirmed completely unchanged, proving the immutable-snapshot design works under a real rate change, not just in theory. **Test 7** — a real Forex Executive test account (lacking `forex.delivery.manage`) attempted delivery directly via a forged POST and was correctly rejected with a genuine `403`, with the request status unchanged. **Test 8** — the Forex Manager overrode a blocked delivery with a written reason; confirmed a permanent `forex_approvals` row was created capturing the exact blocker list, the reason, the approver's ID, and the IP address. **Test 9** — replacing a rejected passport document was confirmed to leave the old row intact (`is_current = 0`, `deleted_at` set) while the new upload became current and `pending` again. **Test 10** — searching by `VG-FX-2026-000001` returned the request, and its FEMA/audit record view showed the complete status history from creation through delivery. Additionally confirmed: the public `/forex/track/` page shows the correct customer-safe timeline for a matching reference+mobile pair and the same generic "couldn't find a matching" message for a mismatched mobile (non-enumerating, per this project's established pattern); a delivered transaction correctly could not be deleted (no delete button rendered, and a forged delete POST was also rejected server-side). **Every test artifact — all test requests, customers, documents, quotations, approvals, payments, deliveries, declarations, rates, audit-log rows, and both test Forex accounts — was removed from the database and `storage/forex-documents/` before finalizing.**

**Not yet built, deferred to Phase B per the agreed plan**: payment gateway integration (Phase A is manual status entry only); denomination-level delivery breakdown (`forex_denominations` — Phase A records only the total amount delivered); customer-facing PDF quotation generation and the Download PDF/WhatsApp/Email send buttons; declaration template management with `{{variable}}` substitution and version history (Phase A supports uploading and verifying a signed declaration with its required metadata, but not a generated, admin-editable template); a friendlier country-rules admin workflow (the current one is a functional but plain CRUD form); automated candidate/HR email notifications (same honest constraint as HRMS — no SMTP configured yet); full reports (currency-wise, country-wise, pending-compliance, delivery, payment, audit/FEMA) and CSV/Excel/PDF export — the FEMA/audit view built in Phase A is read-only; and the customer portal tie-in. This is also explicitly a record-management tool, not a claim of FEMA/RBI legal compliance — every compliance-facing screen says so directly, and the country-rules table ships empty specifically so no legal assumption is baked in without the company's own authorized compliance/legal review.

## Primary header nav — icons added to every top-level tab

Client request: add icons to the main site header's top-level nav tabs (Visa Services, Attestation, Countries, Visa Process, Visa Updates, Company). A new `primary_nav_icon()` helper (`includes/functions.php`) was added rather than reusing `company_nav_icon()` directly, but deliberately matches its exact visual language — 16px, `viewBox 0 0 20 20`, `stroke="currentColor"`, `stroke-width="1.6"`, rounded caps/joins — so a top-level tab icon and the icons inside its own mega-menu (e.g. Company's trigger icon next to the "About Us"/"Leadership"/"Partners" icons in its mega-menu) read as one consistent icon system rather than two unrelated sets. Six icons were designed: a passport-style card for Visa Services, a certificate-with-checkmark for Attestation, a globe (circle + meridian ellipse + equator line) for Countries, two connected nodes for Visa Process (reflecting the step-by-step nature of that page), a bell for Visa Updates, and a two-building skyline (reusing the existing "building" visual idea already established in the Company mega-menu) for Company.

`includes/header.php` was updated on both the desktop nav and the mobile accordion menu — six insertion points each — wrapping each icon+label in a `.site-header__nav-icon` span. One layout bug was caught before shipping: the three mobile `<summary>` accordion triggers (Attestation, Countries, Company) rely on `display:flex; justify-content:space-between` to keep their label on the left and the "+"/"×" expand indicator on the right; dropping a bare icon span directly inside `<summary>` would have made it a third flex child and broken that spacing. Fixed by wrapping icon+label together in a single `.site-header__mobile-summary-label` span so the layout still sees exactly one content item plus the indicator.

`public/assets/css/layout.css`: top-level nav links became `inline-flex` (desktop) / `flex` (mobile) containers with a small gap; `.site-header__nav-icon` defaults to a muted color and transitions to the brand blue in sync with its parent link's own hover/active color rule, so icon and label always change color together rather than looking like two separate elements. Changing `.site-header__mobile a` from `display:block` to `display:flex` was checked against the existing CSS first — the more specific `.site-header__mobile-accordion-body li a` rule already overrides `display:block` for links nested inside an opened accordion, so this change only affects the six true top-level mobile items as intended.

**Verified visually** with Playwright at both 1440×900 and 390×844: all six desktop icons render aligned with their labels with no overflow; hovering the Company tab correctly turns both its icon and label blue while opening its mega-menu; the mobile hamburger menu shows all six icons correctly aligned, and opening the Attestation accordion confirms the icon/label/indicator layout holds with the panel expanded (the accordion body's own pre-existing category icons continue to render underneath, unaffected). No functional code was touched — this is markup/CSS/icon-helper only.

## Footer column headings — icons added, matching the header nav

Client follow-up request: extend the same icon treatment to the footer's column headings (Visa Services, Attestation, Countries, Company, Resources). Rather than design a second icon set, `primary_nav_icon()` (`includes/functions.php`, added in the header pass above) was reused as-is for the four headings it already covers — a "Visa Services" icon in the footer is the identical SVG as the header's "Visa Services" tab icon, reinforcing one icon system across the whole site rather than two. One new key, `resources` (an open-book glyph, same 16px/`viewBox 0 0 20 20`/`stroke=currentColor` spec as the rest), was added for the one footer-only column that has no header-nav counterpart.

`includes/footer.php`: each of the five `<h3>` column headings got an icon span (`<span class="site-footer__col-icon">`) inserted before its label; the Brand column (logo + tagline) was left untouched since it isn't a labeled list column.

`public/assets/css/layout.css`: `.site-footer__col h3` became a `flex` container (`align-items:center; gap`) so the icon sits inline with the uppercase heading text without disturbing the existing `white-space:nowrap` rule that keeps every column heading on one row; `.site-footer__col-icon` colors the icon `var(--visa-gold)` — a static accent rather than the header nav's hover-driven color, since footer headings aren't links and don't have a hover/active state to sync against.

**Verified visually** with Playwright at 1440×900 and 390×844. Desktop: all five icons render in gold next to their headings, correctly aligned, with the five-column row still lined up. Mobile: confirmed via viewport screenshots at precise scroll offsets rather than an element screenshot — an element screenshot of the (very tall, since it's `display:block` full-width on mobile) `.site-footer` node produced a stitching artifact where the page's `position: sticky` header appeared duplicated mid-image, a known Playwright behavior when it composites a multi-viewport-tall element capture with a sticky/fixed ancestor still in the DOM; this is a screenshot-tooling artifact, not a site bug, and was avoided by scrolling to a computed offset and capturing the visible viewport instead. Both the Visa Services and Attestation column icons were confirmed correctly rendered and aligned this way. No functional code was touched.

## Footer brand intro copy update

Client-supplied replacement copy for the footer brand column's descriptive line: `includes/footer.php`'s `.site-footer__muted` paragraph changed from "Technology-enabled visa consultancy and document attestation assistance for travellers, students, professionals, and businesses." to "Visagiri drives innovation with smart, future-ready digital solutions that empower growth and transformation." — a client-provided line, not authored or invented here. The trailing "A Unit of Tripgation Pvt Ltd." legal attribution (client-confirmed elsewhere in this document) was kept rather than dropped, since only the descriptive sentence was asked to change. The `.site-footer__tagline` line ("Seamless Visas. Limitless Journeys.") was left as-is — the request was for the intro paragraph, not the tagline. Verified rendered correctly via `curl` and a Playwright desktop screenshot; no layout shift, no functional code touched.

## Delivery package: cPanel zip + fresh admin/HRMS credentials

Client request: package the site for delivery, with login credentials for the admin panel and HRMS. Handled in three parts.

**Credentials regenerated, not reused.** The one existing `admin_users` row (`admin`, super-admin role — full access to CRM/Forex/Admin/HRMS) had its password hash replaced with a freshly generated one; the old plaintext was never recorded anywhere retrievable in this project (per its own seed-file comment, it was only ever delivered out-of-band, consistent with "a password should never live in version control"), so this delivery generates a new one rather than guessing or exposing a stale hash. A second, new `admin_users` row (`hr.admin`, `hr-admin` role) was added in `database/seed-hrms.sql` — the HRMS module previously had no seeded account of its own; any HRMS access so far went through the same super-admin login as everything else. This gives HR staff a login scoped to `hrms.*` permissions only, separate from the CRM/Forex/Admin super-admin account, matching the RBAC roles this project already defined but never issued a credential for. Both password hashes are bcrypt (cost 12, matching the existing pattern); both plaintext passwords are delivered directly to the client outside this file and outside version control, same discipline as the original admin account — **the placeholder email on the new `hr.admin` account (`hr@visagiri.com`) is not confirmed as a real mailbox and should be updated to the client's actual HR contact address after first login**, same as the password.

**Packaging script fixed to stop shipping real secrets.** `bin/package-cpanel.sh`'s `rsync` reads the live working tree, which — unlike `git`, which already gitignores them — has no awareness of which files hold real secrets. It was silently including the sandbox's actual `config/database.php` (a throwaway local dev password, low risk) and, more seriously, the actual `config/encryption.php` — the real AES-256-GCM key already in use to encrypt passport/PAN numbers in this sandbox's test data. Shipping a live encryption key inside a zip that travels over chat/email is exactly the exposure the project's own gitignore discipline was built to prevent for these two files; the fact that packaging bypassed it was a real gap. Fixed: both files are now excluded from the rsync and replaced with their `.example.php` templates (`CHANGE-ME` placeholders) inside the package, matching what git already ships. `includes/encryption.php` already throws a clear `RuntimeException` rather than silently encrypting with a placeholder key if `config/encryption.php` is left unedited, so this fails loudly instead of quietly shipping bad data.

**Script's printed setup steps corrected for the current schema.** The script's "next steps" output still referenced a single `database/schema.sql` / `database/seed.sql` pair from before the CRM/HRMS/Forex phases split the schema across four files each; it now lists the correct import order (`schema.sql`→`seed.sql`→`schema-crm.sql`→`seed-crm.sql`→`schema-hrms.sql`→`seed-hrms.sql`→`schema-forex.sql`→`seed-forex.sql`, later files depending on tables/rows the earlier ones create) and points to generating a fresh encryption key on the server rather than the removed docs/google-sheets-setup.md reference (docs/ is deliberately excluded from the package to keep it lean).

**Verified end-to-end against the real package, not just the source tree**: built the zip with the fixed script, extracted it standalone, confirmed `config/database.php`/`config/encryption.php` in the extracted package are the `CHANGE-ME` templates (not the sandbox's real values) while all 8 schema/seed files are present; pointed a fresh PHP dev server at the extracted package (with the sandbox's real local config copied back in just for this test, exactly as a real deploy would supply its own) and confirmed `/`, `/countries/`, `/attestation/`, `/visa/usa/`, `/leadership/`, and a static asset all return 200, and that both the reset `admin` password and the new `hr.admin` password log in successfully against `/admin/login/` and `/hrms/login/` respectively in this exact packaged copy. One dead end during this verification is worth recording: testing the flattened package with `php -S host:port -t .` (no explicit router script) produced false `500`s on every folder-named route (`/countries/`, `/admin/`, `/hrms/`, etc.) — `Call to undefined function countries_all()` — because PHP's built-in dev server, with no router script, falls back to serving a matching real *directory*'s own `index.php` directly (bypassing the front controller and all its `require_once`s) whenever the request path happens to match an actual folder on disk, which the flattened layout has plenty of (`countries/`, `admin/`, `hrms/`, ...). This is purely a `php -S`-without-a-router quirk, confirmed not a real bug by re-running with `php -S host:port -t . index.php` (all routes 200) and by re-reading `public/.htaccess`, which already explicitly does not bypass on `-d` (a comment there already flags this exact reasoning) — so real Apache/cPanel hosting is unaffected either way.

**Delivered**: the packaged zip, and both sets of credentials (username + password for `/admin/` and `/hrms/`) sent directly in chat, never written to this file or any tracked file — the client should change both passwords immediately after first login and set a real HR contact email on the `hr.admin` account.

## Live deployment support: import truncation on the client's host + a real ambiguous-column bug found

After delivery, the client's cPanel host repeatedly cut off large multi-statement SQL imports partway through phpMyAdmin — `schema-crm.sql` stopped right after `role_permissions` (before `user_sessions`/`login_attempts`/`customers`/etc.), `schema-hrms.sql` stopped right after `hr_departments`, and separately each of `seed-crm.sql`/`seed-hrms.sql` got through every `INSERT` but not the final `UPDATE admin_users SET role_id = ...` line at the very end of the file — a strikingly consistent "stops right near the end" pattern suggesting a host-side execution-time or request-size limit, not a SQL error. Diagnosed live via `SHOW TABLES` and targeted `SELECT`/`COUNT` checks against the client's actual database (never guessed), then resolved with hand-built "continuation" scripts — containing only the statements that hadn't run yet — verified against a locally-recreated copy of the client's exact partial state before being sent, so nothing risked a "table/column already exists" error on their live site. `schema-forex.sql` had not run at all and was verified and sent in full as-is.

**A real bug, not a deployment issue, surfaced once Forex tables existed**: `admin/pages/forex-dashboard.php`'s currency-wise delivered-volume query joins `forex_deliveries d` and `forex_requests fr` and referenced a bare `currency_code` in both the `SELECT` and `GROUP BY` — both tables have their own `currency_code` column, so MySQL correctly rejected it as ambiguous (`SQLSTATE[23000] 1052`) the first time the page was ever loaded against real data-bearing tables (this couldn't have surfaced during this project's own sandbox verification, since Forex Phase A's own test pass exercised the delivery flow but this dashboard aggregate query specifically was never hit with the tables in a state where it would run — worth noting as a real gap in that earlier verification). Fixed by qualifying both references as `d.currency_code`, matching the delivery's own currency rather than the request's (they're expected to match, but the delivery row is the authoritative record of what was actually handed over). A project-wide grep for every other `currency_code` reference confirmed all of them already qualify with a table alias — this was an isolated instance, not a repeat of the earlier `LIKE :search` repeated-placeholder pattern. Verified by running the corrected query directly against a schema-accurate local database.

## Admin sidebar: fixed unstyled group labels + collapsible sections, premium polish pass

Client screenshot showed the "Forex" section label rendering as bare unstyled text flush against the browser edge, breaking out of the sidebar's padding entirely — traced to `.admin-sidebar__group` (the CSS class used for the Forex/Content/System section dividers in `includes/admin-layout.php`) never having had a CSS rule defined anywhere in the project. It happened to go unnoticed until now because the CRM/HRMS/Forex sidebar sections were built across different phases of this project and none of those verification passes happened to screenshot a page with `content.manage`/`forex.requests.view` permissions active alongside a full sidebar render.

Client follow-up: convert those three sections into click-to-expand/collapse groups (previously always fully expanded, showing every sub-link at once) and give the dashboard a more premium visual treatment. `includes/admin-layout.php`: each `.admin-sidebar__group` divider became a `<button>` (accessible — `aria-expanded`/`aria-controls`, keyboard-focusable, semantically a real button rather than a clickable `<div>`) wrapping its section's links in a `.admin-sidebar__subgroup` container; reuses the existing `nav_chevron_icon()` helper already used by the public header's mega-menus, so the same chevron visual language appears in both places rather than a second icon being invented. A new `$navGroups` map in `admin_header_start()` determines server-side which group (if any) the current page's `$activeNav` belongs to, so that group renders already-expanded on first paint — no JS-dependent flash of a collapsed-then-expanding sidebar, and it still works correctly with JavaScript disabled (everything just stays visible, matching the old always-expanded behavior, rather than becoming inaccessible). `public/assets/js/admin.js` (new file, this project's first admin-specific script) adds the actual click-to-toggle on top of that. `public/assets/css/admin.css` also picked up: an accent-bordered, hover-lifting stat card style; a subtle shadow on the topbar instead of a flat border line; and a hover highlight on table rows.

**Verified visually** with Playwright, logged in as a real super-admin session: on `/admin/dashboard/` (no group active) Forex/Content/System all render collapsed with a down-pointing chevron; on `/admin/countries/` the Content group renders already-expanded server-side with its chevron flipped and "Countries" highlighted as the active link, while Forex/System stay collapsed; clicking the Forex header from the dashboard expands it (chevron flips, all Forex sub-links appear) without disturbing Content/System, and clicking it again collapses it back. `php -l` clean on the modified files.

## Global Visa Documentation Portal — Phase A (architecture only; per-country content stays client-supplied)

Client request: a 200+-country visa documentation portal with continent hubs, visa-status directories, printable checklists, downloadable document templates, an AI chatbot, a world-map search widget, and thousands of words of unique per-country/per-visa-type SEO content — explicitly benchmarked against VFS Global/Atlys/BlinkVisa/VisaHQ. This was **not built as specified**: almost every substantive piece of content it asked for (visa fees, processing times, document checklists, embassy contact details, validity periods, "top 25 rejection reasons," 2500–4500 words per page × up to 19 categories × 200+ countries) is real, legally consequential, frequently-changing government/consular data that has no source in this project — inventing it would mean the site confidently telling real travellers wrong visa requirements. This is exactly the risk `visa_requirements`/`embassies`/`consulates` have shipped empty to avoid since Phase 6/8, at a much larger scale. Confirmed via `AskUserQuestion` before writing any code: the client will supply real per-country data over time through the admin CMS, and this pass builds architecture only, verified against the current real 208-country/9-visa-type catalog rather than fabricated content. The AI chatbot and world-map/voice-search widget were explicitly scoped out of this pass as separate, large features needing their own scoping.

**What was built, all reusing the existing 208-country/`visa_requirements`/`visa_types` catalog rather than inventing a parallel one:**

- **`database/schema-visa-portal.sql` + `seed-visa-portal.sql`**: a 10th visa type, Dependent Visa (the client's spec listed 10 core categories; only 9 existed), inserted with `country_visa_types` rows generated for all 208 existing countries via `INSERT ... SELECT` rather than 208 hand-written rows; five new boolean columns on `countries` (`is_gcc`, `is_asean`, `is_saarc`, `is_middle_east`, `is_caribbean`) populated directly in the seed file, since GCC/ASEAN/SAARC membership and the commonly-used "Middle East"/"Caribbean" travel groupings are stable, publicly verifiable political/geographic facts — not visa policy, so safe to hardcode (verified against real country lists, not guessed: 6/10/7/14/13 countries respectively, cross-checked against the actual 208-row seed's exact slugs before writing the migration). A sixth new column, `visa_policy_for_indians` (nullable enum: visa_required/visa_free/visa_on_arrival/evisa), is the one genuine visa-policy fact in this batch and ships `NULL` for every country — same discipline as `visa_requirements`, only ever set by a staff member through `/admin/countries/`, never assumed. India itself is deliberately left out of `is_saarc` despite being a real member, since this is an outbound consultancy for Indian travellers and a "visa to India" listing would serve no purpose here.
- **Continent hub pages** (`/visa/asia/`, `/visa/europe/`, `/visa/africa/`, `/visa/north-america/`, `/visa/south-america/`, `/visa/oceania/`, `/visa/middle-east/`, `/visa/caribbean/`) — `visa/continent.php` (new), dispatched from inside `visa/index.php` before the country-slug lookup runs (`CONTINENT_HUBS` in `includes/data.php` checked first — none of the 8 slugs collide with any real country or the existing 6-region taxonomy, so this couldn't shadow an existing page). Middle East and Caribbean are separate flag-based groupings layered on top of the existing 6-region `regions` table rather than new region rows, specifically to avoid touching `/countries/`'s already-shipped region-anchor breakdown (a Middle East country like UAE stays classified as "Asia" for region purposes, flagged `is_middle_east` for this additional grouping).
- **Visa Status Directory** (`/visa-status/` hub + `/visa-status/{visa-free,visa-on-arrival,evisa,schengen,gcc,asean,saarc}/`) — `pages/visa-status.php` (new). The three `visa_policy_for_indians`-backed directories render a real, honest empty state ("Not yet published... we haven't verified and listed any destinations... yet") rather than ever inventing a country's status; the four flag-backed ones (Schengen/GCC/ASEAN/SAARC) show real results immediately since those groupings are already populated.
- **Public visa application tracking** (`/track-visa/`) — `pages/track-visa.php` (new), restoring a feature this project's own task history shows was built once (Phase 11) and then lost during the earlier no-database rebuild, never rebuilt afterward (confirmed via code inspection: no trace existed anywhere before this pass). Copies the exact non-enumerating pattern already proven for `/forex/track/` — reference number + registered mobile must both match, any mismatch shows an identical generic message, and only a customer-safe status/timeline is shown (never internal notes, assigned consultant, or amounts).
- **Real document checklist UI** on the visa detail page (`visa/index.php`) — the "Required Documents" card now splits the existing `documents_required` free-text field into individual checkbox line items plus a Print Checklist button, instead of one paragraph of run-on text. Still entirely driven by admin-entered data — an empty field still falls through to "Not specified," no invented checklist appears for an unverified country.
- **Generic downloadable document templates** (`/document-templates/` hub + 6 formats: Cover Letter, NOC, Consent Letter for Minor, Self-Declaration, Sponsor Letter, Travel Itinerary) — `pages/document-templates.php` (new), real hand-written fill-in-the-blank formats. Unlike `visa_requirements`, this is safe to ship as real content immediately: these are standard boilerplate documents any visa consultancy provides, not a claim about any specific country's requirements — each page says plainly it's a general format, not legal advice, and to confirm exact wording with the specific embassy/consulate.
- **Admin CMS**: `/admin/countries/` gained checkboxes for the five new grouping flags and a "Visa policy for Indian passport holders" dropdown (defaulting to "— Not yet verified —"), plus a Visa Policy column in the list view so staff can see verification status at a glance without opening each country.

**A real, previously undetected bug found while testing the new admin form, affecting the whole CMS — not something this pass introduced**: `/admin/countries/`'s edit form posts to a bare `action="/admin/countries/"` with no query string and no hidden `id` field, while the save handler derives `$id` only from `$_GET['id']`. On every edit submission `$id` was silently `null`, so "editing" a country actually always executed the `INSERT` branch — either a fatal duplicate-key `PDOException` (slug is `UNIQUE`) or, worse, a silent duplicate row. A project-wide check (background agent, read-only) found the identical bug shape in four more files: `admin/pages/visa-types.php`, `admin/pages/embassies.php`, `admin/pages/faqs.php`, and — most seriously — **`admin/pages/customers.php`**, where `customer_reference_no` is freshly generated on every insert, meaning editing an existing customer's details would have silently created a brand-new duplicate customer record rather than updating the original, with no error at all to alert staff. Every other `id`-based admin/HRMS save form was individually checked and confirmed already safe (id embedded directly in the form's `action` URL, or already using the correct hidden-field pattern, or a composite-key/upsert shape not susceptible to this failure mode at all) — the blast radius is exactly these five files. Fixed identically in all five: `$id` now falls back to `$_POST['id']` when no GET query string is present, and each edit form gained `<input type="hidden" name="id" value="...">`, matching the one file (`admin/pages/users.php`) that already had this right. **Verified end-to-end**: submitted the real rendered edit form (not a manually-corrected URL) for Singapore with a real browser-equivalent POST to the bare `/admin/countries/` action — confirmed exactly one Singapore row exists before and after, and the field change actually persisted — proving the fix works via the real form, not just in theory. Given this bug predates this session's changes, **the client's own live database should be checked for duplicate customer/country/visa-type/FAQ/embassy rows created by any past edit attempts** — this pass could only fix the code going forward, not retroactively detect duplicates already created on production before the fix was deployed.

**Verified end-to-end against the real local database**: every new route (`/visa/asia/`, `/visa/middle-east/`, `/visa/caribbean/`, `/visa-status/`, all 7 status directories, `/track-visa/`, `/document-templates/` and 6 detail pages, plus existing `/visa/singapore/`, `/visa/singapore/dependent/`, `/visa/uae/`) returns `200` with a clean server log (no PHP warnings/notices); Middle East hub correctly lists all 14 real countries with real flags, GCC directory correctly lists exactly the 6 real member states; the visa-free directory correctly shows the honest empty state; the document checklist UI was verified with a temporary real `visa_requirements` row (inserted and removed after the screenshot, not left in the database) rendering as real checkboxes with a working Print button; the footer's Visa Services column automatically picked up "Dependent Visa" with no template change needed, confirming it's driven by the live `visa_types` table rather than a hardcoded list. `php -l` clean across every new/modified file.

**Not yet done, by design, per the agreed sequencing**: any actual per-country visa requirement, fee, processing-time, or embassy-address content (all still ship as "pending verification" until a staff member enters real, sourced data); the AI chatbot; the world-map visual search widget with voice input; bulk 200-country content generation of any kind. The reference-number format change the client's original spec requested (`VG-SGP-TV-20260826-000123`) was flagged as a breaking change to an already-live, shared numbering scheme and intentionally not touched pending explicit confirmation.

## Global Visa Documentation Portal — Phase B (site-wide search widget + rule-based Visa Assistant)

Client selected this as the next phase (over HRMS/Forex Phase B) via an explicit prompt: build the search-everywhere and chatbot pieces deferred from Phase A.

**New `/visa/` Main Hub**: `visa/index.php` now renders `visa/hub.php` when no country slug is present (previously redirected to `/countries/`; confirmed via grep that nothing internally linked to bare `/visa/`, so the behavior change was safe). The hub shows Browse by Continent (8 real tiles with live `COUNT()`s via the existing `countries_by_continent_slug()`), Browse by Visa Status (7 tiles via `countries_by_visa_status_slug()`), Popular Destinations (existing `is_popular_destination` flag), Recently Updated (real `visa_requirements` rows ordered by `updated_at DESC`, only rendered when non-empty), and a More Tools row to Document Templates / Track Your Application / Embassy Directory.

**Site-wide instant search widget**: `includes/functions.php` gained `visa_search_widget_items()` (builds one searchable catalog from real data — every country, every visa type, all 8 continent hubs, all 7 status directories, each with its real URL) and `render_visa_search_widget()`, which prints the widget markup plus the catalog as a `<script type="application/json">` tag (`JSON_HEX_TAG | JSON_HEX_AMP` to keep it script-injection-safe). `public/assets/js/visa-search-widget.js` is a vanilla-JS autocomplete: substring match on label, up to 8 grouped results, full keyboard nav, and a `localStorage`-backed "recent searches" list (max 5) shown on focus. Placed on the homepage above the existing detailed search form (left untouched) and on the new `/visa/` hub.

**Voice search**: an optional microphone button using the standard `SpeechRecognition`/`webkitSpeechRecognition` API, hidden via HTML `hidden` attribute and only revealed by JS when the browser actually supports it. Required loosening `includes/security.php`'s `Permissions-Policy` header from `microphone=()` to `microphone=(self)` — first-party use only, no third-party embed can invoke it.

**Visa Assistant — deliberately rule-based, not generative**: `includes/visa-assistant-widget.php` + `public/assets/js/visa-assistant.js` add a floating chat panel (bottom-left, so it never overlaps the existing bottom-right enquiry widget) that does keyword scoring against the real `visa_faqs` table (via a new `all_active_faqs()` helper) and echoes back a matched FAQ's real answer verbatim, or an honest "I don't have a published answer for that yet" plus quick-links to Document Checklists / Track My Application / Embassy Directory / Browse by Continent / Talk to a Human. This was a deliberate scope-down from the client's original "AI chatbot" ask: a true generative assistant could paraphrase or guess at visa fees, documents, or processing times it doesn't actually know — exactly the fabrication risk this entire project has avoided elsewhere by shipping `visa_requirements`/`embassies` empty rather than guessed. A real generative integration is a separate decision requiring the client to pick an LLM provider and supply an API key; not built here.

**New `/embassy-directory/`**: `pages/embassy-directory.php` lists only countries with at least one real row in `embassies`/`consulates`/`visa_application_centres` (via `EXISTS` subqueries); ships the same honest "Not yet published" empty state pattern as everywhere else in the portal, since none of those tables have real data yet.

**A real bug found and fixed during this pass's own verification, not present in any previously-shipped code**: the assistant panel's CSS (`.visa-assistant__panel`) set `display: flex` unconditionally, with no rule for the `[hidden]` state. The HTML `hidden` attribute only works via the browser's user-agent stylesheet (`[hidden] { display: none }`), and *any* author-stylesheet rule that sets `display` on that element overrides the UA stylesheet regardless of selector specificity — so the panel rendered open on every single page load, not just when toggled, visibly covering the search widget and the continent/status tiles beneath it (caught via Playwright screenshot of a fresh `/visa/` load, where the assistant was never programmatically opened). Fixed with one added rule, `.visa-assistant__panel[hidden] { display: none; }`, immediately after the base rule. Re-verified via fresh screenshots: the panel now stays closed until `#visa-assistant-toggle` is clicked, and closes again on the × button.

**Verified end-to-end against the real local database** (Playwright, 1440×1200): `/visa/` hub renders all 8 continent tiles with correct live counts (Asia 51, Europe 48, Africa 54, North America 27, South America 12, Oceania 16, Middle East 14, Caribbean 13) and all 7 status tiles; homepage search widget correctly autocompletes "sing" → Singapore with a Country badge; `/embassy-directory/` correctly shows its honest empty state with working links; the assistant was opened via its real toggle button, asked "processing time", and returned a real, previously-seeded FAQ answer verbatim (not fabricated by this pass) — confirming the keyword-match pipeline works against genuine data, not a hardcoded demo string. `php -l` clean across every new/modified file.

**Not yet done, by design**: a true generative AI chatbot (needs client's LLM provider + API key decision); the world-map visual search widget from the original spec (the list-based autocomplete covers the same "search everywhere instantly" goal without needing map-tile assets or geo-boundary data); any bulk per-country content.

## Full-site audit + /documentation/ — replacing the dead legacy documentation.html

Client asked for a full technical/content/SEO audit of the live site. A background crawl agent hit a hard block: `visagiri.com` is denied by this sandbox's network egress policy (confirmed via the proxy's own status endpoint and a direct `curl` CONNECT test — a 403 on the tunnel, not a site or tool problem). No live page could be fetched. The audit was rebuilt instead from direct code + database inspection (the same codebase and DB this project has built and verified throughout) and delivered as a report artifact, explicitly labeled as not-a-live-crawl. Headline findings: (1) the country/visa-type detail pipeline is fully built and wired but genuinely 0% content-populated — 208 countries × 10 types, 0 with a published requirement, 0 with embassy data, 0 with a visa-policy flag set, all by the same deliberate "ship empty, never fabricate" design as Visa Portal Phase A; (2) `legacy-site/documentation.html` — the exact page the client flagged — is confirmed to be a real leftover file from the pre-rebuild Bootstrap template (Tawk.to widget, dead links to `business.html`/`crew.html`/`country.html` and 32 more siblings), physically excluded from every packaging script (`bin/package-cpanel.sh`) and therefore never touched by this project, meaning it's very likely still reachable on the live server since nothing ever deleted the original.

Client selected the documentation page as the first fix. Since there was no `/documentation/` route in the app at all (not a redesign of an existing PHP page — a genuine gap), built one from scratch: `pages/documentation.php`, routed at `case 'documentation'` in `public/index.php`. Content follows the client's requested section list (why documentation matters, passport/photograph/financial/employment/invitation/cover-letter/itinerary/hotel/flight/insurance/sponsorship/minor/business/student/work-visa documents, plus document authentication) as real, general, reusable document-preparation guidance — the same "true but not destination-specific" standard already established by `document-templates.php`, not a claim about any one country's requirements. Document Translation and Notarization are described only as concepts an applicant may encounter, explicitly not as Visagiri-offered services, since neither is a confirmed active offering (unlike Apostille/Attestation/Legalization, which are real and linked to `/attestation/` rather than duplicated). The client's "modern and technology driven" framing is delivered as a 4-item feature grid describing only real, already-shipped platform mechanics — reference-number tracking (`/track-visa/`), access-controlled off-web-root document storage with staff-only upload (`includes/documents.php`), encrypted PII fields, and audit-logged handling (`includes/audit.php`) — deliberately not claiming a public self-upload portal or AI verification, neither of which exist in this project. CTA ("Check My Document Requirements") links to the real `/enquire/` CRM form; a WhatsApp CTA uses the existing `whatsapp_enquiry_href()` helper. FAQ section reuses real general FAQs (`faqs_general()`), not invented ones.

Linked from three places, all real existing pages: the footer's Resources column, a new fourth card on `/visa/`'s "More Tools" row, and a contextual line added to `/document-templates/`'s intro pointing back the other way.

**Verified end-to-end against the real local database**: `/documentation/` returns 200 with a clean server log; 16 document-category cards and 4 platform-feature cards render (confirmed by counting the `service-card`/`feature-card` class occurrences in the actual response body, not assumed); the FAQ accordion renders with real general FAQs; both CTAs point to real, working destinations; the new footer link, hub card, and document-templates cross-link all confirmed present in their respective pages' real rendered output. `php -l` clean across every new/modified file. Screenshot-verified full page render at 1440px.

**Not yet done**: confirming whether `legacy-site/documentation.html` and its 34 siblings are still live on production and removing them if so — this needs the client to check directly, since this sandbox cannot reach the live site (see above). The rest of the original 40-section brief (per-country content population, visual/photography redesign, CRM UTM wiring, accessibility/performance passes, the other 34 legacy-file cleanups) remains unscoped pending the client's review of the audit report.

## Premium header polish + redesigned admin login

Client asked for a more "modern premium" header and a redesigned login panel (admin — HRMS login left untouched, per client's own scope choice), plus reported the Countries mega-menu search and the homepage hero search widget "not working." Reproduced both reported bugs first, via real Playwright interaction (typed into each search box, checked actual result counts) rather than reading code and guessing: both work correctly against this codebase and local database — homepage widget returns Singapore for "sing", the mega-menu search filters correctly, `/countries/`'s own search filters correctly, zero console errors across all three. Since this sandbox still can't reach `visagiri.com` (same network egress block as the earlier audit), the live-site symptom is most likely a stale/partially-applied delivery zip or browser cache rather than a defect in this code — flagged back to the client with a request for a live screenshot + console error if it persists after this delivery.

**Header** (`public/assets/css/layout.css`): replaced the flat hairline border-bottom with a real shadow at rest and a frosted-glass state on scroll (`backdrop-filter: blur(14px) saturate(160%)` behind `@supports`, with a plain translucent fallback for browsers without it); added a gold underline that grows in from center on nav-link hover/active, replacing the old flat color-only swap; every mega-menu panel now carries a thin gold top rule and a slightly deeper, layered shadow; mega-menu item hover gained a gold left-accent bar instead of a flat background swap. No markup or JS changed — pure CSS, so zero behavioral risk to the mega-menu's existing hover/focus/Escape logic.

**Admin login** (`admin/pages/login.php` + `public/assets/css/admin.css`): replaced the plain centered card on a flat navy background with a two-panel layout — a navy-gradient brand panel (logo, tagline, an inline SVG passport/checkmark motif matching the existing "original illustration, not stock photography" convention used elsewhere on the site, and an honest note: "Internal staff access only. Every sign-in and change is logged" — true, since `log_action('login', ...)` already fires on every login and the wider CRM audit-logs every mutation) alongside a clean form panel with icon-adorned username/password fields and a lifted, shadowed submit button. Stacks to a single column under 860px. No change to the auth logic itself (rate limiting, timing-safe password check, CSRF) — only the markup/CSS around the existing form, same `action="/admin/login/"` and hidden CSRF field.

**Verified end-to-end**: real Playwright login using the actual admin credentials through the redesigned form — submits, passes CSRF + rate-limit + password checks unchanged, redirects to `/admin/dashboard/` exactly as before. Screenshot-verified header at rest, scrolled (frosted state), and with the Countries mega-menu open, plus admin login at 1440px and 390px. Zero console errors across every check. `php -l` clean.

**Not done**: HRMS login redesign (client scoped this pass to admin only); the two "not working" search reports remain open pending a live reproduction, since this sandbox has no path to the live site to confirm the actual cause.

## Customer accounts + B2B Partner portal restored; header rebuilt as a travel-tech utility bar

Three separate client requests landed close together and are covered in this entry: (1) "Customers Login Panel, Partners Login & Employee Login" in the header, (2) social media links moved to the footer, (3) a full header redesign spec (tagline, live currency rate, 24×7/WhatsApp, a Login dropdown replacing Enquire Now).

**Customer accounts, rebuilt onto the CRM, not a parallel identity system.** Earlier in this project's history a customer self-service login/dashboard existed, then was removed during the "single-folder no-database rebuild." Recovering it wholesale from git history wasn't right — that old code used a generic `users`+`roles` table this project no longer has. Instead: added auth columns directly to the live `customers` table (`schema-customer-partner-portal.sql`, fully additive), and register/login/logout/forgot-password/reset-password + a full dashboard (applications, documents with real customer self-upload — a genuinely new capability, since no public upload path existed before — two-way messages, appointments, payments, notifications, profile). Registering with an email that already has a CRM record (from a past enquiry) claims that record by setting its password rather than creating a duplicate, exactly the same discipline as the admin-CMS duplicate-record bug fixed earlier in this project. Remember-me uses a real persistent DB token, not just an extended session cookie — the latter silently stops working once PHP's session garbage collector reaps the underlying session file, well before the claimed 30 days.

**B2B Partner portal, built from zero — no partner-related data existed anywhere before this.** `/partner/` mirrors the customer auth pattern with its own session key and remember-me token so none of admin/customer/partner sessions can ever collide. Self-registration lands in `pending` status; a partner can log in and see their own pending state, but referral/commission data only unlocks once staff approve them (new `admin/pages/partners.php`). Each active partner gets a shareable referral link (`/register/?ref=<reference>`) that attributes a new customer signup via `customers.referred_by_partner_id` — only if that partner is currently active, so a pending or suspended partner's link can't backdate attribution once approved. Commissions (`partner_commissions`) ship `amount_due` as `NULL` until staff set a real figure on the application detail page — same "ships empty until real" discipline as `visa_requirements`/embassies elsewhere in this project, not a fabricated number. Building the commission UI also meant adding admin-side appointment scheduling (`customer_appointments`) and a "visible to customer" checkbox on application notes (turning the existing internal-notes table into a real two-way message thread) — both were already built on the customer-dashboard side from the first phase of this work but had no admin-side way to actually populate them until now.

**Header rebuilt around a travel-tech utility bar**, per a separate, detailed client spec. A new thin bar above the main nav (collapses away entirely on scroll, keeping the sticky state slim) holds a USD→INR "indicative rate" pill, 24×7 Support (`tel:`, using the existing `contact_phone_dial` setting), and WhatsApp (existing `whatsapp_enquiry_href()` helper, existing number — nothing new invented). The logo's subtitle became the client's tagline, "Technology Meets Travel & Visa." The old flat "Enquire Now" CTA is gone from the header entirely, replaced by a Login dropdown (Customer / Employee / B2B Partner), per the spec's own explicit structure — Employee maps to the existing `/hrms/` login, the closest real analog for that label.

**The live rate is real, not simulated** (`includes/currency-rate.php`): fetches USD→INR from Frankfurter (ECB reference rates, no API key — nothing to leak, so the "keep credentials server-side" requirement is satisfied by having no credential at all), cached to a local file with a 1-hour TTL so ordinary traffic never re-hits the upstream per request. A stale cached value is served (clearly labeled "Last known rate") if the upstream is unreachable; if there's never been a successful fetch, the widget honestly shows "Rate unavailable" rather than a fabricated number. A small client-side poll (every 5 minutes) refreshes the same number for a visitor whose tab stays open, without a full reload. **This sandbox's own network egress is blocked for arbitrary external domains** (the same restriction documented in the earlier site-audit entry, confirmed again here against Frankfurter directly) — meaning the "unavailable" fallback path is what's actually been exercised end-to-end from here, not the live-fetch success path. That's a limitation of this dev sandbox, not of the deployed app: the client's actual cPanel host is a separate, normal internet-connected server and should reach the API fine — worth the client confirming once deployed.

**Verified end-to-end**: full customer register → dashboard → all 7 sub-pages → logout → re-login, then re-tested against real seeded application/document/payment/appointment/message data (not just empty states) and cleaned up afterward. Full partner flow: register → pending screen → admin approve → referral link → a second customer registers through that link → `referred_by_partner_id` set correctly → admin schedules an appointment, sends a customer-visible note, sets a commission → partner dashboard shows the correct referral and commission figures. Header: Login dropdown opens/closes correctly via click, outside-click, and Escape (checked programmatically, not just visually); all three login routes return 200; `tel:`/WhatsApp hrefs verified against the real configured number; scrolled state correctly collapses the utility bar; mobile menu carries the same rate/support/WhatsApp/login content. One real bug caught and fixed during this pass's own verification: the dashboard/partner shells' `render_*_end()` functions were calling `includes/footer.php`, which reads variables set inside `includes/header.php`'s own function call — a separate function scope, so PHP's `require()` didn't inherit them, causing a fatal error on every dashboard and partner page. Fixed with a minimal closing shell instead of the full marketing footer, matching what the original pre-removal customer dashboard already did for the same reason.

**Not done**: the "Get Assistance" quick-enquiry dialogue and floating contact widget from an earlier, still-separate request — deliberately not touched in this pass since it's a distinct piece of work; the header CTA area it would have lived in is now the Login dropdown instead, so that widget (when built) becomes the primary quick-enquiry entry point.

## Header follow-up: tagline and Login moved into the utility bar, WhatsApp merged into 24×7 Support

Client follow-up on the header spec above, refining element placement rather than changing scope: keep the "VISAGIRI / Visa Management" logo untouched (the tagline swap onto the logo subtitle from the previous pass is reverted), move the tagline text itself up into the utility bar's left side, move the Login dropdown up into the utility bar too (out of the main nav row, which now holds only the search icon), remove the standalone WhatsApp link, and make 24×7 Support itself open WhatsApp instead of dialing.

`includes/header.php`: logo subtitle reverted to `Visa Management`. Utility bar left slot now shows the tagline ("Technology Meets Travel & Visa"); its right slot holds the rate pill, a single "24×7 Support" link, and the Login dropdown trigger/menu (Customer / Employee / B2B Partner — unchanged targets). The old separate `tel:`/WhatsApp pair is gone — "24×7 Support" now points at `whatsapp_enquiry_href()`, the same helper already used site-wide, so the only channel offered is the one actually staffed. Main nav's `.site-header__actions` is now just the countries search icon. Mobile menu mirrors the same structure: tagline line, one WhatsApp-linked "24×7 Support" button, then the Login section with all three links.

`public/assets/css/layout.css`: `.site-header__login-trigger` got button-reset styling since it now sits inline in the utility bar's link row rather than as a standalone nav dropdown trigger; added `.site-header__tagline` and `.site-header__mobile-tagline` rules. No JS changes — the existing login-dropdown open/close/outside-click/Escape handler and the rate-refresh poll are untouched, since only their container moved.

**Verified end-to-end** (Playwright, 1440×900 and 390×844): 24×7 Support's href resolves to the correct `wa.me` URL with the pre-filled enquiry message; zero remaining "WhatsApp"-labeled elements anywhere in the utility bar; Login dropdown opens/closes correctly from its new position with the chevron rotating as before; logo subtitle confirmed back to "VISA MANAGEMENT"; mobile menu screenshot confirms the tagline, single WhatsApp-linked 24×7 Support button, and Login section render correctly with no duplicate WhatsApp entry. Zero console errors. `php -l` clean on both changed files.

## Header follow-up: "Enquire Now" restored on the right

Client asked to add "Enquire Now" back into the header's right side after the utility-bar rework replaced the old CTA slot with the Login dropdown. Added as a gold `btn-gold btn-sm` link to the existing `/enquire/` CRM form, placed in `.site-header__actions` next to the search icon (both already flex/gapped, so no new CSS was needed). Mobile menu gets the same link as an outline button directly under 24×7 Support. No JS or CSS changes — reuses existing button classes and the existing route.

**Verified**: Playwright confirms the button renders once, links to `/enquire/`, and the mobile equivalent does too; zero console errors; `php -l` clean.

## Floating widgets consolidated to one; live-site "not working" reports investigated

Client reported four issues from live-site screenshots after applying the header package: (1) the Login dropdown didn't open on click, (2) the homepage search widget didn't show suggestions, (3) a visible white gap under the footer before the floating buttons, (4) two separate floating chat buttons — asked to remove one and keep floating buttons for WhatsApp / Email / Enquire only.

**Investigated (1)-(3) against this exact codebase, not just re-read the code**: real Playwright run confirms the Login dropdown opens/closes correctly on click, the hero search widget returns a live "Singapore" suggestion for "sing", and a full-page screenshot shows the footer ending exactly at the document's actual bottom (`scrollHeight` and the footer's own bottom edge match to the pixel) — no structural gap exists in the markup or CSS. Since the delivered header markup in the client's screenshot is confirmed to be the *new* version (tagline/rate/Login all present in the right place) but the interactive behavior isn't, the most likely explanation is that `assets/js/site.js` and/or `assets/js/visa-search-widget.js` on the live server are still serving an older cached copy — Phase 17 added long cache lifetimes for static assets specifically for performance, which means a same-named JS file only updates for a visitor once the browser's cache actually expires or is bypassed. Flagged back to the client to verify: confirm both JS files' upload timestamp/size on the server match this delivery, and hard-refresh (or test in a private window) before concluding it's still broken — this is a deployment/caching check, not a code defect reproducible here.

**(4) implemented as asked.** Removed the separate rule-based "Visa Assistant" FAQ chatbot entirely — `includes/visa-assistant-widget.php` and `public/assets/js/visa-assistant.js` deleted, its `.visa-assistant*` CSS block removed from `components.css`, its `<script>` tag and `require` dropped from `includes/footer.php`, and the now-unused `all_active_faqs()` helper removed from `includes/data.php` (it had no other caller). The remaining floating widget (`includes/enquiry-widget.php`, bottom-right) now expands to exactly three actions — WhatsApp (unchanged, real number via `whatsapp_enquiry_href()`), Email (unchanged, `mailto:` to the real configured address), and a new Enquire action linking to the same real `/enquire/` CRM form as the header's Enquire Now button — replacing the old Call/`tel:` action, which is gone now that the header's own 24×7 Support is also WhatsApp-based.

**Verified**: Playwright confirms zero `.visa-assistant` elements remain, exactly one `.enquiry-widget` element exists, and its three expanded actions resolve to the correct WhatsApp/mailto/`/enquire/` targets; zero console errors. `php -l` clean on every changed file; no other file in the codebase still references the removed widget or helper (confirmed by a full-repo grep).

## Real fix for the Login dropdown: an overflow-clipping bug, not a caching issue

Client pushed back that the Login dropdown was still not opening after the previous delivery, with a screenshot also showing the Countries mega-menu's search filtering correctly for "Singapore" — which ruled out the "stale cached JS" theory from the previous entry: if the JS were genuinely stale/missing, the *other* JS-driven interaction (mega-menu search) wouldn't have worked either, but it clearly did in their own screenshot.

Re-tested the Login dropdown locally with a stricter check than before — not just whether the `is-open` class gets applied (it did, every time), but whether the menu is actually *visible in a rendered screenshot*. It wasn't: `.site-header__utility` (the bar the Login trigger now lives in, after moving it there per an earlier request) has `overflow: hidden`, added specifically so the utility bar's collapse-on-scroll animation clips its shrinking content cleanly. That same `overflow: hidden` was also clipping the Login dropdown's popup menu, which is a `position: absolute` element meant to extend below the utility bar's own 40px height — so the menu was toggling open correctly in the DOM (chevron flips, `aria-expanded` flips) while rendering completely invisible, clipped by its own ancestor. A real, reproducible layout bug, not a deployment/cache issue — my earlier caching explanation was wrong.

**Fix**: moved `overflow: hidden` off the bar's resting state and onto only its `.is-scrolled` (collapsed) state in `public/assets/css/layout.css`, so the bar still clips cleanly while animating shut on scroll, but no longer clips its own dropdown while sitting open at the top of the page.

**Also checked, not a bug**: the mega-menu panel's edges showing hero-section content behind it (visible in the client's screenshot) is the mega-menu's normal, working overlay behavior — it's a centered floating panel, not a full-screen modal, so page content is visible around it by design; reproduced the exact same look locally with the same "Singapore" filter working correctly. If the client would prefer a dimmed backdrop behind open mega-menus/dropdowns for a more "modal" feel, that's a small follow-up, not a bug fix — flagged here rather than built without being asked.

**Verified**: Playwright screenshot (not just DOM class checks) confirms all three Login links (Customer/Employee/B2B Partner) now render fully visible and clickable on open; outside-click still closes it; scroll-collapse animation still clips correctly when scrolled (utility bar's `max-height` reaches `0px` as before). Zero console errors.

## The real "Countries search not working" bug: click was navigating away, not opening the panel

Client reported the Countries search widget "not working." Reproduced the exact interaction a real desktop mouse user would make — clicking the "Countries" nav item rather than hovering over it first — and it navigated straight to `/countries/`, never opening the mega-menu at all. The dropdown's own search box was never reachable by click; only hovering (holding the mouse still over the link before clicking) opened it, because `site.js`'s click handler only intercepted navigation on touch devices (`matchMedia('(hover: none)')`), deliberately leaving mouse clicks to behave as a normal link on the theory that desktop users would hover first. In practice, most people click nav items rather than pausing to hover, so this sent them straight past the feature.

**Fix** (`public/assets/js/site.js`, `initMegaMenu()`): the click handler now always calls `preventDefault()` and toggles the panel open/closed, on every device, not just touch. This applies uniformly to all three mega-menus (Attestation, Countries, Company) since they share the same handler and had the same underlying issue. Users can still reach the plain destination pages via the real links inside each panel (e.g. "Explore All Countries", "View Complete Country Directory", the Attestation CTA) — the top-level word itself now behaves as a disclosure trigger rather than double-duty as a direct link, which is the standard pattern for this kind of mega-menu.

**Verified**: a plain `page.click()` (no hover) on each of the three mega-menu triggers now opens its panel and keeps the user on the same page; typing "singapore" into the Countries panel's own search box correctly filters to Singapore; clicking the trigger a second time closes it again; zero console errors. This was the actual cause behind the "search not working" report — not caching, not the earlier overflow-clipping bug (that was a separate, real issue with the Login dropdown specifically, already fixed).

## Admin credentials regenerated (client requested, plaintext not recoverable from the stored hash)

Client asked for the admin credentials. The only record of the previous plaintext password lived in chat at the time it was originally generated, per this project's own rule (`database/seed.sql`'s own comment: "a password should never live in version control") — bcrypt hashes are one-way, so there was no way to recover or "share" the old password from the hash stored in the repo. Generated two brand-new random 14-character passwords instead (one for `admin`, one for `hr.admin`), hashed them with PHP's `password_hash()` (same function the app's own registration/reset code already uses), and updated `database/seed.sql` / `database/seed-hrms.sql` with the new hashes — the plaintext itself is not committed anywhere, matching the existing convention.

**Verified end-to-end, not just an offline `password_verify()` check**: logged in through the real `/admin/login/` form with each new username/password pair against the local dev database, through the actual rate-limiting/CSRF/session code path, landing on `/admin/dashboard/` for both. Client was given the new plaintext credentials directly in chat, plus a `UPDATE admin_users SET password_hash = ...` SQL statement to run against their live database via phpMyAdmin, since editing this repo's seed file has no effect on an already-deployed database — only a fresh import or a manual UPDATE does.

## B2B Partner CRM — Phase 1: RBAC foundation + real admin Partner CRM

Client sent a 50-section spec asking to expand the existing minimal B2B partner referral system into a full Partner Enrollment + CRM platform (OTP registration, document management, quotations, invoicing, wallet/credit, team sub-users, tiered pricing, 7-role RBAC, communication centre, reporting, SEO landing page). The spec's own §49 asked for an audit + phased proposal before writing code — this session did exactly that: two research passes read the real codebase (not assumptions) to map what already existed, confirmed several pieces the spec assumes exist do not (no OTP/SMS provider, no WhatsApp Business API beyond a `wa.me` deep link, no payment gateway, no charting library, no PDF/Excel export, no outbound email at all — not even for password reset, which is dev-mode-only today — and this sandbox has no network access to test any external API regardless), then produced a 10-phase roadmap. Given the size, the client chose to build **Phase 1 only** in this pass: RBAC foundation + a real, paginated admin Partner CRM, replacing the old bare-bones one. Phases 2–10 (enrollment wizard, approvals, quotations, wallet, team management, reporting, SEO page, deferred third-party integrations) are documented in the plan but not started.

**Database** (`database/schema-b2b.sql`, additive): `partners.assigned_admin_id` (nullable FK → `admin_users`, the Relationship Manager assignment — ships NULL until staff assign one, same "ships empty until real" convention as `partner_commissions.amount_due`); `audit_logs.partner_id` (nullable FK → `partners` — `admin_user_id` on that table was already nullable, confirmed by reading the live schema before adding this, so it's a small, consistent extension of an existing shape, not a new pattern); new `partner_status_history` table (partner_id, from_status, to_status, changed_by, remarks, created_at), modeled directly on `forex_status_history`. `partners.status` deliberately stays the current 3-value ENUM (`pending`/`active`/`suspended`) — the spec's 9-state workflow is driven by a multi-step registration wizard and approval flow that don't exist yet; widening the ENUM now would add states nothing could reach.

**RBAC** (`database/seed-b2b.sql`): a dedicated `partners.*` permission namespace (`partners.view`, `partners.view_all`, `partners.manage`) replacing the old piggyback on `customers.view`/`customers.manage`. Six new roles, matching the spec's staff structure: `b2b-admin` (full visibility + manage), `b2b-relationship-manager` (scoped to only their assigned partners — no `partners.view_all`), and `b2b-visa-manager`/`b2b-accounts-manager`/`b2b-sales-manager`/`b2b-support-executive` (all full-visibility read-only, since none of them are the "assigned to me only" role — that's specifically the Relationship Manager). `includes/rbac.php` gained `current_admin_b2b_scoped_to_assigned()`, mirroring the existing `current_admin_forex_scoped_to_assigned()` pattern.

**Admin UI**: `admin/pages/partners.php` fully rebuilt on `admin/pages/customers.php`'s pattern (the old version had no pagination at all). Real search (company/contact/email/reference/mobile) + status filter + two-query pagination; profile page follows this codebase's established "one long page, stacked `<h2>` sections" convention rather than introducing tabs — there is zero tab-UI precedent anywhere in this app (confirmed by direct grep before deciding), so tabs weren't added just for this one page despite the spec's wording. Sections: Overview (with Relationship Manager assignment), Referred Customers & Applications, Commissions (both existing, unchanged logic), and the new Status History. No Documents/Quotations/Invoices/Wallet/Team sections — those tables don't exist until later phases, and this project's own convention is to never show empty/fabricated feature sections. Approve/Suspend/Reactivate now write to `partner_status_history` in addition to the existing audit log, and a new Assign/Reassign Relationship Manager action was added.

**Shared helpers introduced**: `status_badge(string $status, array $map): string` in `includes/functions.php`, replacing the inline-ternary badge pattern repeated at nearly every admin list/detail page (confirmed no shared helper existed despite dozens of call sites) — used on the new partners page, not retrofitted everywhere else in this pass. `log_action()` in `includes/audit.php` gained an optional `?int $partnerActorId` parameter (mirrors the existing `$actorId`) — unused by any action in this phase (everything here is admin-driven), but exists now so a later phase where a partner acts on their own account doesn't need to touch this function's signature again.

**A real bug caught during verification, not present in the final code**: an early draft of the approve/suspend/reactivate status-transition helper passed a fixed `['admin' => ..., 'id' => ...]` params array to every UPDATE statement regardless of whether that specific query actually used a `:admin` placeholder. This project's PDO connection uses non-emulated prepares, which reject `execute()` calls containing a named key with no matching placeholder in the query (`SQLSTATE[HY093]: Invalid parameter number`) — confirmed by a quick standalone reproduction against the real connection before it ever shipped. Fixed by having each caller supply exactly the params its own UPDATE SQL needs.

**Verified end-to-end** (Playwright, real accounts seeded via the actual `password_hash()`/role-assignment path, not fixtures): a `b2b-admin` test account sees all seeded test partners, search and status-filter both correctly narrow the list, approving a pending partner writes a real `partner_status_history` row (`pending → active`) and audit log entry, and assigning a Relationship Manager persists and displays correctly. A `b2b-relationship-manager` test account sees only partners assigned to it (confirmed both via the list and via a direct-URL access attempt on an unassigned partner, which correctly returns "not found"), and sees zero Approve/Suspend/Set-Manager buttons anywhere (no `partners.manage`). A third test account holding only the old `visa-consultant` role (which still has `customers.view`) now gets a real `403` on `/admin/partners/` and no longer sees the Partners link in the sidebar at all — confirming the old `customers.*` piggyback is genuinely gone, not just hidden. `php -l` clean across every changed/new file. All test accounts and partner rows removed from the local dev database after verification.

**Not done in this pass** (see the 10-phase roadmap for the agreed order): multi-step enrollment wizard + email verification + document upload, admin approval workflow beyond the existing 3 actions, partner-submitted visa applications, quotations/tiered pricing, wallet/invoicing, team sub-users, communication centre + real notification bell, CSV export + document-expiry alerts, public SEO landing page, and everything blocked on client-supplied third-party credentials (real mobile OTP/SMS, WhatsApp Business API, Google OAuth, payment gateway, virus scanning, transactional email/SMTP — the last of which several later phases will need resolved early, since almost nothing in this spec works end-to-end without it).

## B2B Partner CRM — Phase 2: multi-step enrollment wizard + real email sending

Second of the 10-phase B2B roadmap (see the Phase 1 entry above). Client confirmed they'd supply real SMTP credentials, so this phase builds the full 5-step enrollment wizard end-to-end — including real transactional email — rather than leaving email as an open question.

**Email — a real hand-rolled SMTP client, not a stub.** This codebase has zero external dependencies (no Composer, no vendor/ directory anywhere), so `includes/mail.php` speaks just enough SMTP by hand (EHLO, STARTTLS, AUTH LOGIN, MAIL FROM/RCPT TO/DATA, RFC 5321 dot-stuffing, RFC 2047 header encoding for non-ASCII subjects) to work against a standard cPanel mailbox or any relay (SendGrid/SES/Postmark all speak the same protocol) — matching the same zero-dependency discipline as `includes/encryption.php`. Supports an unauthenticated local relay too (empty username/password), the common case for cPanel's own local mail server. Configuration lives in `config/smtp.php` (gitignored, template at `config/smtp.example.php`), same pattern as `config/database.php`/`config/encryption.php`. `send_mail()` never throws — no config, a connection failure, or a rejected recipient all just return `false`, and every caller already degrades to the existing dev-mode "show the link on screen" behavior, so nothing regresses before the client's credentials are in place.

**Verified against a real SMTP conversation, not just read for correctness**: built a throwaway fake SMTP server (`stream_socket_server`, logs every line) and ran `smtp_send()` against it for five real scenarios — full AUTH LOGIN happy path, unauthenticated relay, a rejected recipient, a failed auth, and no host configured — confirming the exact byte-for-byte command sequence (including that a body line starting with "." gets correctly doubled per RFC 5321) and the correct true/false return in each case. This sandbox still can't reach any real external SMTP server (confirmed earlier in this project's history and true again here), so the live-send-to-a-real-inbox path remains untested from here — that's on the client to confirm once real credentials are in `config/smtp.php`. The existing customer and partner "forgot password" flows were also wired onto this same helper while it was being built, since they already had the exact matching dev-mode fallback to preserve.

**The wizard** (`partner/pages/register.php` rewritten as step 1, five new pages, one new helper file `includes/partner-enrollment.php`): Business Info → Contact & Login → Business Profile → Documents → Terms, matching the client's spec order. Key design decisions, each made deliberately rather than following the spec literally:
- **Account creation happens at step 2** (contact + password), not step 5. Steps 1's business-info fields are held in `$_SESSION` only until then — nothing is written to the database for a visitor who abandons before finishing step 2. From step 2 onward the partner is a real, logged-in (if incomplete) account, so document uploads and later steps operate on real rows instead of trying to hold files in session state indefinitely.
- **`partners.status` was deliberately NOT widened** to the spec's 9-state workflow this phase — the wizard produces exactly the same `'pending'` status Phase 1's approve/suspend/reactivate actions already handle. A new `enrollment_completed_at` column (set only at step 5) distinguishes "fully submitted" from "account exists but wizard abandoned partway through," without adding workflow states nothing can reach yet.
- **OTP → email verification link.** No SMS/OTP provider exists or can be tested from this sandbox (confirmed again this phase — no outbound network access to any external host). The client's "do not activate the partner account until verification is completed" requirement is honored literally: `admin/pages/partners.php`'s Approve action now checks `email_verified_at` server-side (not just hiding the button) and refuses to approve an unverified partner — confirmed by a real POST bypass attempt against the endpoint directly, not just clicking through the UI.
- **IATA certificate is the only hard-required document**, exactly matching the spec's own conditional wording ("If Yes: IATA Certificate Upload") — every other document type is optional at registration; verification actions (approve/reject a document) are Phase 3, not this one, so every upload ships `verification_status = 'pending'` and stays there.
- **Duplicate prevention** (spec §27) checks company name/GSTIN/PAN/IATA number at step 1 (before an account exists) and email/mobile at step 2, using one shared `find_partner_duplicate()` — returns the client's exact requested message.
- **Terms & Declaration is honest about what's real**: only Terms & Conditions and Privacy Policy link to actual published pages (`/terms/`, `/privacy/`); the three B2B-specific policies (Partner Agreement, Commission Terms, Document Handling) don't have drafted legal text yet, so the checkboxes say so plainly rather than linking to or fabricating contract language — this project's "never fabricate" discipline extended to legal copy specifically.

**Admin side**: the partner profile page (`admin/pages/partners.php`) gained real Business Profile and Documents sections (reading `partner_business_profiles`/`partner_documents`, both new this phase), a new `admin/pages/partner-document-download.php` (same auth/scoping pattern as the existing `document-download.php`), and Email Verified / Enrollment status shown plainly on every partner's overview. The admin partner list shows an "Incomplete" badge for any pending partner who hasn't finished the wizard yet.

**Verified end-to-end via Playwright** — two full runs, cleaned up after: (1) the entire public wizard, all 5 steps plus the confirmation page, including the IATA-required block (tried skipping it, got blocked; uploaded a real PDF, got through), the JS-driven submit-button gate on the terms step (confirmed it starts disabled, confirmed the server-side check still holds if JS somehow didn't run), the dev-mode verification link, re-login correctly skipping straight to the dashboard once enrollment is complete instead of re-entering the wizard, the dashboard's unverified-email banner appearing then disappearing after clicking the real verification link, and duplicate-company-name prevention — 22/22 checks passed, zero console errors; (2) the admin side against that same real test partner: Business Profile and Documents sections render the actual submitted data, the document download link serves the real uploaded PDF, and the Approve action correctly transitions `pending → active` with a `partner_status_history` row — 16/17 automated checks passed (the one "failure" was an overly strict test regex; the underlying database state confirmed the real transition happened correctly). A separate unverified test partner confirmed the approve gate: disabled in the UI, and a direct POST bypass attempt against the endpoint was rejected server-side with the database left at `pending` — not just a hidden button. `php -l` clean across every changed/new file.

**Not done this pass** (Phase 3 territory): document verify/reject actions, `partner_notifications`, the admin approval workflow beyond approve/suspend/reactivate. Real mobile OTP/SMS, WhatsApp Business API, and payment gateway integration remain explicitly deferred per the original roadmap — none of those changed this phase.

## B2B Partner CRM — Phase 3: admin approval/document-verification workflow + partner notifications

Third of the 10-phase B2B roadmap (see Phase 1/2 entries above). Builds the fuller approval workflow the previous two phases left as approve/suspend/reactivate only, plus the badge-only in-app notification feed the roadmap scoped for this phase (the fuller communication centre is a later phase, not this one).

**Database** (`database/schema-b2b-phase3.sql`, additive): `partners.status` widened from the 3-value ENUM Phases 1–2 deliberately left untouched to `pending`/`documents_required`/`active`/`suspended`/`rejected`/`deactivated` — this phase is specifically the one building admin UI that can reach the three new states, so the deferred widening from Phase 1's own notes finally happens here, not speculatively ahead of it. New `partner_notifications` table, mirroring `customer_notifications`'s exact shape (`partner_id, type, title, body, link, is_read, created_at`) so it can reuse the same rendering pattern as the existing customer notification bell.

**`notify_partner()`** (`includes/partner-enrollment.php`): a single helper that inserts a `partner_notifications` row — badge-only, no email, matching the roadmap's own "(badge-only for now)" scoping for this phase. Called from every status transition and every document verify/reject action below.

**Admin approval workflow** (`admin/pages/partners.php`, full rewrite of the action-button block): per-status action sets — `pending`/`documents_required` → Approve (still gated on `email_verified_at`, unchanged from Phase 2), Request More Info → `documents_required`, Reject → `rejected`; `active` → Suspend, Deactivate; `suspended` → Reactivate, Deactivate; `rejected`/`deactivated` are terminal (no actions, shown plainly rather than an empty button row). Reject/Request More Info/Deactivate all require a non-empty reason — re-checked server-side (a blocked attempt with an empty reason redirects back with a flash error and leaves the database untouched, confirmed by direct query, not just by not seeing a UI change) rather than trusting a client-side `required` attribute as the real gate, consistent with this project's established pattern for every other business-rule boundary (email-verification gate, delivery-compliance gate in the forex module, etc.). Every transition now also writes its reason into `partner_status_history.remarks` (a column that existed unused since Phase 1) and calls `notify_partner()`. The Status History section on the partner profile page now shows the reason alongside each transition.

**Document verification**: the Documents section's old "verification actions aren't available yet" placeholder is replaced with real Verify/Reject controls per document (inline form, optional remarks field), mirroring the existing pattern already used in the forex module's own document-verification UI (`admin/pages/forex-requests.php`) rather than inventing a new one. Verifying/rejecting updates `partner_documents.verification_status`/`verification_remarks`/`verified_by`/`verified_at` (all columns that existed unused since Phase 2) and fires a `notify_partner()` call. Gated on the existing `partners.manage` permission — no new granular permission introduced this phase, avoiding speculative RBAC proliferation for a single new action.

**Partner-facing notifications**: `partner/pages/notifications.php` (new) is a direct clone of the customer portal's own `dashboard/pages/notifications.php` — same list-with-Mark-All-Read pattern, just reading `partner_notifications` instead of `customer_notifications`. `includes/partner-layout.php` gained a `notifications` nav entry with the same unread-count red badge as the customer dashboard's own nav (`includes/dashboard-layout.php`), copied byte-for-byte in structure since this codebase had exactly one existing precedent for that UI pattern and there was no reason to invent a second one.

**Partner dashboard reason display**: the `!is_partner_active()` branch in `partner/pages/dashboard.php` now branches per status — `documents_required`/`rejected`/`deactivated`/`suspended` each show the latest non-null `partner_status_history.remarks` for that status (a small query scoped to `partner_id` + current `status`, so a partner only ever sees the most recent reason for their *current* state, not their entire history) — rather than the previous one-size-fits-all "pending or suspended" messaging.

**Verified end-to-end via Playwright** (28/28 checks passed, zero failures on the second run — the first run's two "failures" were the test script itself following a stale assumption about a redirect target, not application bugs, corrected before the final run): registered two fresh test partners through the real wizard (one plain, one IATA-registered with a real uploaded PDF); confirmed the reject/request-more-info/deactivate reason requirement is enforced server-side (empty-reason attempt blocked, database confirmed still at the prior status); walked one partner through the full `pending → documents_required → active → suspended → active → deactivated` lifecycle, confirming the correct action buttons appear at each state, the terminal-state message appears once deactivated, and each transition's reason shows up both in the admin Status History table and on the partner's own dashboard; confirmed the partner-side notification badge count and the notifications page itself update correctly, and that Mark All Read clears the unread count in the database; verified document rejection with remarks updates the document's badge and remarks column, fires a `document_rejected` notification, and displays correctly; verified a second partner rejected directly from `pending` sees the rejection reason on login. `php -l` clean across every changed/new file. All test partner rows, status history, notifications, business profiles, and the one uploaded test PDF were removed from the local dev database/filesystem after verification.

**Not done this pass** (later-phase territory per the roadmap): partners submitting real visa applications, quotations/tiered pricing, wallet/invoicing, team sub-users, the fuller communication centre (this phase's notifications remain badge-only, no email), CSV export, document-expiry alerts, and the public SEO landing page. Real mobile OTP/SMS, WhatsApp Business API, Google OAuth, and payment gateway integration remain explicitly deferred, unchanged from prior phases. Real SMTP credentials still have not been supplied by the client — `config/smtp.php` still does not exist in this environment, so all email in this project remains in dev-mode fallback.

## B2B Partner CRM — Phase 4: partners submitting real visa applications + case tracking

Fourth of the 10-phase B2B roadmap. As planned from Phase 1 onward, this reuses the existing `customers`/`visa_applications` tables entirely through `customers.referred_by_partner_id` (added in Phase 1) — **no new tables**. An active, approved partner can now refer a customer and submit a real visa application on their behalf, and track it through to completion using the same `visa_application_history` table the admin CRM already writes to.

**`partner/pages/application-create.php`** (new): a form capturing the referred customer's name/email/mobile plus destination country, visa type, travel date, and notes — deliberately the same field set and dropdown sources (`countries_all()`, `visa_types_all()`) as `admin/pages/visa-applications.php`'s own "create" action, so a partner's application looks identical in the CRM to one a staff member created. Gated on `is_partner_active()` — only approved partners can refer customers, matching the intent of the approval workflow Phase 3 built. Validation follows the wizard pages' own in-page `$errors` array + redisplay pattern (not the admin layout's flash+redirect, which this partner page doesn't have available) since it's the established convention for partner-facing forms in this codebase.

**Duplicate/ownership handling**: looks up any existing customer by email or mobile before creating one. If found and it's a customer this same partner already referred, the new application attaches to that existing customer (a partner referring the same person twice doesn't create two customer records). If the email/mobile belongs to someone else's customer record — a walk-in customer, or a different partner's referral — the request is blocked with a message pointing them to Visagiri support, rather than silently attaching a new application to a customer record the partner doesn't own the referral for. This is a real access-control boundary, not just a UX nicety: verified below that Partner B genuinely cannot claim Partner A's referred customer by re-submitting their email.

**`partner/pages/applications.php`** (new): list + detail, scoped entirely by `customers.referred_by_partner_id = <this partner>` in every query (list, detail, and the 403-equivalent "not found" on a direct URL to another partner's application). The detail view's "Case Timeline" section reads the same `visa_application_history` table and renders it the same From/To/When shape as Phase 3's own Partner Status History table, for consistency within this codebase rather than inventing a different timeline widget. A read-only Documents section is also shown (type, verification status, upload date) — upload capability from the partner side is not part of this phase.

**Dashboard integration**: `partner/pages/dashboard.php` gained a "+ New Application" / "My Applications" button pair, and the existing Referred Customers table's application-count column is now a link into the applications list. `includes/partner-layout.php` gained a "My Applications" nav entry between Dashboard and Notifications.

**Verified end-to-end via Playwright** (16/16 checks passed): registered and admin-approved two separate active test partners; confirmed the real server-side "at least one of email or mobile" validation (deliberately exercised through fields with no client-side `required`, to test the actual PHP validation path rather than the browser blocking submission); created an application for a new referred customer and confirmed the customer row, the application row, and its initial `draft` history entry all landed correctly; submitted a second application for the same customer (matched by email) and confirmed no duplicate customer row was created while the application count for that customer became 2; confirmed the dashboard and applications list both reflect the new data; confirmed Partner B gets the same "not found" response as a nonexistent record when directly navigating to Partner A's application by ID; confirmed Partner B attempting to refer a customer using Partner A's referred customer's email is blocked with the "already exists" message rather than silently taking over the record. `php -l` clean across every changed/new file. One genuine test-script bug was caught and fixed during this pass, not an application bug: Playwright's `button[type="submit"]` selector was matching the sidebar's Logout button (also a bare `<button type="submit">`, and earlier in the DOM) instead of the actual form's submit button — scoping the selector to the specific form fixed it. All test partners, customers, applications, and history rows were removed from the local dev database after verification.

**Not done this pass** (later-phase territory): quotations/tiered pricing, wallet/invoicing, team sub-users, the fuller communication centre, CSV export, document-expiry alerts, and the public SEO landing page. Real mobile OTP/SMS, WhatsApp Business API, Google OAuth, payment gateway, and real SMTP credentials remain unchanged from prior phases.

## B2B Partner CRM — Phase 5: tiered pricing/commission engine

Fifth of the 10-phase B2B roadmap. **A real scope adjustment from the original planning notes, made after actually reading the codebase rather than assuming**: the Phase 1 roadmap had sketched this phase as "Quotations + tiered pricing/commission engine (`partner_tiers`, `partner_quotations`)." Once this phase actually started, `visa_quotes` (from the pre-existing `schema-crm.sql`) turned out to already fully cover quoting any `visa_applications` row — partner-referred or not — with its own complete admin create/view flow (`admin/pages/visa-applications.php`'s `add_quote` action, already working since long before this B2B project began). Adding a second, parallel `partner_quotations` table as originally sketched would have duplicated that entirely. So this phase built only what was genuinely missing: the tier master data and the commission calculation it drives. This is exactly the kind of check-before-building the whole B2B project has done at every phase (Phase 1's own audit found several roadmap assumptions that didn't match reality either).

**Database** (`database/schema-b2b-phase5.sql`, additive): new `partner_tiers` table (name, `commission_type` ENUM `percentage`/`flat`, `commission_value`, description, sort order, active flag) and `partners.tier_id` (nullable FK → `partner_tiers`, `ON DELETE SET NULL`) — ships NULL until an admin assigns one, the same "ships empty until real" convention as `assigned_admin_id` since Phase 1.

**Admin — tier master data** (`admin/pages/partner-tiers.php`, new): a straightforward CRUD list, deliberately mirroring `admin/pages/visa-types.php`'s existing simple-master-data pattern (list + toggle-active + edit + delete) rather than inventing a new admin UI shape for what is, structurally, the same kind of thing. Gated on `partners.manage` — tiers exist purely to drive partner commissions, not a general content-management concern, so no new permission key was introduced. Deleting a tier that partners are currently on falls back those partners to "no tier assigned" rather than being blocked, consistent with the FK's `ON DELETE SET NULL` — verified this actually happens, not just assumed from the schema.

**Admin — tier assignment**: `admin/pages/partners.php`'s profile page gained a "Commission Tier" row and a Set Tier form, positioned right next to the existing Assign Manager control since they're the same kind of per-partner admin configuration action.

**The commission engine itself** (`admin/pages/visa-applications.php`): the existing manual "Partner Commission" section (set an amount, pick a status) now shows the referring partner's tier and rate, and — when no commission has been set yet — computes a suggested amount from the tier and the application's most recent quote's *service fee* specifically (not the government fee, which passes straight through to the embassy and isn't Visagiri's margin to split commission from: percentage tiers take a cut of service fee, flat tiers just use their fixed value regardless of quote figures). The suggestion is only ever a starting point, pre-filled into the same input the admin already types into — no separate "accept suggestion" workflow was added, since a plain pre-filled, editable number field already fully satisfies "engine assists, human decides" without new UI surface.

**A real bug caught during verification, not shipped**: the pre-existing `$commission = $commissionStmt->fetch();` line (written before this phase, in the original Phase-1-era code) initialized `$commission = null` but then reassigned it from `PDOStatement::fetch()`, which returns `false` — not `null` — when no row matches. Nothing before this phase ever did a strict `$commission === null` check, so the mismatch was invisible; this phase's new "only show the suggestion when no commission exists yet" guard was the first code to check it strictly, and the hint silently never rendered because `false !== null`. Caught by the Playwright verification (the pre-filled *input value* worked, since `??` treats `false['x']` as null too via PHP's loose fallback there, but the surrounding hint paragraph's strict guard didn't) — fixed by normalizing with `?: null`, a one-line change to code this phase didn't otherwise touch.

**Partner-side visibility**: the partner dashboard (`partner/pages/dashboard.php`) gained a fourth stat card showing the partner's own tier name and rate (or "No tier assigned yet"). The partner application detail page (`partner/pages/applications.php`, built in Phase 4) gained a Quotes section showing any non-draft `visa_quotes` for that application — the same data the admin side already had, now visible to the partner who referred the customer.

**Verified end-to-end via Playwright** (13/13 checks passed on the final run): created a percentage tier (Gold, 10%) and a flat tier (Silver, ₹500) through the real admin UI; registered and approved a test partner, assigned Gold, confirmed it displays on both the admin profile and the partner's own dashboard; had the partner submit an application (reusing Phase 4's flow) and the admin add a quote (₹1,000 government / ₹2,000 service); confirmed the commission section correctly suggested ₹200 (10% of the ₹2,000 service fee) and pre-filled it into the input; saved it and confirmed the real value landed in `partner_commissions`; confirmed the partner sees the quote's fee breakdown on their own application detail page; confirmed deleting a tier that's actively assigned to a partner correctly falls that partner back to unassigned rather than blocking the delete or leaving a dangling reference. `php -l` clean across every changed/new file.

Two rounds of test-script debugging were needed before the real run, both revealing genuine (if minor) issues worth recording: (1) the same "ambiguous submit-button selector" class of bug as Phase 4 — the admin layout's own Logout button is *also* a bare `<button type="submit">` and sits earlier in the page than the tier form, so an unscoped Playwright selector silently logged the test admin out mid-flow instead of saving the tier; scoping the selector to the tier form fixed it. (2) The commission-suggestion bug described above. Both were caught specifically *because* this session insists on real browser-driven verification with strict, specific assertions rather than "the code looks right." Separately (not a bug, expected behavior): this sandbox's admin-login and partner-registration rate limiters (5 attempts / 15 minutes, `includes/auth.php`) count every attempt, not just failures, so repeated Playwright test runs against the same local dev database within a short window legitimately lock the test accounts out — cleared via a direct `DELETE FROM rate_limits` between runs, standard test-environment housekeeping, not a production concern. All test tiers, partners, customers, applications, and their related rows were removed from the local dev database after verification.

**Not done this pass**: wallet ledger + invoicing, team sub-users, the fuller communication centre, CSV export, document-expiry alerts, the public SEO landing page. Real mobile OTP/SMS, WhatsApp Business API, Google OAuth, payment gateway, and real SMTP credentials remain unchanged from prior phases.

## B2B Partner CRM — Phase 6: wallet ledger + invoicing

Sixth of the 10-phase B2B roadmap. Manual/staff-driven throughout — no payment gateway, the same stance the forex module has held since its own Phase A (that module never integrated one either, and nothing in this session's environment could reach one regardless).

**Database** (`database/schema-b2b-phase6.sql`, additive): `partner_wallet_transactions` (partner_id, `type` ENUM `credit`/`debit`, amount, reason, an optional `reference_type`/`reference_id` pair used only to make an automatic credit idempotent, `created_by`) and `partner_invoices` (reference no, partner_id, optional period, total_amount, `status` ENUM `draft`/`issued`/`paid`, notes, timestamps). A partner's wallet balance is deliberately **not** a stored column anywhere — it's computed as `SUM(credit) - SUM(debit)` over the ledger every time it's needed, so there is nothing that can drift out of sync with the transaction history that is the actual source of truth.

**The wallet auto-credits from commissions, once**: `admin/pages/visa-applications.php`'s existing `set_commission` action (unchanged in every other respect) now inserts a wallet credit transaction whenever a commission is saved with status `approved` or `paid` and a non-null amount — but only if no wallet transaction already references that application (`reference_type = 'visa_application'`, `reference_id = <application id>`). This guard matters because `set_commission` can legitimately be called more than once on the same application (e.g. `pending → approved → paid`) without meaning to credit the same money twice — verified directly: saving the same commission a second time with a different status produced zero new wallet rows.

**Admin — wallet management**: `admin/pages/partners.php`'s profile page gained a Wallet section (running balance, full transaction table) between Commissions and Status History, plus a manual credit/debit form for anything the automatic commission credit doesn't cover — a payout via bank transfer, a correction, a bonus. This is the only way money ever leaves a partner's wallet in this system: there's no payment gateway integration to do it automatically, matching the phase's own stated scope.

**Admin — invoicing** (`admin/pages/partner-invoices.php`, new): a straightforward list/create/view flow, one linear status lifecycle (`draft → issued → paid`, no going backwards — the same shape as `visa_quotes`' and the forex module's own quotation statuses). Creating an invoice for a partner pre-fills the total amount from that partner's *current* wallet balance at the moment of creation (not automatically recalculated later — an invoice is a snapshot, not a live view), while staying fully editable in case the invoice covers a different figure. A partner's own invoices are also listed inline on their admin profile page, next to the Wallet section, with a "+ New Invoice" link that carries the partner preselected.

**Partner-side**: a new `partner/pages/wallet.php` (linked from a new "Wallet" nav item and a dashboard button) shows the partner's own balance, full transaction ledger, and invoice list — entirely read-only, since every wallet/invoice action in this phase is staff-driven by design.

**Verified end-to-end via Playwright** (15/15 checks passed on the first full run): created a 10% commission tier and an active test partner on it; had the partner submit an application and the admin add a ₹1,000/₹2,000 quote and approve the commission — confirmed exactly one wallet credit transaction landed at ₹200 (10% of the ₹2,000 service fee, reusing Phase 5's engine) and that it shows correctly on the admin profile; re-saved the same commission with status `paid` and confirmed the wallet transaction count stayed at 1, not 2; created an invoice and confirmed its total pre-filled from the ₹200 balance; walked it through `draft → issued → paid`, confirming the terminal "no further actions" state at the end; recorded a manual ₹200 debit ("Payout via bank transfer") and confirmed the balance returned to ₹0.00; confirmed the partner's own wallet page shows the correct balance, both transactions, and the paid invoice. `php -l` clean across every changed/new file. All test tiers, partners, customers, applications, wallet transactions, and invoices were removed from the local dev database after verification.

**Not done this pass** (the two remaining roadmap phases): team/sub-users, the fuller communication centre, real notification-bell UI beyond what Phase 3 already built; CSV exports, in-app document-expiry panel, tier recalculation; B2B enquiry capture + the public SEO landing page. Real mobile OTP/SMS, WhatsApp Business API, Google OAuth, payment gateway, and real SMTP credentials remain unchanged from prior phases.

## B2B Partner CRM — Phase 7: team/sub-users + communication centre

Seventh of the 10-phase B2B roadmap. The original Phase 1 planning notes described this phase as also needing "real notification-bell UI (genuinely new pattern, none exists anywhere yet)" — that part turned out to already be done: Phase 3 built exactly that (badge-only `partner_notifications`, unread-count nav badge). So this phase's actual new work was the other two pieces: letting a partner company add additional logins under their account, and a real two-way message thread between a partner and Visagiri staff.

**Team/sub-users — a second identity table, not a rewrite of the first.** `partner_team_members` (new, `database/schema-b2b-phase7.sql`) holds *additional* logins scoped to an existing `partner_id` — each with its own email/password_hash and a `role` (`manager` or `viewer`). The original owner login (`partners.email`/`password_hash`) is completely unchanged; "owner" isn't a stored role value at all, it's simply the absence of a team-member session, checked in code. `includes/partner-auth.php`'s `log_in_partner()` gained an optional second parameter to record which identity is active; `current_partner_role()` re-derives the role from the database on every check rather than trusting anything client-supplied. `partner/pages/login.php` now falls through to the team-members table when the primary login doesn't match, requiring `status = 'active'` (an `invited` member hasn't set a password yet; `suspended` is blocked). Deliberately **no "remember me" for team-member logins** — the existing remember-cookie mechanism only stores a `partner_id`, and resuming it would silently log the browser back in as the owner, quietly discarding the team member's own more limited role; rather than build a parallel remember-token system for a secondary identity, this phase just doesn't offer the checkbox there.

Invitations (`partner/pages/team.php`, owner-only for every mutation) use the exact same token pattern as email verification and password reset (`create_partner_team_invite()`/`verify_partner_team_invite_token()`/`accept_partner_team_invite()` in `partner-auth.php` — random 32-byte token, only its SHA-256 hash stored, this one with a 7-day expiry since it's an out-of-band invite rather than a same-session link) and the same `send_mail()` + `APP_DEBUG` on-screen fallback every other partner-facing email in this project already uses. `partner/pages/team-accept.php` is where an invited member sets their password and gets auto-logged-in. A `viewer` can see the dashboard, applications, wallet, and invoices but is blocked — server-side, not just a hidden button — from `partner/pages/application-create.php` (403), and the "+ New Application" button is hidden from their nav/dashboard/applications-list views. Only the owner sees the invite form and the Suspend/Reactivate/Remove controls on the team roster.

**Communication centre — extends the table that already does this job for customers.** `dashboard/pages/messages.php` (pre-existing, from the original site build) already implements a real two-way per-application chat by reusing `visa_application_notes` with a `customer_id`/`visible_to_customer` pair rather than a dedicated messages table. This phase adds the equivalent `partner_id`/`visible_to_partner` pair to that same table (`schema-b2b-phase7.sql`) instead of standing up a parallel `partner_messages` table that would just duplicate the identical shape — the two audiences stay completely separate because each query filters on its own flag; a partner never sees the customer-facing thread on the same application and vice versa. `partner/pages/messages.php` mirrors the customer version almost line-for-line, scoped to `customers.referred_by_partner_id = <this partner>`. On the admin side, `admin/pages/visa-applications.php`'s existing note-composer form gained a second checkbox ("Send as a message the referring partner can see in their portal", shown only when the application has a referring partner) alongside the pre-existing customer one, and sending a partner-visible note calls `notify_partner()` — so a new staff message shows up in the partner's Phase 3 notification badge, not just silently in the thread. All team-member logins post messages attributed to the partner company as a whole ("You" / "Partner"), not to the individual team member — this project doesn't track which specific team member sent a given message, a deliberate scope limit rather than an oversight.

**Verified end-to-end via Playwright** (17/17 checks passed): registered and approved a test partner (owner); invited a `viewer` team member and captured the dev-mode invite link; the viewer accepted the invite, set a password, and landed on the dashboard with the "New Application" button correctly absent; confirmed direct navigation to `application-create` is blocked server-side with a 403, not just hidden; confirmed the viewer's own credentials work for a fresh login after logging out; had the owner suspend the viewer and confirmed the suspended account can no longer log in at all ("Invalid email or password", not a "suspended" message that would leak account existence); had the owner submit an application, the admin send a partner-visible note, confirmed the owner's notification badge incremented and the message appeared in their thread; had the owner reply, and confirmed the admin sees the reply tagged "Partner" with the "Visible to partner" badge. `php -l` clean across every changed/new file. One test-script-only bug caught before the real run: the dev-mode invite link regex was hardcoded to the Playwright test's own port (8099) instead of the site's actual configured `APP_URL` (8106 in this environment) — the same class of mistake documented in this project's very first wizard-verification pass, now caught and fixed the same way (match the token generically, don't hardcode the host). All test partners, team members, customers, applications, notes, and notifications were removed from the local dev database after verification.

**Not done this pass** (the one remaining roadmap phase): CSV exports, in-app document-expiry panel, tier recalculation; B2B enquiry capture + the public SEO landing page. Real mobile OTP/SMS, WhatsApp Business API, Google OAuth, payment gateway, and real SMTP credentials remain unchanged from prior phases.

## B2B Partner CRM — Phase 8: CSV exports, document-expiry panel, tier recalculation

Eighth of the 10-phase B2B roadmap — the roadmap's own wording specified all three of these should be admin-triggered, not cron-driven, since no cron infrastructure exists anywhere in this project.

**CSV export — the first anywhere in this codebase.** Confirmed by grep before writing anything: no page in this entire project has ever generated a CSV. `includes/csv-export.php` (new) is one small shared helper, `export_csv_response()`, that every export action now calls rather than each page hand-rolling its own `header()`/`fputcsv()` sequence — UTF-8 BOM included so Excel (still the most likely opener) renders non-ASCII company names correctly instead of guessing the wrong encoding. Three export points, all read-only and respecting existing RBAC scoping (a Relationship Manager scoped to their own assigned partners can only export their own partners, exactly like the list view they're already restricted to): the Partners list (`?action=export`, honors whatever search/status filter is currently applied — export what you're looking at, not always everything), and per-partner Wallet Transactions / Commissions exports from the profile page (only shown when there's data to export).

**Document-expiry panel** (`admin/pages/partner-document-expiry.php`, new): `partner_documents.expiry_date` has existed since Phase 2 but nothing ever read it. This page lists every partner document that has an expiry date on file, soonest (or already-expired) first, so staff can chase IATA certificate/license renewals before they lapse — expired shown in red, expiring within 30 days in amber, everything else green. Deliberately no email reminders or automated notifications here — that's the "in-app panel" scope the roadmap asked for, not a notification system; an admin has to actually look at the page.

**Admin-triggered tier recalculation**: `partner_tiers` gained a nullable `min_referred_customers` qualification threshold (`database/schema-b2b-phase8.sql`) — NULL means "manual assignment only, never touched by recalculation," which is what every tier created before this phase keeps doing until an admin deliberately sets a threshold on it. A new "Recalculate Tier" button on the partner profile counts that partner's referred customers and finds the highest-`sort_order` active tier whose threshold is met (reusing `sort_order` as the tier-ranking signal, the same "display order" convention this codebase already uses for visa types and countries, rather than inventing a second ranking field) — assigns it if it differs from the current tier, notifies the partner (`notify_partner()`, so it shows up in their Phase 3 notification badge), and reports "no change needed" if recalculating lands on the same tier the partner is already on. This is one click, not automatic — nothing recalculates on its own, matching the "admin-triggered, not cron" instruction exactly.

**Verified end-to-end via Playwright** (13/13 checks passed): created a 0-referral "Bronze" tier and a 2-referral "Gold" tier; registered and approved a test partner and recalculated their tier with zero referrals, landing correctly on Bronze; recalculated again with still zero referrals and confirmed it correctly reported no change; had the partner refer 2 customers via the real application-create flow, recalculated again, and confirmed the upgrade to Gold; confirmed two real tier-change notifications were recorded (one per actual transition — unassigned→Bronze, then Bronze→Gold); downloaded the Partners CSV export via a real authenticated request and confirmed both the `text/csv` content-type and that the test partner's row (with its new Gold tier) is present in the file; inserted a document with a 10-day expiry directly (the document *upload* UI doesn't currently collect an expiry date at all — a real, pre-existing gap this phase didn't set out to close, since it was scoped to building the panel that reads this column, not extending every upload form to capture it) and confirmed the expiry panel lists it with the correct partner, document type, and "expires in ~10 days" status. `php -l` clean across every changed/new file. All test tiers, partners, customers, applications, notifications, and the one manually-inserted test document were removed from the local dev database after verification.

**Not done this pass** (the one remaining roadmap phase): B2B enquiry capture + the public SEO landing page. Real mobile OTP/SMS, WhatsApp Business API, Google OAuth, payment gateway, and real SMTP credentials remain unchanged from prior phases — this remains explicitly deferred, blocked on the client supplying real third-party credentials, per the original Phase 1 roadmap.

## B2B Partner CRM — Phase 9: enquiry capture + public SEO landing page

Ninth of the 10-phase B2B roadmap — the last buildable phase. Phase 10 (real mobile OTP/SMS, WhatsApp Business API, Google OAuth, payment gateway, virus scanning, transactional email/SMTP) remains explicitly deferred, blocked on client-supplied third-party credentials, unchanged from the original Phase 1 roadmap; there is nothing actionable there in this sandbox.

**Database** (`database/schema-b2b-phase9.sql`, additive): a new `partner_enquiries` table — reference number, company/contact/email/mobile, optional business type and message, `status` ENUM `new`/`contacted`/`converted`/`closed`, optional `assigned_admin_id`, IP address, soft-delete columns. This is a lead-capture record for a *prospective* partner who isn't ready to go through the full registration wizard yet, not a partner account — deliberately a separate table from `partners` rather than a pre-partner status on that table, matching how `general_enquiries` already exists separately from `customers` for exactly the same "public form → staff follow-up, may or may not convert" shape.

**Public landing page** (`pages/partner-program.php`, new, routed at `/partner-program/`): hero, a six-item benefits grid, and a four-step "How It Works" list — every claim on the page describes a feature this project actually built in Phases 1-8 (tiered commissions, real-time case tracking, wallet/invoicing, team accounts, direct messaging, document management), nothing aspirational or fabricated. Two calls to action: "Register Now" straight into the real wizard (`partner/pages/register.php`), and this page's own enquiry form for a prospect not ready to commit. The form follows `pages/enquire.php`'s established pattern exactly — same honeypot field, same `rate_limit_check()` call (5/900s, the same key shape as every other public form), same `generate_reference_number('PENQ', 'partner_enquiries', 'reference_number')` retry-on-collision loop, same `notify_enquiry_channels()` call so a submission reaches the same Google-Sheets/email channels every other enquiry type already does. SEO metadata (title/description/canonical/BreadcrumbList JSON-LD) follows `pages/documentation.php`'s shape. Linked from the footer's Resources column and reachable via `public/index.php`'s router.

**Admin CRM** (`admin/pages/partner-enquiries.php`, new, routed at `/admin/partner-enquiries/`): list (with a status-filter button row) + detail + status/assignment update + soft delete, mirroring `admin/pages/general-enquiries.php`'s pattern line-for-line since it's the same record shape. Gated on the existing `partners.view`/`partners.manage` keys — no new permission introduced, since this is squarely a partners-namespace concern. The detail view carries an explicit note that setting status to "Converted" is a status label only and does not itself create a partner account — staff still have to walk the prospect through the real wizard or point them to it.

**A real, pre-existing, site-wide bug found and fixed — not a CSP issue.** While verifying the new Delete button, a soft-delete that looked successful in the browser (the page navigated to the enquiries list, as if a redirect had fired) turned out to leave `deleted_at` untouched in the database every time. The cause: this page's `$id` was read only from `$_GET['id']`, but the Delete form's `action` attribute deliberately omits the query string (it posts to the bare list URL, relying on a hidden `<input name="id">` in the POST body instead — the same shape the Update form on the same page uses, except Update's `action` *does* carry `?id=`). With `$id` null on that POST, the `$postAction === 'delete' && $id` guard was always false, so the whole delete branch was silently skipped and the request fell straight through to the bottom of the file, which renders the ordinary enquiries list — indistinguishable from a real post-delete redirect unless you check the database. Twelve other admin pages already guard against exactly this by falling back to `$_POST['id']` when `$_GET['id']` isn't set (`admin/pages/visa-types.php`, `customers.php`, `users.php`, `partner-tiers.php`, `countries.php`, `faqs.php`, `embassies.php`, and others) — this new page copied the pattern from `general-enquiries.php`, which turned out to be one of only two existing pages missing that fallback (the other being `admin/pages/visa-applications.php`'s own Delete button). All three now use the same `$_GET['id'] ?? $_POST['id']` fallback as the other twelve; **this means Delete on general enquiries and on visa applications has been silently non-functional in the admin panel this whole time**, not just on this new page — a real, previously-undiscovered defect this phase's verification surfaced and fixed as a drive-by, since it's a one-line, low-risk change directly adjacent to what this phase was already touching. (An earlier hypothesis during debugging — that the site's `script-src 'self'` CSP was silently blocking every inline `onsubmit="return confirm(...)"` handler project-wide — was investigated and ruled out: the delete-confirmation `confirm()` dialog genuinely doesn't fire under CSP, exactly as suspected, but the delete request itself was never blocked by that; it fails purely because of the `$id` bug above. The missing confirmation dialog is a separate, lower-severity, genuinely pre-existing UX gap — present on every admin delete button across the whole panel — that this phase did not attempt to fix, since it's unrelated to the enquiries feature and its safest fix (moving confirmation into `assets/js/admin.js` as a real event listener, which the CSP already permits) touches every admin page with a delete button, not just this one.**

**A second real, this-phase-only bug found and fixed**: the public landing page's real (non-honeypot) enquiry submission inserted the `partner_enquiries` row correctly but then threw `Call to undefined function notify_enquiry_channels()`, which the surrounding try/catch swallowed into a generic "something went wrong" error message shown to the visitor — even though their enquiry had, in fact, been saved. `includes/google-sheets.php` (where `notify_enquiry_channels()` lives) is `require`d explicitly at the top of every other page that calls it (`pages/enquire.php`, `pages/contact.php`, `attestation/index.php`) rather than being autoloaded globally; the new landing page never added that `require`. Fixed by adding it. Caught by the Playwright verification's real-submission assertion failing even though the database check on the very next line passed — a good example of why this phase (like every phase before it) insists on asserting the actual rendered response, not just the resulting database state.

**Verified end-to-end via Playwright** (14/14 checks passed on the final run, after both fixes above): landing page loads with correct title/canonical/JSON-LD; hero content and the Register CTA are present; a honeypot-triggered submission creates no database row; a real submission succeeds, shows the `VG-PENQ-YYYY-NNNNNN` reference number on-screen, and creates a `status = 'new'` row; the admin enquiries list shows the new submission; the detail view shows company/contact/message correctly; updating status and assignee persists; deleting the enquiry actually soft-deletes it now (confirmed via direct DB check, not just the UI) and it drops off the list. `php -l` clean across every changed/new file. Two test-script-only issues were also found and fixed while chasing the two real bugs above (neither is an application defect): the canonical-URL assertion compared against the Playwright test server's own host (`127.0.0.1:8099`) instead of the site's configured `APP_URL` (`127.0.0.1:8106` in this environment) — the same class of mistake this project has now hit and fixed in three separate phases, always by matching the path suffix generically instead of hardcoding a host; and the real-submission assertion originally used `Promise.all([waitForNavigation().catch(() => {}), click(...)])`, which papered over a genuine timeout (the landing page renders its success message inline rather than redirecting, so an un-caught `waitForNavigation` was in fact the right primitive once the real bug above was fixed, since a plain form POST is still a real page navigation even without a `Location` header). All test enquiry rows and `rate_limits` rows were removed from the local dev database after verification.

**Not done this pass**: fixing the missing delete-confirmation dialog site-wide (documented above as a real but separate, lower-severity issue spanning every admin page with a delete button, not scoped to this phase's work). Real mobile OTP/SMS, WhatsApp Business API, Google OAuth, payment gateway, and real SMTP credentials remain unchanged from prior phases — Phase 10 stays explicitly deferred, blocked on the client supplying real third-party credentials. With Phase 9 complete, every buildable phase of the original 10-phase B2B roadmap is now done.

## Utility Bar Redesign — indicative USD→INR rate + unchanged Login panel

A client-supplied redesign brief for the header's top utility strip: replace it with two things only — an indicative USD→INR rate on the left, and the existing Login/Sign-Up panel untouched on the right, no other clutter.

**Most of the backend already existed and was reused, not rebuilt.** Before writing anything, checked the current header and found `includes/currency-rate.php` already implements exactly what the brief asked for: a live fetch from Frankfurter (no API key needed), a 1-hour file cache, graceful fallback to the last successfully-cached rate (clearly marked "Last known rate") when the live fetch fails, and `rate: null` — never a fabricated number — if there has never been a successful fetch at all. A `/api-usd-inr-rate/` JSON endpoint and a same-session 5-minute polling refresh in `assets/js/site.js` already existed too. None of that needed touching; this pass was a front-end restructuring of the utility bar itself plus one small new interactive component.

**What changed** (`includes/header.php`, `public/assets/css/layout.css`, `public/assets/js/site.js`):
- Removed the "Technology Meets Travel & Visa" tagline and the "24×7 Support" WhatsApp link from the utility bar — both were real, working links, not deleted from the site, just out of scope for what the brief wanted this specific bar to communicate. WhatsApp (and email, and Enquire Now) stay one tap away sitewide via the floating `includes/enquiry-widget.php`, which every public page already includes independently of the header.
- The rate display is now a real interactive component (`.site-header__rate-widget`), not a static `<span>`: a `<button>` trigger opens a popover on hover (desktop, via the existing `.has-dropdown` CSS already used by the Login menu) or tap (touch, via JS), showing the full rate, "Indicative exchange rate," a "Last updated: [date, time]" line sourced from the same `as_of` timestamp the cache already tracked, the disclaimer text the brief specified ("Rates may vary from actual bank/provider rates."), and a "Get Forex Assistance" CTA.
- The Login dropdown itself — trigger, all three options (Customer/Employee/B2B Partner), styling, behavior — is untouched, per the brief's explicit "do not redesign the Login panel" instruction. It simply moved from being the third item in a crowded row to being the utility bar's only other element.
- Mobile: the utility bar used to be hidden entirely below 768px (`display: none`), with a duplicate, separate rate readout living inside the hamburger drawer instead. Per the brief's explicit mobile section, the bar itself is now visible at every width, compacted to the rate only (Login stays reachable via the hamburger drawer's own pre-existing Login section, so it isn't duplicated in an already-narrow bar); the "Indicative Rate" label text is hidden below 420px while the rate figure itself stays visible, matching the brief's "compact version" instruction exactly.

**The "Get Forex Assistance" CTA connects to a real, existing CRM pipeline, not a new or fabricated one.** The brief said it "should connect to the existing Forex enquiry CRM" — checking the codebase first (not assuming) found there is no dedicated public forex-enquiry landing page; forex requests are staff-created from the admin side only. Rather than inventing a new page/table for this, the CTA links to `/contact/?topic=forex`, and `pages/contact.php` (the site's one general-purpose public enquiry form, already feeding `general_enquiries` with a generated reference number and the same Google-Sheets/email notification path every other enquiry type uses) now recognizes that query parameter: it prefills the message with real context ("I saw the indicative USD to INR rate on your website and would like help with a forex/currency exchange requirement."), swaps the eyebrow/heading copy to "Forex Assistance," and — since `general_enquiries.service_type` is a free-text column, not an ENUM — tags the saved row `service_type = 'forex'` instead of `'general'`, a genuinely queryable distinction for staff. Plain `/contact/` visits are completely unaffected.

**A real, pre-existing bug found and fixed while building this, affecting the Login dropdown too, not just the new rate widget.** Playwright verification of the new popover's Escape-to-close behavior kept failing: the JS correctly removed the `is-open` class, but the popover stayed visually open. Root cause: the existing Login dropdown's own Escape handler calls `.focus()` on the trigger afterward (intentionally, so keyboard focus isn't lost) — but `.has-dropdown`'s CSS also opens the menu on `:focus-within`, so refocusing the trigger immediately re-triggers the exact CSS rule that was supposed to be defeated by removing `is-open`. This is a genuine, previously-undiscovered defect in the pre-existing Login dropdown, not something introduced by the new component — confirmed directly: pressing Escape on the unmodified Login menu with the mouse still over its trigger left it visibly open too. The mega-menu component (`initMegaMenu()` in `site.js`) had already solved this exact problem with an `is-force-closed` CSS class cleared on `mouseenter`/`mouseleave`/`focusout` — rather than reinventing a fix, both the Login trigger and the new rate trigger were refactored onto one shared `initSimpleDropdown(triggerId, menuId)` helper that reuses that same proven pattern, fixing the bug in both places at once and removing what had been two near-duplicate hand-rolled implementations.

**Verified end-to-end via Playwright** (29/29 checks passed): tagline and WhatsApp-support link confirmed removed from the utility bar; rate widget renders with the correct `USD → INR ₹XX.XX` figure; popover hidden by default, opens on click, sets `aria-expanded="true"`, shows the rate value/"Indicative exchange rate"/"Last updated"/disclaimer text, closes on an outside click and closes on Escape (the fixed behavior); Login dropdown confirmed still exactly the three original options with Employee Login still pointing at `/admin/login/`; clicked the real CTA through to `/contact/?topic=forex`, confirmed the message field prefilled and the eyebrow changed, submitted the real form, and confirmed via direct DB query the saved row was tagged `service_type = 'forex'`; confirmed a plain `/contact/` visit is unaffected (original eyebrow, empty message field); on a 375px mobile viewport confirmed the utility bar is visible, compact, single-row, and the Login trigger is not duplicated there; on a 360px viewport confirmed the "Indicative Rate" label is hidden while the rate figure stays visible. `php -l`/`node --check` clean on every changed file. Since this sandbox has no outbound network access to the live rate API and no rate had ever been successfully cached here, verification used a locally-seeded test cache file (`storage/cache/usd-inr-rate.json`, which is gitignored via `storage/*` and was deleted again after verification) purely to exercise the interactive/populated-rate code paths — the "Rate unavailable" fallback path (confirmed to render correctly with no rate cached) is what this environment will actually show until it either gets real network access or a first successful fetch happens in production.

**Not done this pass** (out of scope per the brief): the optional EUR→INR secondary currency — the brief explicitly frames it as "if there is sufficient space" and "do not overcrowd," and USD→INR alone already fills the utility bar's intended minimal footprint, so it was left out rather than added speculatively; it would be a small, additive follow-up (Frankfurter supports fetching multiple target currencies in one request) if the client wants it later. The pre-existing mobile hamburger drawer's own separate rate/tagline/support block was left untouched — it's a different UI surface (the slide-out menu, not the utility bar itself) and wasn't part of what the brief asked to redesign.

## Utility Bar — restored WhatsApp "24×7 Support" link

Follow-up to the utility bar redesign above: the client asked to keep the WhatsApp "24×7 Support" link in the bar after all, rather than relying solely on the sitewide floating enquiry widget for it.

**What changed** (`includes/header.php`, `public/assets/css/layout.css`): restored the WhatsApp link between the rate widget and the (still-unchanged) Login dropdown, wrapped in the same `.site-header__utility-contact` flex group the pre-redesign markup used. On mobile, the link's icon stays visible in the now-compact utility bar while its "24×7 Support" text label hides below 768px (wrapped in a new `.site-header__utility-link-text` span so CSS can target just the text, the same technique already used for hiding "Indicative Rate" at very small widths) — keeping the bar to one line without wrapping, same constraint as before.

**Verified end-to-end via Playwright** (12/12 checks passed): rate widget, WhatsApp link, and Login dropdown all present and in the correct left-to-right order; the WhatsApp link still opens in a new tab with the correct message; the Escape-key close fix (from the previous pass) still works on both the rate popover and the Login menu; on a 375px mobile viewport the rate and the WhatsApp icon are both visible, the Login trigger stays hidden from the utility bar (still reachable via the hamburger drawer, unchanged), the bar stays a single compact row, and the "24×7 Support" text is hidden while the icon remains. `php -l` clean. Test rate cache seeded for verification was deleted afterward, same as the prior pass.

## Legal & Support Centre — footer redesign, 7 policy pages, grievance ticketing, contact/sitemap overhaul

A client-supplied 25-section brief asking to redesign the footer-linked legal/support pages (Privacy Policy, Terms & Conditions, Travel Terms, Payment Policy, Refund Policy, Cookie Policy, Grievance Redressal, Sitemap, Contact Us) into a professional "Legal & Support Centre," plus a footer overhaul — all 9 existing URLs preserved exactly, since they may already be indexed.

**Research before any design decision, same discipline as every prior phase.** A background research pass, then a design pass, confirmed two things the brief assumed rather than verified against this codebase: (1) none of the 7 policy pages had any real content — all 7, plus `/disclaimer/`, shared one generic "not ready yet" stub (`pages/legal.php`), `noindex`'d; (2) no SLA/escalation system, and no Team Leader/Manager roles, exist anywhere in this project, despite the brief describing a 4-level auto-escalation chain and referring to "the existing CRM SLA system" as though it already existed. Two decisions were put to the user directly rather than guessed at, and both were confirmed before writing any code:

1. **Unverifiable legal facts** (registered office address, governing-law/jurisdiction, grievance-officer contact) are never invented and never shown as bracket placeholders — they're new empty-by-default `setting()` keys (`legal_registered_office_address`, `legal_governing_law`, `legal_jurisdiction_city`, `legal_grievance_officer_name`, `legal_grievance_officer_email`), and every page template omits the sentence/clause referencing an unset key entirely rather than rendering it fake or broken-looking. An admin fills these in later via `/admin/settings/`, zero code changes — the same "ships empty until real" convention as `partner_tiers.min_referred_customers` since Phase 8 of the B2B build.
2. **Grievance SLA/escalation is real but deliberately scoped down**: a genuine SLA due-time per grievance (computed from a configurable `grievance_sla_hours` setting, default 72), a visible breach badge computed at render time (no cron exists anywhere in this project, so this is never a stored flag — `sla_due_at < NOW() AND status NOT IN (resolved, closed)`, checked fresh on every page load), and a manual "Escalate" button that reassigns to another admin holding `grievances.manage` and logs it via the existing `log_action()` audit pattern. No new roles were invented, no automatic multi-level chain was built — that was explicitly ruled out by the user rather than quietly dropped.

**Schema** (`database/schema-legal-phase1.sql`, `database/seed-legal-phase1.sql`, additive): a standalone `grievances` table (not an ALTER of the shared `general_enquiries` table, which is used by attestation/general/forex enquiries too and has no room for SLA columns without polluting all of them) — reference number, category, contact fields, `sla_due_at`/`escalated_at`/`escalated_from_user`/`escalation_reason`/`resolved_at`, soft-delete, the same shape as `partner_enquiries`/`general_enquiries`. A sibling nullable `documents.grievance_id` column, following the table's existing `visa_application_id`/`general_enquiry_id` pattern, so grievance attachments reuse the real document-upload system. Two new permissions (`grievances.view`/`grievances.manage`), granted to `super-admin` and `admin` via the exact `CROSS JOIN ... LIKE` idiom `seed-b2b.sql` established — no new role. `grievance_sla_hours` plus the five empty-by-default legal-fact settings, all added to `admin/pages/settings.php`'s existing `$fields` array alongside the per-page "Last updated" dates (see below).

**A real design correction caught before writing any content**, not shipped as originally sketched: the plan's first draft proposed mirroring `includes/partner-layout.php`'s two-function page-lifecycle shape (`render_legal_start()`/`render_legal_end()`) for the shared legal-page component. Re-reading `partner-layout.php` closely revealed *why* its second function deliberately skips `require`ing `includes/footer.php` — footer.php reads `$visaServiceLinks`/`$attestationCategories`/`$companyMenu`, which become local variables inside whichever function calls `header.php`, and don't survive across a second function's call boundary. That's the right tradeoff for a logged-in app shell (partner portal), where a full marketing footer wouldn't belong anyway — but blindly copying it onto the new legal pages would have silently dropped the real sitewide footer from all 7 public policy pages. Redesigned `includes/legal-layout.php` as two content-fragment functions (`render_legal_hero()`/`render_legal_hero_close()`) instead, called from each page's own top-level script exactly like `pages/documentation.php` already does — `header.php` and `footer.php` both required directly by the page, in the same scope, so the real footer renders correctly everywhere.

**The 6 pure-content pages** (`pages/privacy.php`, `terms.php`, `travel-terms.php`, `payment-policy.php`, `refund-policy.php`, `cookie-policy.php`, new): each uses the shared hero/breadcrumb/sticky-desktop-TOC/mobile-accordion component, with real prose grounded in what this codebase actually does — Privacy Policy cites the real AES-256-GCM field encryption (`includes/encryption.php`) and the real Google Sheets/Drive staff-notification bridge (`includes/google-sheets.php`) rather than generic security boilerplate; Terms & Conditions reuses the footer's own "does not guarantee visa approval" disclaimer sentence verbatim rather than paraphrasing it differently in two places; Payment Policy was written only after confirming there is no payment gateway anywhere in this codebase (`visa_payments`/`forex_payments` both record `payment_method` as staff-entered free text) — so it describes the real offline/staff-coordinated flow, and accurately cites the real government-fee/service-fee split already in `visa_quotes`; Cookie Policy was written only after grep confirmed there is no GA/GTM or any analytics script anywhere on the site, so it explicitly states analytics/preference/marketing cookies are "not currently used" rather than fabricating categories; Travel Terms frames flight/hotel/insurance sections as what applies *when* such assistance is provided alongside a visa service, never as a standing claim that Visagiri is a standalone travel agency, consistent with this project's established service-scope discipline.

**Grievance Redressal** (`pages/grievance.php`, new, replaces the stub): an intro (commitment, 5-step process, a response-time statement that reads `setting('grievance_sla_hours')` live so it can never drift from what the admin side actually enforces) plus a real form on the exact `contact.php`/`enquire.php` skeleton — honeypot, `csrf_require()`, `rate_limit_check()`, `generate_reference_number('GRV', ...)` with the standard 2-attempt retry-on-collision loop, an optional file attachment via a new `store_grievance_document_upload()` (`includes/documents.php`) that deliberately leaves both `uploaded_by` and `uploaded_by_customer_id` NULL, since a public grievance submitter is neither a logged-in admin nor a logged-in customer — a genuinely new uploader category this project hadn't needed before, confirmed correct by checking that `documents.uploaded_by` was already made nullable by an earlier migration (`schema-customer-partner-portal.sql`) specifically to support non-admin uploaders.

**Admin CRM** (`admin/pages/grievances.php`, new, cloned structure from `admin/pages/general-enquiries.php`): list/detail/status/priority/assignment/soft-delete, each change audit-logged exactly as `general-enquiries.php` already does, plus the SLA breach badge (list and detail) and the manual Escalate action described above. New nav entry in `includes/admin-layout.php`, gated on `has_permission('grievances.view')`, and new routes in `admin/index.php`.

**Contact page redesigned into a real "How Can We Help?" service selector** (`pages/contact.php`, modified): the existing `?topic=forex` boolean (shipped in an earlier session for the header utility bar's "Get Forex Assistance" CTA) became one case of a proper 5-way selector — Visa Assistance / Apostille & Attestation / Forex / Travel Services / General Enquiry — still writing into the same `general_enquiries` table with `service_type` tagged per selection, per the brief's own instruction to connect to the existing CRM rather than build a new one. `?topic=forex` is kept as a permanent backward-compatible alias so the header CTA's existing link never breaks. Added real contact cards (WhatsApp/Email/Call), all sourced from `setting()` — no invented phone numbers, addresses, or emails.

**Sitemap rebuilt** (`pages/sitemap.php`, modified): visual card-grid redesign with section icons (reusing the existing `primary_nav_icon()` helper), reorganized into Visa Services / Visa by Country / Apostille & Attestation / Documents / Travel & Forex / Resources / Contact & Support / Legal & Policies / About Visagiri. Every link was checked against the real route list in `public/index.php` before being added — no fabricated "Travel Services" landing page exists, so that category links to the real routes that do exist instead (`/travel-terms/`, `/forex/track/`, `/contact/?service=travel`, `/contact/?service=forex`). Fixed the two self-references (`/sitemap/`, `/contact/`) the old sitemap was missing from its own Legal & Support section.

**Footer redesigned** (`includes/footer.php`, modified): added a "Need Assistance?" CTA banner and a bottom contact strip (WhatsApp | Call Us | Email Us | Get Assistance) between the existing disclaimer bar and the legal-row — all 9 existing footer legal-row URLs untouched. "Get Assistance" (both the banner button and the strip item) opens the existing floating enquiry widget via a small new JS hook (`.js-open-enquiry-widget` in `assets/js/site.js`) rather than building a second enquiry surface, per the brief's own "open the existing quick enquiry dialogue" instruction. Migrated the previously-hardcoded copyright line (parent company/CIN/founding date) to read from `setting()`, with the exact current values as defaults so nothing visibly changes unless an admin edits Settings — added a small `format_founding_date()` helper (`includes/functions.php`) to turn the stored `YYYY-MM` into "April 2015" display text rather than adding a duplicate setting.

**A real bug caught and fixed during verification, not shipped**: the new footer "Get Assistance" triggers opened the floating enquiry widget and then immediately closed it again in the same click — the widget's own pre-existing "close on outside click" document-level listener didn't know to ignore clicks on the new trigger buttons (which live in the footer, outside `#enquiry-widget` by design), so the same click that opened it also satisfied "clicked outside the widget" once the event bubbled to `document`. Fixed by excluding `.js-open-enquiry-widget` elements from that outside-click check.

**Verified end-to-end via Playwright across every phase, never just code review** (final tallies): 6 content pages, 74/75 (the one "failure" was a test-script label mismatch, not an app bug — Travel Terms' actual H1 "Travel Terms & Conditions" doesn't equal the shorter footer-link label "Travel Terms" the test reused for both checks); Grievance round-trip as one connected flow (submit → reference number → admin list/detail → status update → age the SLA into the past → breach badge appears → escalate → audit log entries for both status_change and escalation → 403 for a non-privileged admin account), 19/19; Contact service selector, all 5 services plus the legacy alias plus the client-side toggle plus the contact cards, 28/28; Sitemap, every one of 64 unique links on the page crawled and confirmed non-4xx/5xx, 7/7; Footer CTA/contact-strip/copyright-migration/legal-row regression (including confirming the shared footer still renders correctly on `/privacy/`, not just the homepage), 16/16; a final consolidated sweep — SEO metadata (title/canonical/robots/description/JSON-LD) on `/grievance/`, `/contact/`, `/sitemap/` plus confirming `/disclaimer/` correctly stayed `noindex`'d as the unchanged stub it was always meant to remain, 19/19; mobile no-horizontal-overflow spot-check across grievance/contact/sitemap/footer, 5/5. `php -l` clean on every new/changed PHP file. All test grievances, general-enquiry test rows, and rate-limit rows were removed from the local dev database after verification — confirmed empty by direct query before this entry was written.

**Not done this pass, by deliberate decision, not oversight**: `/disclaimer/` was left on the shared stub, unchanged — it was never one of the 9 target URLs. The client's own previously-deferred "Unified Enquiry CRM + SLA Assignment System" proposal (recorded earlier in this document) was not resurrected — SLA tracking here is scoped only to the new standalone `grievances` table; `general_enquiries`, `visa_enquiries`, and the Forex module remain untouched. No Team Leader/Manager roles and no automatic multi-level escalation chain were built, per the user's own explicit decision recorded above.

## National Location SEO — Phase 1: states/UTs data model, admin CMS, `/visa-consultant/{state}/{city}/`, 2 real pilot pages (Bihar/Patna, Uttar Pradesh/Noida)

Client sent a 42-section "national SEO dominance" spec asking Visagiri to rank for `[Visa Agency/Consultant/Agent/Consultancy] + [Location]` across all 28 Indian states, 8 union territories, their major cities, and country×city combinations — while explicitly forbidding the shortcuts that make that easy: no doorway pages, no "swap the city name" templated content, no fake `LocalBusiness`/address/reviews, no mass-indexing without a real quality gate, no keyword stuffing. The spec's own instructions require auditing the existing codebase before building anything, and finding real content for 36 states/UTs plus dozens of cities plus 15 countries is a genuinely unbounded task — so this ships the complete, reusable machinery plus two fully real, content-complete pilot state/city pairs, not an attempt at national coverage in one pass.

**Audit before building** (confirmed by direct grep/read): no `states`/`cities` tables and no `/visa-consultant/`-style routes existed anywhere. The closest analog, the `countries` system, was mirrored column-for-column: `countries` (`id, name, slug UNIQUE, region_id, is_popular_destination, is_active, meta_title, meta_description`), `admin/pages/countries.php` (CMS gated on `require_permission('content.manage')`), `includes/data.php`'s `countries_all()`/`country_by_slug()`, and `visa/index.php`'s `$segments`-based sub-routing (`$segments[1]` = country slug, `$segments[2]` = type slug, a separate `hub.php` for the bare listing, `render_not_found()` for bad slugs) were all reused as the pattern for `states`/`cities`/`visa-consultant/`. `visa_faqs`' nullable dual-scope shape (`country_id NULL`, `visa_type_id NULL`) was reused directly for the new `location_faqs` table. `pages/sitemap-xml.php` already had an explicit "don't submit thin pages" philosophy in its own docblock — the same discipline the spec demands was already proven out in this codebase, just extended to the new tables.

**Physical-office question, asked directly before writing any schema** (the spec explicitly forbids fake `LocalBusiness`/address claims, and this codebase had no existing fact to fall back on — `site_settings.legal_registered_office_address` was still empty from the earlier Legal Phase): user confirmed **two** real offices exist and supplied both addresses — Patna, Bihar (*203, Sharda Mansion Apartment, Kailashpuri, Kankarbagh, Hanuman Nagar, Patna, Bihar 800020*) and Noida, Uttar Pradesh (*#UG-5, Provica Homes, Tower-02, Sarfabad, Noida-73, Gautam Buddha Nagar, Uttar Pradesh 201301*). Rather than overload the single-string `legal_registered_office_address` setting (a distinct fact — the one legal/registered-office address for the Terms & Privacy jurisdiction clause, not necessarily either of these two operational addresses), each real address lives on its own `cities.office_address` row (nullable, empty for every city except these two). A page renders `LocalBusiness`/`PostalAddress` JSON-LD only when its own row's `office_address` is non-empty — the sole gate, enforced structurally rather than by content-review convention. Bihar/Patna and Uttar Pradesh/Noida became Phase 1's two fully-authored, indexable pilot pairs specifically because they're the two genuine offices, not an arbitrary pick.

**Schema** (`database/schema-location-seo.sql`, `database/seed-location-seo.sql`, both new, additive): `states` (mirrors `countries`' shape — `name, slug UNIQUE, type ENUM('state','union_territory'), zone ENUM(...6 Indian zones...), intro_content, meta_title, meta_description, is_indexable, is_active, sort_order`) and `cities` (same shape plus `state_id FK`, `is_major`, `office_address`) and `location_faqs` (`visa_faqs`' exact nullable dual-scope shape, `state_id`/`city_id` instead of `country_id`/`visa_type_id`). `is_indexable` defaults to `0` on every row and is never set anywhere except by a human in the admin CMS — the explicit quality gate the spec's own §16/§28 demand, mirroring how `visa_requirements` already ships empty-until-verified per country. Seeded: all 28 states + 8 UTs as real geographic fact (name/slug/type/zone — verifiable, not authored content) with `is_indexable = 0`; Bihar and Uttar Pradesh flipped to `is_indexable = 1` with real hand-written `intro_content`/meta fields; Patna and Noida seeded as the two pilot `cities` rows (`is_indexable = 1`, real content, real `office_address`); 4 more real Bihar city names (Gaya, Muzaffarpur, Bhagalpur, Darbhanga) and 5 more real UP city names (Lucknow, Kanpur, Varanasi, Agra, Ghaziabad) seeded as `is_indexable = 0` stubs, listed as plain (unlinked) text on their state pages' "Cities We Serve" section rather than fabricated content or dead links; 6 `location_faqs` rows, genuinely different questions per state/city, not the same FAQ with a name swapped.

**Admin CMS** (`admin/pages/locations.php`, new, cloned from `admin/pages/countries.php`'s list/create/edit/toggle/delete shape, `require_permission('content.manage')`, no new permission): one file, tabbed by `?type=state|city`. Editable: `intro_content`, `office_address` (cities only, with an inline warning that filling it in adds `LocalBusiness`/"visit us" language to the live page), `meta_title`/`meta_description`, `is_indexable` (with an inline warning that it should only be flipped once real content exists), `is_active`, `sort_order`. Wired into `admin/index.php` (`case 'locations':`) and `includes/admin-layout.php`'s existing "Content" nav group, alongside Countries/Visa Types/FAQs/Embassies.

**Public router + pages**: `public/index.php` gained one `case 'visa-consultant':` line mirroring the existing `case 'visa':` line exactly. `visa-consultant/index.php` (new) mirrors `visa/index.php`'s `$segments`-based dispatch (`$segments[1]` = state slug, `$segments[2]` = city slug) to `hub.php` (all 36 states/UTs grouped by zone, every state linked regardless of `is_indexable` so nothing is ever an orphan page), `state.php` (renders for any active state, including drafts — sets `$noindex = true` when `is_indexable` is false rather than 404ing, so an admin can preview a page before flipping it live; always emits `Service` schema, never `LocalBusiness` — a state isn't a single geocoded location, so a genuine address belongs on the specific city page instead), and `city.php` (same is_indexable-gated rendering; emits `LocalBusiness`/`PostalAddress` JSON-LD only when `office_address` is set, `Service` schema otherwise; states honestly that the city is served remotely when there's no local office). `includes/data.php` gained `states_all()`, `state_by_slug()`, `cities_by_state()`, `city_by_slug()`, `location_faqs_for()` — same static-cache-per-request shape as the existing country lookups.

**Sitemap + nav**: `pages/sitemap-xml.php` adds state/city URLs filtered to `is_indexable = 1` only, matching the file's own pre-existing philosophy — verified only 5 URLs (hub + 2 states + 2 cities) appear, none of the other 34 states or 9 draft cities. `pages/sitemap.php` (human sitemap) gained a "Visa Consultant by Location" section, same filtering. `includes/footer.php` gained a compact 3-link "Popular Locations" footer group (hub + the two real pilots) — deliberately small, not a mass link dump, per the spec's own warning against exactly that.

**Two real bugs caught and fixed during verification, not shipped**: (1) `admin/pages/locations.php` line 18/28 computed `$type`/`$postType` via `in_array($_GET['type'] ?? 'state', ...) ? $_GET['type'] : 'state'` — the ternary's true branch re-read `$_GET['type']`/`$_POST['type']` directly instead of the already-coalesced value, firing an "Undefined array key" warning on any request without `type` in that specific superglobal (e.g. every POST, since the form's `action` has no query string). Fixed by coalescing once into a local variable and branching on that. (2) `bin/package-cpanel.sh`'s printed post-deploy database-import instructions were still stale from the original 4-pair list (schema/seed, crm, hrms, forex) — already caught and fixed once this session after the live production directory-listing incident (see below) — now extended again with the new `schema-location-seo.sql`/`seed-location-seo.sql` pair so the printed instructions stay accurate for future re-deploys.

**A live production incident, unrelated to this feature but handled in this same session**: the user reported `visagiri.com` serving a raw Apache directory listing of the entire application root (`admin/`, `config/`, `database/`, `auth/`, etc.) instead of the site — the live document root was pointed at the raw repo checkout instead of `public/`, with no front controller at that path and directory indexing left on. Not fixable from this sandbox (no hosting/cPanel access) — flagged to the user with the root cause and the two correct fixes (point the docroot at `public/`, or deploy the flattened `bin/package-cpanel.sh` package instead of the raw repo), and `public/.htaccess` got `Options -Indexes` added as defense-in-depth regardless of which fix they apply. Once the docroot was corrected, a second live incident (PDO access-denied, then a literal `<placeholder>` string including the angle brackets pasted into `config/database.php`'s `username` value) was talked through and resolved directly with the user — both real production credential/config edits happened on their end, not through any tool call in this session.

**Verified end-to-end via Playwright/curl against the real local dev DB, never just code review**: all 8 route combinations (hub, 2 indexable states, 2 indexable cities, 1 draft state 200s-but-noindexed, 2 bad-slug 404s) return the correct status; Patna and Noida confirmed `index, follow` with real `LocalBusiness`/`PostalAddress` JSON-LD containing the exact real addresses; a draft state (Goa) confirmed `noindex, nofollow`; FAQ scoping confirmed correct and distinct between a state page (2 state-scoped FAQs) and its city page (2 different city-scoped FAQs) from the same 6-row seed; sitemap.xml confirmed to contain exactly the 5 expected indexable URLs and no others, and confirmed still-valid XML (247 total URLs); admin CMS create → edit (toggle `is_indexable`, confirmed via direct DB query since a test-script regex window was too tight to catch it in rendered HTML) → delete round-trip against a disposable test city, all confirmed via direct DB queries after each step; 403 confirmed for a non-`content.manage` admin (`hr.admin`, temporary password swap reverted immediately after, hash confirmed restored byte-for-byte); footer "Popular Locations" group confirmed rendering correctly on `/privacy/` (not just the homepage). `php -l` clean on all 13 new/changed PHP files. All test data (the disposable city, its audit-log rows, `rate_limits`) removed and confirmed empty via direct query before this entry was written.

**Not done this pass, by deliberate decision, not oversight**: only 2 of 36 states/UTs and 2 of 11 seeded cities are indexable — the other 34 states and 9 cities exist as real geographic rows (safe to seed) but stay `noindex`'d until real, unique content is authored for each, which is future work, not automatable without producing exactly the thin/templated pages the spec forbids. Country×city combination pages, the SEO dashboard, the keyword-to-URL mapping document, and GA4/GTM analytics wiring (no analytics of any kind exists anywhere in this codebase) were explicitly out of scope for this phase and were not started.

## National Location SEO — Phase 2: 8 more states + primary cities (Delhi, Maharashtra/Mumbai, Karnataka/Bengaluru, West Bengal/Kolkata, Telangana/Hyderabad, Tamil Nadu/Chennai, Gujarat/Ahmedabad, Rajasthan/Jaipur)

Direct continuation of Phase 1 using the exact same machinery — no PHP code changed this phase at all, only `database/seed-location-seo-phase2.sql` (new, additive). Asked the user what Phase 2 should cover (more states with real content vs. country×city pages vs. an SEO dashboard); they chose more states, following the established pattern.

**Content, not code**: each of the 8 states got genuinely distinct, hand-written `intro_content` — Delhi framed around most embassies/consulates being based there, Mumbai around finance/trading, Bengaluru around IT/tech and onsite-assignment work visas, Kolkata around historic trade/education links, Hyderabad around IT/pharma, Chennai around IT/manufacturing/healthcare, Ahmedabad around its trading community and diaspora ties, Jaipur around tourism — real, generic, uncontroversial facts about each region's economic character, never a fabricated statistic and never the same paragraph with the city name swapped. One primary, fully-authored, indexable city page per state (skipping Delhi, since as a UT it already functions as a single city-state and a separate "New Delhi" page would just duplicate the state page — a real duplicate-content risk avoided rather than built and cleaned up later); 2-3 more real city names per state seeded as non-indexable stub rows (Pune/Nagpur/Nashik, Mysuru/Mangaluru, Siliguri/Durgapur, Warangal, Coimbatore/Madurai, Surat/Vadodara, Jodhpur/Udaipur) for the "Cities We Serve" list, exactly matching Phase 1's Bihar/Uttar Pradesh pattern. 9 new `location_faqs` rows, each a genuinely different question tied to that state or city's own framing (embassy-appointment prep for Delhi, business visas for Mumbai, IT work visas for Bengaluru, etc.), not a templated FAQ repeated with the name changed.

**None of these 7 new cities has a real Visagiri office** — `office_address` stays `NULL` on every one of them, so every new city page emits `Service` schema and states plainly that Visagiri's offices are in Patna and Noida and that the city is served remotely. Only Patna and Noida (Phase 1) carry `LocalBusiness`/`PostalAddress` schema, and that remains true after this phase — verified directly by grepping the rendered HTML of all 8 new pages for `LocalBusiness` and confirming zero matches.

**Verified end-to-end**: all 8 new state pages return 200 with `index, follow` (confirmed via curl) and zero `LocalBusiness` schema; FAQ scoping re-confirmed correct and distinct on a new pair (Delhi state: 2 state-scoped FAQs; Mumbai city: 1 city-scoped FAQ; Maharashtra state itself has 0 FAQs, correctly, since none were seeded at that level); sitemap.xml grew from 5 to the correct 20 location URLs (1 hub + 10 indexable states + 9 indexable cities) and remains valid XML (262 total URLs sitewide); the human sitemap page's dynamic "Visa Consultant by Location" section picked up all 10 states/9 cities automatically with no code change, confirming the Phase 1 machinery is genuinely reusable rather than needing per-batch wiring. `php -l` was not needed this phase (no PHP files touched) but re-run anyway on every location-related file as a regression check — clean.

**Not done this pass**: 26 states/UTs and 2 seeded cities (Delhi's own stub-tier neighbors aside) remain `noindex`'d drafts, same reasoning as Phase 1 — real content takes real authoring time, not a bulk operation. Country×city pages, the SEO dashboard, and analytics wiring remain out of scope, per the user's own Phase 2 scope choice.

## National Location SEO — Phase 3: 8 more states + primary cities (Punjab/Jalandhar, Kerala/Kochi, Haryana/Gurugram, Andhra Pradesh/Visakhapatnam, Goa/Panaji, Assam/Guwahati, Madhya Pradesh/Indore, Odisha/Bhubaneswar) — all 6 zones now covered

User said "Phase 3 start" — same continuation pattern as Phase 2, no new questions needed since scope was already established. Again, zero PHP changed; only `database/seed-location-seo-phase3.sql` (new, additive). This batch was deliberately chosen to cover the two zones Phase 2 missed entirely — Central (Madhya Pradesh) and Northeast (Assam) — alongside Punjab and Kerala, two states with genuinely well-documented, real reasons to matter for a visa consultancy specifically (Punjab's long-standing emigration culture to Canada/UK/Australia, Kerala's large Gulf-region expatriate population), plus Haryana (Gurugram's NCR/multinational corridor), Andhra Pradesh (its own distinct framing from sibling state Telangana — coastal trade plus a long-established US-bound Telugu diaspora, not just "IT hub" repeated), Goa (tourism economy), and Odisha (Bhubaneswar's emerging IT/education profile).

**Same discipline as every prior phase**: each state's `intro_content` is genuinely distinct — no paragraph reused with a name swap. One fully-authored, indexable primary city per state (Jalandhar chosen for Punjab over the larger Ludhiana specifically because Jalandhar has outsized real-world relevance to visa-consultancy search demand); 2 more real, unauthored stub city names per state for the "Cities We Serve" list (Ludhiana/Amritsar, Thiruvananthapuram/Kozhikode, Faridabad/Panchkula, Vijayawada/Guntur, Margao/Vasco da Gama, Dibrugarh/Silchar, Bhopal/Jabalpur, Cuttack/Rourkela). 8 new `location_faqs` rows, each genuinely tied to that state/city's own real framing (Canada/UK study visas for Punjab, Gulf work-visa documentation for Kerala, MNC business visas for Gurugram, etc.).

**No new offices**: `office_address` stays `NULL` on all 9 new city rows — none of these is a real Visagiri location, so every one of them emits `Service` schema and states plainly it's served remotely from the real Patna/Noida offices. Confirmed by grepping all 9 new page renders for `LocalBusiness` and finding zero matches, same check as every prior phase.

**Verified end-to-end**: all 9 new pages (8 state + Punjab's Jalandhar city, plus the other 7 states' own primary cities) return 200 with `index, follow` and zero `LocalBusiness` schema; sitemap.xml grew from 20 to the correct 36 location URLs (1 hub + 18 indexable states + 17 indexable cities) and remains valid XML (278 total URLs sitewide); FAQ scoping re-confirmed on a new pair (Punjab state: 1 state-scoped FAQ; Jalandhar city: correctly 0 FAQs, since none was seeded at that specific level, and the accordion section correctly doesn't render when empty). `php -l` re-run on every location-related PHP file as a regression check (none were touched) — clean. Local dev DB now has 18 of 36 states/UTs and 17 cities total, 9 of them indexable.

**Not done this pass, same reasoning as every prior phase**: the remaining 18 states/UTs stay `noindex`'d drafts until genuinely authored. Country×city pages, the SEO dashboard, and analytics wiring remain untouched and out of scope.

## National Location SEO — Phase 4: 9 more states (Chandigarh, Himachal Pradesh/Shimla, Jammu and Kashmir/Srinagar, Uttarakhand/Dehradun, Puducherry, Jharkhand/Ranchi, Dadra and Nagar Haveli and Daman and Diu, Chhattisgarh/Raipur, Tripura) — 27 of 36 now live

User said "Start phase 4" — same continuation, no new questions needed. Zero PHP changed again; only `database/seed-location-seo-phase4.sql` (new, additive). This batch worked through most of the remaining North-zone states/UTs plus a mix from South/East/West/Central, leaving mainly the smallest-population Northeast states and a few island/hill UTs (Ladakh, Andaman and Nicobar Islands, Lakshadweep, Arunachal Pradesh, Manipur, Meghalaya, Mizoram, Nagaland, Sikkim) for a future phase — deliberately not forced into this batch, since writing genuinely differentiated, non-thin content for very small population entities takes more care, not less, and rushing it risks producing exactly the kind of thin/templated pages this whole build has been designed to avoid.

**Real differentiating angle per state, same discipline as every prior phase**: Chandigarh framed around sharing Punjab's well-documented emigration culture (a genuinely accurate, well-known fact, not an invented one); Himachal Pradesh around tourism plus growing UK/Canada/Australia student-visa interest; Jammu and Kashmir kept strictly neutral and apolitical, purely about visa-consultancy service availability, no territorial commentary; Uttarakhand around Dehradun's real, well-known education-institution density; Puducherry around its genuine, uncontroversial French colonial history (safe angle for France/Schengen-visa framing); Jharkhand and Chhattisgarh both around their real steel/mining/industrial economies (Jamshedpur/Ranchi and Bhilai/Raipur are genuinely well-documented industrial hubs, not fabricated); Dadra and Nagar Haveli and Daman and Diu around its real manufacturing base and Daman/Diu's genuine Portuguese colonial history; Tripura kept modest and honest, no invented specifics.

**City-page decision this phase, made explicitly rather than defaulted**: only the 5 states with a real, separately-known capital/hub city distinct from the state name (Himachal Pradesh→Shimla, J&K→Srinagar, Uttarakhand→Dehradun, Jharkhand→Ranchi, Chhattisgarh→Raipur) got a dedicated city page. Chandigarh and Puducherry are single-city UTs already fully represented by their own state-level page (same reasoning as Delhi in Phase 2); Dadra and Nagar Haveli and Daman and Diu likewise has no second town with enough distinct material; Tripura has no clearly documented second city strong enough to avoid essentially repeating the state page. Skipping these rather than padding them out avoids fabricated specificity and near-duplicate content.

**No new offices**: `office_address` stays `NULL` on all 5 new city rows — confirmed by checking every one of the 9 new page renders for `LocalBusiness` and finding zero matches, including the long UT slug `dadra-and-nagar-haveli-and-daman-and-diu` (verified it doesn't break routing or the sitemap).

**Verified end-to-end**: all 9 new pages return 200 with `index, follow` and zero `LocalBusiness` schema; sitemap.xml grew from 36 to the correct 50 location URLs (1 hub + 27 indexable states + 22 indexable cities) and remains valid XML (292 total URLs sitewide); FAQ scoping re-confirmed correct on two new state-level pairs (Chandigarh, Puducherry), each showing exactly its own distinct FAQ. `php -l` re-run on every location-related PHP file (none touched) — clean. Local dev DB now has 27 of 36 states/UTs indexable, 22 cities.

**Not done this pass**: the 9 remaining states/UTs (mostly the smallest-population Northeast states plus Ladakh, Andaman and Nicobar Islands, Lakshadweep) stay `noindex`'d drafts, deliberately deferred rather than rushed. Country×city pages, the SEO dashboard, and analytics wiring remain untouched.
